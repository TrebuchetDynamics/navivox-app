# Hermes Wing readiness audit

Last updated: 2026-07-18.

2026-07-13 status refresh: the latest main-branch `Hermes platform smoke` run
(29129974871, head `b7b99d8`, 2026-07-10) failed three jobs. Browser smoke
failed because `hermes-smoke.spec.mjs` still asserted the pre-`fc40db6`
diagnostics chip copy `Voice: device STT -> Hermes text`; the spec now asserts
the current `Voice: device STT → Hermes` copy and the focused smoke passed
locally on 2026-07-13. iOS/macOS builds failed because `flutter_onnxruntime`
(Pocket Speech) requires minimum iOS 16.0/macOS 14.0 while the projects
targeted 13.0/10.15; deployment targets were raised in `6ca4afb`, and the
push-triggered main run 29256462998 (head `9c44607`, 2026-07-13) succeeded on
all native jobs with a validated receipt written to
`build/receipts/hermes-platform-workflow.json`. The
`Dependency and license review` job fails on every PR with
"Dependency review is not supported on this repository"; enabling the
Dependency graph in repository settings is an owner action.

This audit maps the current Hermes-only Hermes Wing client goal to concrete
evidence and remaining blockers. It is intentionally conservative: a passing
test only counts for the behavior it directly exercises. For a quick read-only
local snapshot, run `npm run hermes:readiness-audit`; that helper prints every
explicit external or deferred blocker but is not a completion receipt.

Receipt inventory for this checkout: `build/receipts/` is absent. Receipt-backed
rows below are therefore labeled historical or unverified for the current
checkout even when a dated run previously passed. The explicit Android physical,
two-gateway, server-audio, and deferred-surface blockers remain unchanged.

## Success criteria and evidence

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Provider-backed Hermes chat smoke with real model/provider credentials | `npm run hermes:provider-smoke:local` writes `build/receipts/hermes-provider-smoke.json` after a no-retry Playwright pass against a configured local Hermes home. The receipt is bound to the current `head_sha`, records a sanitized origin-only `base_url`, includes `evidence_for` entries for `provider-backed Hermes typed text turn` and `deterministic transcript voice turn`, and includes `not_evidence_for` caveats for physical Android mic, Hermes realtime/server audio, native-host receipts, platform workflow publication, deferred parity, and whole-goal completion. | Historical configured-local claim only; the current-checkout receipt is absent and provider-backed chat is unverified here. |
| Provider-backed Hermes voice smoke | Same provider smoke submits a deterministic device transcript via `wingE2EHermesSubmitVoice` and verifies a model reply. The helper caveat says this is deterministic transcript voice only, not physical microphone evidence or Hermes realtime/server audio, and the readiness audit rejects stale/malformed provider receipts. | Historical transcript-voice claim only; the current-checkout receipt is absent, and physical mic/server audio remain unverified. |
| Android speech/mic readiness | `npm run android:voice-smoke` passed on a KVM-backed `fractal_test` emulator on 2026-07-03, verifying speech recognizer availability and granted mic permission. `WING_ANDROID_SKIP_BUILD=1 WING_ANDROID_HERMES_URL=http://10.0.2.2:8642 npm run android:live-mic-prep` also installed/launched/granted mic permission on `fractal_test`. A later direct target recheck showed `adb devices` had no attached Android devices while `flutter devices` listed only Linux desktop and Chrome web. The canonical manual physical-audio checklist is `docs/runbooks/android/live-mic-smoke.md`; the live receipt helper and readiness audit reject whitespace-only observed excerpts and invalid/non-origin Hermes URLs. | Readiness/prep covered; real spoken audio not covered. |
| Android automated voice loop | `npm run android:hermes-voice-loop-smoke` writes `build/receipts/android-hermes-voice-loop-smoke.json` after an Android integration test runs `HermesChatScreen` with deterministic transcript capture, fake Hermes channel/provider replies, fake TTS, and a distinct second turn after continuous-voice re-arm. The receipt is bound to current `HEAD` and records Android device properties. | Historical no-human mechanics claim only; the current-checkout receipt is absent, and this remains no evidence for physical mic/provider/server audio. |
| Android debug APK build | `flutter build apk --debug` produced `build/app/outputs/flutter-apk/app-debug.apk`; the APK pulled back from physical target `RFCX81EJPNN` after the clean `chat_ux_qa.yaml` run exactly matches the build artifact at SHA-256 `81e44e75d0d36ba1ee5f05f4c49931a4befb6ad1ee28a7642d0cc0ca36851059`. Receipt: `/tmp/maestro-hermes-wing-chat-ux-current/receipt.json`; `npm run hermes:readiness-audit` prints the current APK SHA-256 when present. | Current installed-artifact identity and the scoped chat flow are covered. The present APK is artifact identity for that run, not evidence for later host-only source/docs changes, multi-gateway behavior, physical microphone audio, or the whole objective. |
| Linux release build | `flutter build linux --release` passes against the current dirty worktree and produces `build/linux/x64/release/bundle/wing` (host runner SHA-256 `bc85533eb4c7815f517d26427adf6105d61e098e9c9141c83ce0c108473b7883`) plus Dart payload `lib/libapp.so` (`40835690bdd93a668461a82212611df6b9bdc46c3f4918a1a8c053a34ec71a50`); the deterministic 29-file bundle manifest hash is `c568189cf03dee16787ce5870eaeac8253237a54e934407532c32ac8e3cd45a0`. The restricted host's missing GStreamer/libsecret development files were supplied without root or system mutation by extracting matching Ubuntu development packages under `/tmp` and setting only build-process `PKG_CONFIG_PATH`/`LIBRARY_PATH`. The GTK runner now owns native Hermes Wing, Window, and View menus with Settings, `Ctrl+,`, fixed-information About, Minimize, Maximize/Restore, Full Screen, and F11 actions; only Settings uses the payload-free command channel. Isolated empty HOME/XDG Xvfb runs mapped a real `Hermes Wing` window with WM class `com.trebuchetdynamics.hermes.wing`, displayed the application/window/view actions and accelerator labels, rendered Settings after both an actual menu click (`/tmp/hermes-wing-linux-native-menu-click-current/`) and Ctrl+, (`/tmp/hermes-wing-linux-native-menu-current/`), opened About after a native click (`/tmp/hermes-wing-linux-about-current/`), and under isolated Fluxbox changed exact X11 state from 1280×720 restored to 1600×1000 `_NET_WM_STATE_FULLSCREEN` through the View menu, restored through F11, changed to 1600×953 maximized, restored again, then reached `IconicState`/`_NET_WM_STATE_HIDDEN` minimized (`/tmp/hermes-wing-linux-window-controls-current/`). Against a unique credential-free loopback gateway, the current full bundle also displayed message and transcript secondary-click menus, copied the exact synthetic message, and copied exact Markdown through the system clipboard (`/tmp/hermes-wing-linux-context-menu-current/`). | Current local bundle identity, mapped-window identity, native Settings menu/shortcut, fixed-information About, minimize/maximize/restore/full-screen behavior, and desktop transcript context actions covered. This is not a clean-machine package/install, signing, update, managed-runtime, SSH, filesystem, broader menu, or production-display receipt. |
| Windows build | A dated `build/receipts/hermes-platform-workflow.json` previously recorded a watched GitHub run with a successful `Windows desktop build` job and non-expired `wing-windows-debug-bundle` artifact. The current Win32 runner now adds native Hermes Wing, Window, and View menus with Settings, `Ctrl+,`, fixed-information About, Minimize, Maximize/Restore, Full Screen, and F11; only `openSettings` crosses the same payload-free MethodChannel used by Linux/macOS. After `flutter precache --windows`, an extracted, non-system MinGW 13 toolchain cross-compiled the changed `flutter_window.cpp` and `main.cpp` to x86-64 COFF objects under `/tmp/hermes-wing-windows-cross-syntax/`; strict warnings found only an existing Flutter wrapper pessimizing-move and the generated template's unused WinMain parameters. | Current source syntax and static command/menu contract covered; the historical workflow does not cover this checkout, and no current Windows link, Flutter build, launch, menu interaction, packaging, or signing receipt exists. |
| iOS simulator build | A dated `build/receipts/hermes-platform-workflow.json` (run 29256462998, head `9c44607`, 2026-07-13) recorded a successful `iOS simulator build` job and non-expired `wing-ios-simulator-app` artifact after the deployment target was raised to 16.0 for `flutter_onnxruntime`. | Historical only; the current-checkout receipt is absent and iOS is unverified here. |
| macOS desktop build | A dated `build/receipts/hermes-platform-workflow.json` (run 29256462998, head `9c44607`, 2026-07-13) recorded a successful `macOS desktop build` job and non-expired `wing-macos-debug-app` artifact after the deployment target was raised to 14.0 for `flutter_onnxruntime`. | Historical only; the current-checkout receipt is absent and macOS is unverified here. |
| Desktop host identity, Settings, About, window, and full-screen controls | Linux registers the canonical `com.trebuchetdynamics.hermes.wing` application ID; Linux and Windows retain the Hermes Wing window/product title; macOS uses `Hermes Wing` as its visible bundle/display name while retaining the existing `wing.app` artifact path. Linux, Windows, and macOS native Settings items invoke only the allow-listed `openSettings` method over `com.trebuchetdynamics.hermes.wing/desktop_host_commands`; the Dart listener selects the existing `/settings` route and ignores unknown methods. Ctrl/Command+, selects the same existing route without server work. All three hosts also expose native About plus Minimize, Maximize/Restore (macOS Zoom), and Full Screen behavior; Linux/Windows About uses fixed local text, and window controls remain entirely host-local. Focused widget/static host-contract tests cover route/shortcut selection, fail-closed unknown commands, all three native adapters' target/channel agreement, local-only About/window/full-screen wiring, cross-host identity, and agreement with the existing macOS CI artifact path. The current Linux release bundle additionally mapped a real canonical-class/title window, visibly exposed the native Settings/About menu with Ctrl+, label, rendered Settings after native menu click and accelerator, opened the fixed-information About dialog, and produced exact Fluxbox/X11 full-screen, maximize, restore, and minimize state transitions (`/tmp/hermes-wing-linux-native-menu-click-current/`, `/tmp/hermes-wing-linux-native-menu-current/`, `/tmp/hermes-wing-linux-about-current/`, `/tmp/hermes-wing-linux-window-controls-current/`). Current Windows Settings/About/window/full-screen sources cross-compile to x86-64 COFF objects (`/tmp/hermes-wing-windows-cross-syntax/`) but cannot be run on this host. | Linux build/runtime native Settings, About, window, and full-screen controls covered locally; Windows has source-level cross-target evidence only, macOS menu behavior and Windows/macOS current-checkout host builds remain unverified, and other native menus and host adapters remain missing. |
| Browser fake Hermes smoke | Focused Playwright regression rerun of `wing-e2e.spec.mjs` plus `hermes-smoke.spec.mjs` passed on 2026-07-03 against `node serve_web.mjs` with 68 Chromium tests. The current focused `hermes-smoke.spec.mjs` now passes **3 Chromium tests** after its stale Fork label/confirmation path was aligned to Branch and approval/reconciliation sequencing, while the fixture emits the exact `run.completed` terminal event instead of leaving a false durable-run lease. It covers fake Hermes health/capabilities/sessions/branching/runs/events/approvals/tool progress/stop; desktop transcript context menus are intentionally excluded on web. | Current focused fake-server smoke covered; historical broader-browser total is not a current-checkout receipt. |
| Installed Hermes API connect smoke | `npm run hermes:live-smoke` passed on 2026-07-03 against installed local Hermes with isolated temp home and no provider credentials; Playwright reported 1 pass. The helper caveat says this is API connect/session rendering only, not provider/model evidence, not chat/voice provider smoke, not physical microphone evidence, and not whole-goal completion evidence by itself. | Covered for live connect/session surface. |
| Chat and session authorization | Chat/run/status/approval/stop transport and session create/rename/fork/delete enforce every scope declared by each exact endpoint before controls appear or network I/O begins. Authorized history supports filtered multi-selection and sequential confirmed deletion through the exact per-session DELETE contract; partial failures continue safely with a bounded summary, live-reply sessions are excluded, and unauthorized tokens receive no selection controls. Exact advertised branching requires confirmation, is hidden for active replies, blocks duplicate in-flight requests, and retains a server-accepted child with the locally loaded source transcript if the follow-up history refresh fails. Phone-panel, wide-rail, partial-failure, active-reply, authorization, branch-lifecycle, and 200% text-scale widget/channel tests cover the behavior. `scripts/maestro/session_bulk_selection_qa.yaml` physically verified selection mode, select-all, selected count, delete affordance, and non-destructive cancel on `RFCX81EJPNN`; command-only receipt: `/tmp/maestro-hermes-wing-session-bulk-selection-current/`. `scripts/maestro/session_branch_qa.yaml` physically created an isolated empty source session, confirmed an exact branch, selected its child, and found its parent marker on the same device; command-only receipt: `/tmp/maestro-hermes-wing-session-branch-current/` (**27 completed, 1 skipped**). A clean `scripts/maestro/chat_ux_qa.yaml` run on `RFCX81EJPNN` additionally passed **65 completed, 2 skipped** commands, covering local slash discovery/help, new-session empty state, multiline Telegram-style composition, a real synthetic provider turn with reasoning and token usage, Markdown transcript export, session search/no-match, and completed-turn restoration after leaving Chat; content-safe receipt and five inspected screenshots: `/tmp/maestro-hermes-wing-chat-ux-current/`. Native desktop secondary-click menus additionally expose Reply/Copy for a visible message and bounded text/Markdown export for the authorized active transcript, with Android/iOS long-press unchanged and web excluded. Focused widget tests cover desktop actions and mobile suppression; `/tmp/hermes-wing-linux-context-menu-current/` verifies exact Linux system-clipboard output against a credential-free loopback gateway. Legacy endpoint declarations with no scope metadata retain compatibility behavior. | Session authorization, branching, bulk-history interaction, and desktop transcript context actions are covered locally and physically; scoped profile-aware mutation on a separately compatible gateway remains blocked. |
| Concurrent session streams | A single authorized gateway channel can own concurrent session-scoped direct-chat or run SSE streams. Session selection preserves live local transcript state; stop targets only the selected run; approvals retain run/session identity; background failures do not replace the selected session error; voice submission completes against its originating session; session rows expose streaming and failed state; and ordinary Android resume preserves an attached stream instead of reconnecting it away. A status-confirmed running/queued stream loss stores at most 16 opaque gateway/profile/session/run leases in platform secure storage, restores the guard after process recreation, prevents duplicate submission until exact terminal status, and stores no prompt, output, credential, or transcript. Focused channel and gateway-screen tests cover these behaviors. | Covered locally; physical concurrent-run, background-resume, and process-recreation receipts remain missing. |
| Deferred server realtime audio honesty | `lib/core/hermes/policy/hermes_surface_readiness.dart` marks advertised-but-unwired server realtime voice/audio as blocked and unadvertised server audio as deferred; README states voice is local-first. | Covered by code/docs. |
| Deferred config admin honesty | Surface readiness marks config editing/admin as `deferred`; the surface-readiness dialog repeats that no mobile config mutation controls are enabled. | Covered by code/docs. |
| Deferred memory UI honesty | Hermes surface readiness marks Memory UI as `deferred`; the surface-readiness dialog repeats that no mobile memory mutation controls are enabled. | Covered by code/docs. |
| Jobs/schedules | `/tasks` exposes a gateway- and profile-scoped, read-only `GET /api/jobs` inventory only when the endpoint declares `tasks:read` and the token is granted that scope. Startup, profile reload, and manual refresh fail closed before network I/O otherwise. It preserves explicit unsupported/empty/failure states, bounded schedule metadata, and no raw server error details. Focused tests cover exact route-and-scope fail-closed behavior, gateway switching, refresh, and 200% text scale. `scripts/maestro/schedules_unsupported_qa.yaml` validates fail-closed behavior on the current saved physical-device gateway, which does not advertise `GET /api/jobs`; live inventory and refresh still require a compatible gateway. Surface readiness separately keeps schedule administration `deferred`. | Read-only inventory covered; create/pause/resume/trigger/delete and Kanban remain contract-blocked. |
| Providers and models | `/providers` uses exact scoped `/api/providers` and `/api/models` contracts for administration. When those are absent but exact `GET /v1/models` is advertised, it shows at most 128 bounded runtime model rows read-only, distinguishes the primary runtime model from route aliases, exposes only bounded resolved target/parent identifiers, discards permissions and unknown fields, and hides credential/assignment controls. Legacy ID-only model state and endpoints with no scope metadata remain readable; any declared scopes must be granted before startup or rendering. Parser/channel/widget tests cover bounds, alias relationships, fail-closed stale suppression, and 200% scale. `providers_models_unsupported_qa.yaml` physically verified the richer primary runtime model and absent mutation controls on `RFCX81EJPNN` against `BlueBlack`; receipt: `/tmp/maestro-hermes-wing-providers-runtime-model-current/`. | Rich read-only runtime model inventory covered locally and physically; compatible-gateway provider/model mutation and secret non-reveal receipt remains blocked. |
| Accessible Office | `/office` projects only authoritative `HermesGatewayDirectory` contacts into a searchable, responsive 2D workspace. Exact profile/query contracts populate named agents; incompatible gateways remain represented by the unscoped default contact rather than inferred profile requests. Cards expose bounded agent/gateway labels, explicit availability, and session counts without transcript previews or session titles; Open chat activates that exact gateway/contact and routes to Chat, while failed activation stays in Office with a bounded error. The screen has refresh, empty, no-match, current-contact, loading, phone, wide-layout, keyboard/search, and 200% text-scale behavior; it exposes no account, wallet, CEO, or mutation shadow state. Focused screen/route/shell/slash tests cover the local slice. `scripts/maestro/office_workspace_qa.yaml` physically verified More navigation, responsive Office inventory, search/no-match, and exact Open chat activation on `RFCX81EJPNN`; command-only receipt: `/tmp/maestro-hermes-wing-office-current/`. | Functional accessible 2D agent-to-chat Office slice covered locally and physically; representative/account/wallet interactions and desktop 3D host rendering remain missing. |
| Skills and toolsets | `/tools` exposes gateway-selected, read-only installed skills with bounded searchable name/description/category metadata and toolsets only when `/v1/skills` and `/v1/toolsets` are advertised. Toolsets retain bounded name/label/description, enabled/configured flags, and at most 64 normalized resolved tool names; all public fields are searchable and expandable, while unknown nested configuration is discarded and no toggle is exposed. Legacy name-only inventory remains readable; any declared skill/toolset scopes must be granted before startup or profile reload, otherwise Wing performs no catalog request and suppresses stale inventory. Parser/widget tests cover hostile-value bounds, resolved-tool search, metadata expansion, unsupported/empty/failure states, gateway switching, and 200% text scale. `scripts/maestro/tools_inventory_qa.yaml` physically verified skill search plus rich Toolsets search, a nonzero resolved-tool count, and expansion on `RFCX81EJPNN` against `BlueBlack`; receipt: `/tmp/maestro-hermes-wing-tools-resolved-current/`. | Rich read-only gateway inventory covered locally and physically; install/remove, toolset mutation, and MCP administration remain contract-blocked. |
| Persona/SOUL and attachments | Persona editing is available only when exact profile SOUL read/write routes and `profiles:read`/`profiles:write` scopes are present. Chat additionally advertises `/persona` only for an exact scoped SOUL read and shows a bounded, selectable view for the client-selected profile; unsupported gateways receive no local persona control or request. The composer supports advertised inline PNG/JPEG/GIF/WebP images and bounded UTF-8 text attachments; arbitrary media still requires opaque server resource handles. | Capability-gated profile persona plus bounded inline attachments covered locally; compatible-gateway persona read/write and arbitrary-media receipts remain blocked. |
| Gateway health | `/gateway` exposes only gateway-selected, bounded `GET /health/detailed` status when the endpoint declares `gateway:read` and the token is granted that scope. It parses the documented busy/drainable flags, active workload, update/process metadata, fixed state-database/config/model/disk/gateway/background-queue checks, and at most 32 messaging-platform names/states; unknown checks, nested platform fields, unsafe counts, paths, commands, and exception bodies are discarded. Startup and manual refresh both fail closed before network I/O otherwise. Focused parser/widget tests cover scope gating, gateway switching, stale-data suppression, hostile-value bounds, secret-field exclusion, and 200% text scale. `scripts/maestro/gateway_status_qa.yaml` covers base live status only on a compatible saved gateway; the richer diagnostics still need an independently compatible physical receipt. | Exact scoped read-only health and bounded readiness observability covered locally; richer physical evidence plus lifecycle, logs, messaging-platform configuration, drain/reload/restart remain blocked. |
| Messaging gateways and files/context folders | Surface readiness keeps messaging-platform administration and files/context folders deferred; neither receives local shadow state or client-path emulation. | Deferred pending authoritative contracts. |
| Diagnostics/log export | Bounded diagnostics export exists and explicitly labels secrets, raw logs, tool payloads, transcripts, and local paths as excluded; surface readiness separately marks raw diagnostics/log export as `deferred`. The diagnostics test seeds transcript and raw tool-payload content and asserts the export reports only counts/statuses plus the explicit `device STT -> Hermes text; server audio not wired` voice boundary. | Bounded diagnostics covered; raw logs/payloads deferred. |
| Multi-endpoint/profile management | `HermesGatewayDirectory` projects saved Hermes Agent endpoints and Hermes profiles into unified activity-ordered contacts, refreshes at most three gateways concurrently with one active streaming channel, retains non-secret cached rows offline, and keeps per-endpoint credentials in secure storage. Background summaries require exact declared and granted `profiles:read` before `/api/profiles`, and never issue profile-qualified session requests without the exact required `profile_context` query contract. Settings provides gateway-scoped rename, reconnect, confirmed removal, and inactive-gateway connection updates. Connection updates sanitize and validate the origin, reject duplicate origins and active gateways before persistence, preserve a blank write-only credential, and clear a credential only through an explicit checkbox; only that gateway refreshes afterward. `scripts/maestro/gateway_connection_settings_qa.yaml` physically verified the write-only editor and non-destructive cancel on `RFCX81EJPNN`; command-only receipt: `/tmp/maestro-hermes-wing-gateway-connection-current/` (**19 completed, 1 skipped**). The canonical static invocations `maestro check-syntax scripts/maestro/chat_ux_qa.yaml`, `maestro check-syntax scripts/maestro/multi_gateway_chat.yaml`, `maestro check-syntax scripts/maestro/gateway_profiles_unsupported_qa.yaml`, `maestro check-syntax scripts/maestro/providers_models_unsupported_qa.yaml`, `maestro check-syntax scripts/maestro/tools_inventory_qa.yaml`, `maestro check-syntax scripts/maestro/office_workspace_qa.yaml`, `maestro check-syntax scripts/maestro/schedules_unsupported_qa.yaml`, `maestro check-syntax scripts/maestro/gateway_status_qa.yaml`, `maestro check-syntax scripts/maestro/gateway_connection_settings_qa.yaml`, `maestro check-syntax scripts/maestro/session_metadata_qa.yaml`, `maestro check-syntax scripts/maestro/session_bulk_selection_qa.yaml`, `maestro check-syntax scripts/maestro/session_branch_qa.yaml`, `maestro check-syntax scripts/maestro/chat_concurrent_sessions_qa.yaml`, and `maestro check-syntax scripts/maestro/chat_process_recovery_qa.yaml` pass, and the package-script contract rejects environment-specific sensitive data; these static checks are not device-behavior evidence. | Covered locally by focused directory, screen, enrollment, settings, and static Maestro contract checks. |
| Secret hygiene for Hermes diagnostics | `test/features/hermes_chat/screens/hermes_chat_screen_test.dart` asserts diagnostics include `Secrets: excluded`, omit `Authorization`, fake transcript tokens, and raw tool payload markers, and redact dynamic metadata fields such as active session title, health strings, capability model/features/endpoints, and model names. | Covered for diagnostics export. |
| Overall Dart/widget regression | `flutter test --coverage --concurrency=1` is the canonical local receipt; exact test and coverage totals come from that run and `coverage/lcov.info`, not this durable runbook. | A current passing command covers the local regression layer but does not replace device/host receipts. |
| Static analysis | `flutter analyze` passed with no issues on 2026-07-18 against the current multi-gateway worktree. | Covered locally. |
| E2E web release build | `flutter build web --release -t lib/main_e2e.dart` passed after the closeout status refreshes and produced `build/web`. | Covered locally for web build only. |
| CI receipt path robustness | `.github/workflows/hermes-platform-smoke.yml` was published and visible as `Hermes platform smoke`. `npm run platform:workflow-smoke` dispatches, watches, and writes `build/receipts/hermes-platform-workflow.json`; the validator requires current `HEAD`, successful run conclusion, required native jobs, and non-empty non-expired native artifacts. | Validator path is covered statically; the current-checkout hosted receipt is absent. |

## Multi-gateway plan acceptance reconciliation

Here, **gateway** means one saved Hermes Agent endpoint, not a Hermes
messaging-platform gateway. This matrix maps the plan's global constraints and
task-level outcomes to a focused test or an explicit readiness deferral. Source
presence or a broad green suite is not substituted for a missing focused
behavior receipt.

| Plan acceptance criterion | Focused evidence | Readiness verdict |
| --- | --- | --- |
| Stable contact identity | `gateway_contact_test.dart`: `identity includes gateway and profile`; directory activation also selects by the full `GatewayContactId`. | Covered by focused tests. |
| Profile-less fallback contact | `hermes_gateway_directory_test.dart`: `profile-less gateway produces one default contact` and `fallback contact skips profile selection`. | Covered by focused tests. |
| Activity ordering and deterministic tie-breaking | `gateway_contact_test.dart`: `contacts sort by latest activity then stable identity`, `contacts without activity sort after active contacts`, and `equal activity sorts by gateway then profile`. | Covered for descending activity, missing activity last, and deterministic gateway/profile tie-breaking. |
| Bounded refresh triggers | Directory tests cover the three-worker limit and `foreground timer stops refreshing while paused`; the provider covers startup without opening the full channel; the contact-list widget test covers `pull to refresh invokes the refresh callback`. | Covered for launch, resume, pull-to-refresh, one foreground periodic timer, paused non-firing, and three-worker concurrency. |
| Single active streaming channel | `hermes_channel_provider_test.dart`: directory refresh does not open a channel; directory tests cover activation and disconnect-before-second-activation. | Covered locally for one full `HermesChannel`. Static tests do not claim a real SSE socket-count receipt. |
| Offline cache and gateway failure isolation | Cache round-trip/removal plus directory `offline refresh retains cached contacts and healthy results` and authentication-failure tests. | Covered by focused tests. |
| Empty contacts stay sessionless | Channel test `connect never creates a session merely by viewing an empty gateway`. | Covered by a focused transport test. |
| Gateway secret hygiene | Contact JSON test omits credentials/transcript previews; directory tests reject leaked error/key sentinels; Settings omits credential text; the package contract checks Maestro fixtures. | Covered for models, cache, errors, Settings, and static fixtures. **Explicit readiness deferral:** the manual device diagnostics/screenshot and analytics-absence inspection remains unrecorded. |
| Safe contact switching | `hermes_chat_gateway_switch_test.dart`: selected or background active runs, pending approval, and in-flight submission each require confirmation; choosing Stay preserves the active contact. | Covered by focused switch-guard tests for selected/background work, approvals, and in-flight submission. |
| Enrollment append or update | Enrollment controller tests `successful enrollment appends and reloads without deletion` and `updating a gateway preserves every unrelated gateway`. | Covered by focused append and update tests. |
| Directory generation and gateway-scoped mutation | Directory tests cover stale refresh rejection, reconnect during refresh, remove during in-flight refresh, rename/reconnect isolation, one-gateway removal, sanitized origin/credential rotation, explicit credential clearing, duplicate-origin rejection, and active-gateway fail-closed behavior. | Covered by focused tests. |
| Contact-list presentation details | View tests cover five ordered rows, opening a contact, pull-to-refresh callback wiring, the empty state with its optional connect action, list/contact refreshing affordances, trimmed multi-code-point avatar graphemes with `?` fallback, one-line ellipsized previews, normalized UTC `HH:mm` timestamps, retained offline rows, and offline semantics. | Covered by focused contact-list presentation tests. |
| Gateway-aware header and history | Gateway-switch tests cover agent/gateway header, sessions-panel opening, selecting an older session without leaving the active gateway contact, compact-phone overflow, explicit back, and system Back returning to all chats without deletion while preserving the active-work guard. | Header, history selection, and both back paths are covered by focused tests. |
| Gateway management and lifecycle integration | Enrollment, directory, gateway-switch, first-connect endpoint, and Settings tests cover append/reload, active-only resume, completed-turn summary refresh, rename, reconnect, inactive-gateway origin/credential rotation without credential reveal, explicit credential clearing, confirmed removal, endpoint-chip deletion with directory reload, and preservation of another gateway. | Covered by focused lifecycle and gateway-scoped management tests plus the non-destructive physical connection-editor receipt. |
| Documentation and local validation | README/changelog/runbook changes exist; serialized coverage, formatting, static analysis, package contracts, and Maestro syntax checks use the commands and current receipts above. | Covered when those current local receipts pass; this is not Android behavior evidence. |
| Android and two-gateway receipts | The debug APK was built/installed previously; static Maestro flows exist and parse. | **Explicit readiness deferral:** current-worktree Android installation and the manual two-gateway 3+2-profile, offline-isolation, older-session, single-stream, and no-secret-leak receipt require explicit device/fixture authorization. |

The plan's commit steps are delivery mechanics, not behavior acceptance. They are
explicitly deferred by the owner instruction for this worktree; no commit, push,
or external publication is counted as evidence.

## Open blockers

1. **Hermes server realtime audio** — not implemented in Hermes Wing; voice remains
   device STT -> Hermes text. Automated Android voice-loop evidence does not
   claim server audio.
2. **Deferred product surfaces** — config editing/admin, Hermes memory UI,
   jobs/schedules admin, messaging gateways, richer profile-builder fields,
   arbitrary media/resource uploads, files/context folders, and raw log export remain outside
   the implemented Hermes mobile MVP. Capability-gated profile create, clone,
   rename, persona/SOUL edit, and delete are implemented for each saved gateway;
   unsupported gateways remain read-only/unavailable rather than gaining local
   shadow profiles. See
   [Gateway profile management and limitations](../product/gateway-profile-management.md).
3. **Polish/hardening** — SSE reconnect/drop edge cases, offline/auth-expired UX,
   session search/grouping, queued follow-ups, and mobile approval/error/session
   sheet polish remain improvement work after the core receipt blockers.
4. **Real spoken Android microphone receipt** — the active closeout still
   requires physical-audio/provider/TTS/re-arm evidence from an audio-capable
   Android target. The deterministic Android voice-loop receipt and provider
   transcript smoke are useful automated gates, but both explicitly say they are
   not physical-mic evidence.

## Current completion audit verdict

The active Hermes Wing goal is **not complete**. Current evidence maps to
the explicit objective as follows:

| Objective item | Concrete artifact/evidence inspected | Verdict |
| --- | --- | --- |
| 1. Complete chat and sessions | `HermesApiChannel` now owns concurrent session-scoped streams, exact run/approval/stop routing, bounded reasoning/tool/run-usage state, secure accepted-run leases persisted before event-stream attachment and restored to their owning session across process recreation, capability-gated Ctrl/Command session-history and new-session desktop shortcuts, queue/retry, voice/TTS, attachments, session CRUD/search/grouping/export, background status UI, epoch-second timestamp normalization for correct ordering/readable display, and bounded session-reported token/cache/reasoning/cost/tool/API/lifecycle details in both the details sheet and portable transcript metadata. Session history now also supports capability-gated confirmed branching plus filtered multi-selection and confirmed sequential deletion in both the phone panel and wide desktop rail. Branching excludes live replies, blocks duplicate in-flight requests, and preserves a server-accepted child through history-refresh failure; deletion excludes live replies and reports partial failures without raw server errors. Physical receipts: `/tmp/maestro-hermes-wing-session-branch-current/` for isolated exact branching and `/tmp/maestro-hermes-wing-session-bulk-selection-current/` for non-destructive selection. Focused parser/transport/widget tests pass. The latest APK installed on `RFCX81EJPNN`; `session_metadata_qa.yaml` physically verified non-empty counters, readable normalized timestamps, redacted copy, and no user-authored preview in details, retaining only non-content command receipts at `/tmp/maestro-hermes-wing-session-metadata-final-redacted/`. `chat_concurrent_sessions_qa.yaml` physically verified session switching with a background stream, an independently completed foreground run, and an independently stoppable original run; only non-content command receipts remain at `/tmp/maestro-hermes-wing-chat-concurrent-final-pass/`. `chat_process_recovery_qa.yaml` physically killed Wing during a live run, reopened the leased non-default session, exposed Reconnect without duplicate Retry or stale Stop, and retained only non-content command receipts at `/tmp/maestro-hermes-wing-chat-process-recovery-verified/`. A clean `chat_ux_qa.yaml` run physically passed **65 completed, 2 skipped** commands for slash help/discovery, new session, multiline composition, real synthetic provider response plus usage, Markdown export, search/no-match, and completed-turn restoration; inspected receipt: `/tmp/maestro-hermes-wing-chat-ux-current/`. Native desktop secondary-click message Reply/Copy and exact text/Markdown whole-chat clipboard output pass widget tests and a current Linux bundle receipt at `/tmp/hermes-wing-linux-context-menu-current/`. | **Partial.** Clarify, runtime server-command discovery, opaque resource/context-folder handles, authoritative rate-limit/context-window metadata, and undo are missing. |
| 2. Validate gateway-scoped profiles | Exact scoped profile list/create/clone/rename/delete/SOUL contracts, profile query context, opaque revisions, stale-write refresh, and background-summary fail-closed behavior are implemented and tested. | **Partial.** The saved physical gateway does not advertise the compatible scoped CRUD/SOUL contract, so live mutation and persona validation remain missing. |
| 3. Providers and models | Exact scoped administration UI exists; the exact `/v1/models` read-only fallback now preserves bounded primary/route-alias relationships while discarding permissions and unknown payloads, and passed physically on `RFCX81EJPNN` against `BlueBlack` (`/tmp/maestro-hermes-wing-providers-runtime-model-current/`). | **Partial.** Compatible live credential/assignment mutation and real-device secret non-reveal evidence remain missing. |
| 4. Discover, Skills, Tools, and MCP | Gateway-selected bounded/searchable installed Skills plus rich read-only Toolsets (labels/descriptions, enabled/configured state, resolved tool names) pass locally and physically (`/tmp/maestro-hermes-wing-tools-resolved-current/`). Declared scopes fail closed before HTTP; legacy scope-free and name-only catalogs remain compatible. | **Partial.** Discover, skill install/remove, toolset mutation, and MCP discovery/lifecycle/configuration have no authoritative scoped contracts. |
| 5. Memory | Surface readiness explicitly defers Memory and exposes no shadow mutation state. | **Missing/contract-blocked.** No authoritative remote memory entries/capacity/provider contract is advertised. |
| 6. Schedules and Kanban | Exact declared/granted `tasks:read` gates gateway/profile-scoped read-only `/api/jobs`, with bounded inventory, refresh, and fail-closed tests. | **Partial.** Current physical evidence is unsupported/fail-closed rather than live inventory; schedule mutations and all Kanban contracts are missing. |
| 7. Gateway administration | Exact scoped `/health/detailed` read-only status, gateway selection, refresh, redaction, and stale-data suppression are implemented. The parser/UI bounds documented busy/drainable state, active workload, process/update metadata, fixed readiness checks, and messaging-platform connection states while dropping unknown/private fields. Local saved-gateway administration now supports sanitized inactive-gateway origin updates plus write-only credential preserve/rotate/explicit-clear semantics, with duplicate-origin and active-work fail-closed guards. Focused directory/settings/parser/widget tests pass; `gateway_connection_settings_qa.yaml` physically verified the write-only editor and cancel path on `RFCX81EJPNN` (`/tmp/maestro-hermes-wing-gateway-connection-current/`). Base health has prior physical evidence, but richer fields do not yet have a compatible live receipt. | **Partial.** Live credential rotation was intentionally not performed; richer health evidence plus Hermes Agent lifecycle, logs, messaging-platform configuration, server key administration, drain/reload/restart, and revision-safe apply contracts are missing. |
| 8. Office, accounts, and wallets | `/office` now provides a functional accessible 2D projection of authoritative gateway contacts, searchable status/session-count cards, refresh and fallback/default-contact handling, and exact contact activation into Chat without exposing transcript previews or inventing Office state. It is reachable from More, the desktop rail, and local `/office`; focused screen, route, shell, slash, failure-redaction, and 200%-scale tests pass. `office_workspace_qa.yaml` physically passed on `RFCX81EJPNN`, covering More navigation, search/no-match, and Open chat; command-only receipt: `/tmp/maestro-hermes-wing-office-current/`. | **Partial.** Representative interactions, Hermes One accounts/sync, and backend-managed wallets/balances remain missing or contract-blocked; desktop 3D rendering remains host-blocked. |
| 9. Desktop host integrations | Host requirements for managed runtime, SSH trust, filesystem grants, menus/windows, packaging, and signed updates are documented. Shared Flutter desktop interaction includes tested Ctrl/Command session-history and capability-gated session-create shortcuts. Bounded native adapters now give Linux, Windows, and macOS actionable Settings menu items that select Wing's existing `/settings` route over the same payload-free MethodChannel; all three also expose native About and Minimize/Maximize/Restore/Full Screen behavior, with fixed local-only About text on Linux/Windows and entirely host-local window controls. macOS brings the existing window forward, unknown methods fail closed, and Ctrl/Command+, selects the same route without server work. Linux now registers the canonical Hermes Wing application ID, and all three desktop hosts retain canonical visible product/window identity. Focused Dart navigation, desktop/mobile context-menu, and static native-contract tests pass. The current Linux release bundle also built successfully and, under isolated empty HOME/XDG Xvfb sessions, mapped the canonical WM class/title, exposed the native menu and accelerator label, rendered Settings after both native click and `Ctrl+,`, opened About, and emitted exact X11 full-screen/restore/maximize/restore/minimize state transitions (`/tmp/hermes-wing-linux-native-menu-click-current/`, `/tmp/hermes-wing-linux-native-menu-current/`, `/tmp/hermes-wing-linux-about-current/`, `/tmp/hermes-wing-linux-window-controls-current/`). The current full Linux bundle also physically verifies message Reply/Copy and whole-chat text/Markdown secondary-click menus with exact clipboard hashes (`/tmp/hermes-wing-linux-context-menu-current/`). The changed Windows Settings/About/window/full-screen runner sources cross-compile to x86-64 COFF objects with MinGW 13 (`/tmp/hermes-wing-windows-cross-syntax/`). | **Partial/host-blocked.** The Windows and macOS menu slices still lack current native-host build/interaction receipts. Managed runtime, SSH, filesystem grants, equivalent full native menus/windows, clean-machine packaging/signing, updater and GPU recovery remain absent; desktop transcript context actions exist, but host-native editable-field and broader non-transcript context integration remain unverified across Linux, Windows, and macOS. |

Do not promote this audit, green tests, APK hashes, configured Hermes home
presence, workflow YAML, or individual physical flows to whole-goal completion
evidence. Every one of the nine objective rows above must be validated or receive
an approved platform exclusion; the current partial, contract-blocked, and
host-blocked rows keep the goal active.

## Current blocker detail

The read-only helper currently expands the remaining blockers into these
non-overlapping categories:

- Provider-backed smoke receipt: configured Hermes home presence is only
  informational. Strict closeout requires a current
  `npm run hermes:provider-smoke:local` receipt with configured model/provider
  credentials. The readiness audit now requires the provider receipt to match
  current `HEAD`, carry a sanitized origin-only `base_url`, report zero
  Playwright retries, include explicit `evidence_for` labels for typed text and
  deterministic transcript voice, and include `not_evidence_for` caveats for
  Android physical mic, Hermes server audio, native hosts, platform workflow,
  deferred parity, and whole-goal completion. Deterministic transcript voice
  still is not physical microphone or Hermes server-audio evidence.
- Android automated voice-loop receipt: strict closeout requires
  `build/receipts/android-hermes-voice-loop-smoke.json` from
  `npm run android:hermes-voice-loop-smoke`. The receipt must match current
  `HEAD`, record Android device properties, prove the two deterministic voice
  turns and fake TTS outputs, and include `not_evidence_for` caveats for
  physical microphone audio, provider-backed replies, server audio,
  native-host receipts, platform workflow publication, deferred parity, and
  whole-goal completion.
- Native/host receipts: the current Linux dirty-worktree release bundle now
  builds with runner, Dart payload, and full-manifest hashes recorded above. It
  has isolated mapped-window/Settings-shortcut receipts at
  `/tmp/hermes-wing-linux-native-menu-click-current/` and `/tmp/hermes-wing-linux-native-menu-current/`, plus full-bundle transcript-context evidence at
  `/tmp/hermes-wing-linux-context-menu-current/`. This does not prove package
  installation, signing, updates, managed runtime, SSH, filesystem grants, or
  production display behavior. A dated platform workflow previously covered
  Windows/iOS/macOS. Current Windows menu sources additionally pass a local
  cross-target syntax compile at `/tmp/hermes-wing-windows-cross-syntax/`, but
  that is not a link, Flutter build, launch, or interaction receipt; the
  current-checkout native hosts remain unverified.
  When Android is missing, the helper prints `flutter devices`, `flutter
  emulators`, and `emulator -accel-check` output as context only, not Android
  voice-loop or physical live-mic receipt evidence.
- Voice/audio: automated Android voice-loop mechanics are covered only as a
  no-human regression gate; real spoken Android physical-mic evidence remains a
  blocker until recorded. Hermes realtime/server audio is not implemented, so
  voice remains device STT -> Hermes text. The future server-audio closeout
  receipt is written by `npm run hermes:server-audio-receipt` to
  `build/receipts/hermes-server-audio-smoke.json`; it must match current `HEAD`,
  prove `hermes_realtime_or_audio_api`, `client_audio_to_hermes_server_audio`,
  `hermes_server_audio_to_client_playback`, provider reply, playback, round trip,
  `device_stt_used=false`, `local_tts_only=false`, safe short non-empty
  prompt/reply excerpts with the provider reply distinct from the prompt, no
  secret leaks, and explicit `not_evidence_for` caveats before it can clear only
  the server-audio blocker.
  Do not run that receipt helper until Hermes server audio is actually wired and
  observed; it is a manual evidence recorder, not an implementation.
- Deferred Hermes surfaces: config editing/admin, broader memory UI,
  jobs/schedules admin, messaging gateways, arbitrary attachment/media
  resources, files/context folders, and raw diagnostics/log export. Exact
  scoped profile persona read/edit is implemented separately. Multi-endpoint/profile management is
  implemented locally with secure per-endpoint credential storage.
- Polish/hardening: current implemented Hermes surfaces have focused coverage
  for SSE reconnect/drop edge cases, offline/auth-expired UX, session
  search/grouping, queued follow-ups, and mobile approval/error/session sheet
  polish. Keep adding focused tests when new surfaces are wired or regressions
  are found.

## Do not count as completion

- Android `voice-smoke` by itself: it proves recognizer/mic permission readiness,
  not spoken audio capture.
- Android `live-mic-prep` by itself: it installs/launches/grants permission and
  prints a checklist; it is not a spoken-audio receipt.
- Deterministic Android voice-loop smoke by itself: it proves Android UI loop
  mechanics, deterministic transcript submission, fake TTS callback, and re-arm;
  it does not prove physical microphone input, provider-backed replies, or Hermes
  realtime/server audio.
- Installed-Hermes live connect smoke by itself: it proves API connect/session
  rendering against an isolated or configured API server, not provider/model
  behavior, chat/voice provider smoke, or physical microphone evidence.
- Provider transcript voice by itself: it proves device-transcript-to-Hermes text
  flow with a model reply, not microphone or Hermes realtime audio.
- Configured Hermes home presence by itself: it suggests a local provider smoke
  may be runnable, but only an actual `npm run hermes:provider-smoke:local`
  receipt proves provider-backed chat/transcript voice.
- Workflow YAML by itself: it proves a planned CI path, not a native-host build
  receipt, even when the YAML parses and has timeouts/artifact uploads.
- Workflow dispatch by itself: `WING_WATCH_WORKFLOW=false` proves only that
  dispatch was requested; a missing visible run id or unwatched run is not a
  platform receipt. Only a watched successful run with required artifacts and the
  validated `build/receipts/hermes-platform-workflow.json` receipt can satisfy
  the workflow/native-host portion. Collect successful job status and artifacts
  with `gh run view` before claiming Windows/iOS/macOS/Android/Linux readiness.
