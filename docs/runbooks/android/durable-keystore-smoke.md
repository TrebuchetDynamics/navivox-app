# Android Durable Keystore Smoke

Validate durable reconnect key storage on Android before release.

1. Install the app on a trusted Android device.
2. Pair with a Gormes gateway that advertises durable reconnect.
3. Confirm a non-secret public key is registered and no pairing token is stored.
4. Restart the app and verify reconnect readiness remains available.
