# Navivox TODO

[IN PROGRESS] Hermes Agent-first Navivox transition — 2026-07-01
  problem: `main` must move from the preserved Gormes `/v1/navivox/*` runtime to direct Hermes Agent API server operation without leaking API keys or keeping the wrong Profile contact/Gateway domain model alive.
  plan: follow `docs/product/hermes-agent-interface-plan.md`, `docs/adr/0006-hermes-agent-first-runtime.md`, and `docs/adr/0007-native-hermes-channel-not-navivox-channel-adapter.md`; first build `lib/core/hermes/` client/config/capability/SSE fixtures (done), then a native `HermesChannel`/`HermesApiChannel` (not a `NavivoxChannel` adapter — the owner rejected that transition-seam shape and asked for a Hermes-native domain model instead, taking `fathah/hermes-desktop` as architecture reference), then Hermes chat/session/continuous-voice screens, then convert setup to Hermes base URL + API key secure storage, hide unsupported Gormes-era surfaces, and finally remove Gormes-only mainline paths after Hermes tests are green.
  progress (2026-06-25): added the first Hermes API fixture slice with URL/header normalization, typed health/capabilities/session/message models, `HermesTransportPolicy`, and a pure Dart Hermes SSE decoder that preserves multiline data, handles `[DONE]`/`done`, and skips malformed JSON stream events.
  progress (2026-07-01): added `HermesApiClient.streamSessionChat` plus a streaming POST transport (io/web/stub) and `HermesSseEventDecoder.decodeStream`/`decodeJsonEventStream` for live SSE consumption. Added the native `lib/core/hermes/channel/` module: `HermesChannel` interface + `HermesChannelState` (sessions/messages/voice-runs, no Gormes-era fields) and `HermesApiChannel` implementing it — connect (health+capabilities+list/create session with a `navi-<timestamp>-<uuid>` client id), session select/create, `sendText` with streamed assistant-delta accumulation and post-stream reconciliation against `GET /api/sessions/{id}/messages`, and the continuous-voice run lifecycle (`startVoiceRun`/`stageVoiceRunTranscript`/`submitVoiceRun`/`cancelVoiceRun`/`failVoiceRun`) reusing the already-generic `NavivoxVoiceRun` model, wired so a submitted device transcript is sent as a Hermes text turn end to end.
  validation: `flutter analyze` (no issues); `flutter test --concurrency=1` (945 pass, up from 927); `flutter pub get` after adding no new runtime deps (used the already-present `uuid` package for session/voice-run ids).
  progress (2026-07-01, later): added the Hermes chat/session UI slice — `lib/features/hermes_chat/` with `HermesVoiceRunController` and `hermesContinuousVoiceReplyToSpeak` (mechanical ports of the Gormes `VoiceRunController`/`continuousVoiceReplyToSpeak`, retyped to `HermesChannel`/`HermesChatTurn`; the Hermes turn model's own per-turn `status` made the reply policy simpler — no separate `turnComplete` flag needed), `hermesChannelProvider` (Riverpod, parallel to `navivoxChannelProvider`, no auto-reconnect yet), and `HermesChatScreen` (manual connect form, session picker, streamed transcript, composer, push-to-talk mic, and a continuous-voice toggle that captures → sends → speaks the reply via TTS → re-arms capture, looping hands-free). Wired an additive `AppRoutes.hermes` (`/hermes`) route into the existing `ShellRoute` — reachable by URL, not yet in the bottom nav, and does not touch the Gormes-first `redirect`/`initialLocation` default.
  progress (2026-07-01, verification): ran a real `flutter run -d web-server` build (not just `flutter analyze`/widget tests) and caught a real bug `flutter analyze` missed: `headers.forEach(request.setRequestHeader)` in `hermes_api_transport_web.dart` is an illegal tear-off of an external JS-interop extension member under the actual web compiler (works fine under `flutter analyze`, fails under `dart2js`/`dartdevc`) — fixed in both the pre-existing `_request` helper and the new `defaultPostStream` by using an explicit closure. Chrome browser automation was unavailable in this environment (extension not connected), so the running screen was not visually eyeballed in a browser; verification for `HermesChatScreen` is widget-test-only (`test/features/hermes_chat/screens/hermes_chat_screen_test.dart`, 5 tests including the full continuous-voice loop) plus the real (non-mocked) `flutter run -d web-server` compile pass after the fix.
  validation (later): `flutter analyze` (no issues); `flutter test --concurrency=1` (963 pass, up from 945); real `flutter run -d web-server` compiles and serves after the JS-interop fix (not opened in a browser — no Chrome automation available here).
  progress (2026-07-01, setup-flow): added `lib/core/hermes/setup/`: `HermesEndpointStore` interface + `EmptyHermesEndpointStore` default (mirrors the `DurableCredentialStore`/`EmptyDurableCredentialStore` convention) and `SecureHermesEndpointStore` — base URL in `SharedPreferences`, API key only in `FlutterSecureStorage`, never both in the same store. `hermesChannelProvider` now auto-connects from any saved endpoint on creation (`hermesAutoConnect`, unit-tested against fakes); `HermesChatScreen` saves the endpoint after a successful manual connect and clears it via a new disconnect action. The concrete `SecureHermesEndpointStore` itself has no dedicated test, matching the existing convention for `SecureStorageDurableCredentialStore` (also untested directly) — it's thin platform-plugin glue exercised through the higher-level auto-connect/save-on-connect tests instead.
  progress (2026-07-01, run transport): added `/v1/runs` capability-gated run transport. `HermesApiClient` gained `startRun`/`runEvents`/`respondApproval`/`stopRun`, backed by a new GET-streaming transport (io/web/stub, refactored to share the POST-streaming implementation) since `run_events` is `GET /v1/runs/{run_id}/events`, not POST. `HermesApiChannel.sendText` now checks `HermesTransportPolicy.supportsRunsTransport` on the connected capabilities and uses `/v1/runs` instead of `/api/sessions/.../chat/stream` when advertised: `message.delta` still appends assistant text through the same `HermesChatTurn` accumulation path, `tool.started`/`completed`/`failed` are recorded as lightweight system-authored turns (not a full tool-card model — deferred, see below), and `approval.request` is emitted through a new `Stream<NavivoxApprovalRequest> get approvalRequests` (reusing the already-generic `NavivoxApprovalRequest` model, same reuse call as `NavivoxVoiceRun` earlier). Added `stopActiveTurn()` (calls the server `/v1/runs/{run_id}/stop`, distinct from the existing client-only `cancelActiveTurn()`) and `respondToApproval()` (maps the boolean `approved` flag to Hermes's `once`/`deny` decisions; `session`/`always` are not exposed yet). `HermesChatScreen` renders a required approval banner (approve/deny) and a stop control while a turn is streaming — required before shipping run transport per the interface plan's explicit warning not to enable it until the UI can render active approval prompts.
  validation (run transport): `flutter analyze` (no issues); `flutter test --concurrency=1` (975 pass, up from 963).
  progress (2026-07-01, approval decisions): added `HermesApprovalDecision` (`once`/`session`/`always`/`deny`, matching the interface plan's documented choices — the enum's `.name` maps 1:1 to the `decision` field Hermes expects, so no manual string mapping was needed). `HermesChannel.respondToApproval` and `HermesApiChannel.respondToApproval` now take a `HermesApprovalDecision` instead of a boolean `approved` flag (a signature change safe to make because both were added this session — nothing outside this slice's own code depended on the boolean). `HermesChatScreen`'s approval banner now renders all four choices (Deny / Allow for session / Always allow / Approve once) instead of a plain approve/deny pair.
  validation (approval decisions): `flutter analyze` (no issues); `flutter test --concurrency=1` (976 pass, up from 975).
  progress (2026-07-01, tool-progress cards): added `HermesToolCall` (name/status/preview/result) and a `HermesTurnKind` (`text`/`toolCall`) on `HermesChatTurn`, replacing the plain-text stand-in for tool events. `HermesApiChannel` tracks each tool invocation by a `${runId}:${toolName}` call id — matching hermes-desktop's `chatToolEventFromRunEvent` synthesis, since Hermes run events carry no guaranteed-stable per-call id — so `tool.started` inserts one card-turn just before the assistant reply and `tool.completed`/`tool.failed` update that same turn in place instead of appending duplicates. `HermesChatScreen` renders `toolCall`-kind turns as a status card (icon + name + preview/result) instead of a chat bubble.
  validation (tool-progress cards): `flutter analyze` (no issues); `flutter test --concurrency=1` (978 pass, up from 976).
  progress (2026-07-01, nav entry): added `/hermes` to `AppShellPresentation` as a first-class rail and mobile bottom-nav destination (Hermes first; Memory moved to mobile overflow to keep the four-primary compact bottom bar). Per the owner decision, this stayed additive: `initialLocation` still preserves the legacy Gormes setup path, but explicit `/hermes` navigation is no longer redirected away just because no Gormes server is configured.
  validation (nav entry): focused router/shell tests (13 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` via `npm test` (979 pass, up from 978); `git diff --check` (pass).
  progress (2026-07-01, endpoint hints): added inline cross-platform Hermes base URL hints to the connect form: local desktop/Linux/Windows/iOS simulator `127.0.0.1`, Android emulator `10.0.2.2`, and physical device LAN/VPN/Tailscale URL. This follows the Hermes Desktop-inspired local/remote setup guidance without building a larger setup wizard yet.
  validation (endpoint hints): `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (13 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` via `npm test` (979 pass); `git diff --check` (pass).
  progress (2026-07-01, capability strip): added a connected-state Hermes capability summary in `HermesChatScreen`, sourced from `HermesChannelState.capabilities` and `HermesTransportPolicy`: model name, run/tool-progress versus session-chat transport, and the local-device voice boundary when Hermes realtime voice is not advertised.
  validation (capability strip): `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (14 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` via `npm test` (980 pass, up from 979); `git diff --check` (pass).
  progress (2026-07-01, real browser Hermes smoke): added `playwright/tests/regression/hermes-smoke.spec.mjs` plus a local Hermes HTTP/SSE fake in `serve_web.mjs` for the e2e web bundle. The spec opens `/#/hermes` in Chromium and verifies both the disconnected connect form/hints and a connected Hermes session through the real web `HermesApiChannel` transport with capability strip, text exchange, approval prompt/response UI, stop control hitting `/v1/runs/{run_id}/stop`, streamed tool-progress card, new-session creation, and device-voice-transcript submission rendered as a Hermes text turn through the real browser accessibility tree.
  validation (real browser Hermes smoke): `node --check serve_web.mjs` (pass); `node --check playwright/tests/regression/hermes-smoke.spec.mjs` (pass); `flutter build web --release -t lib/main_e2e.dart` (pass); `node serve_web.mjs` + `npx playwright test --config=playwright.config.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list` (2 pass).
  progress (2026-07-01, Gormes deprecation notice): per owner decision (asked what's next now that the Hermes MVP is built out; live browser/Android verification remains the only other item and is environment-blocked), started hiding/deprecating Gormes-only screens. Added a reusable `GormesLegacyNotice` (`lib/shared/widgets/gormes_legacy_notice.dart`): a small informational banner ("This is a legacy Gormes screen. Navivox is moving to Hermes Agent.") with an "Open Hermes" action that navigates to `/hermes`. Wired it into the Gormes-era top-level screens: `ProfileContactsScreen` (Chats), `ServersScreen` (Gateways), `AgentsScreen` (Profiles), `MemoryDashboardScreen`, and `ConfigScreen`. Purely additive, no Gormes functionality removed.
  validation (Gormes deprecation notice): focused screen/widget tests for Servers/Agents/Memory/Config/ProfileContacts/notice (36 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (985 pass, up from 980).
  progress (2026-07-01, platform smoke runbook/scaffolds): added `docs/runbooks/hermes-platform-smoke.md` with build gates and live-smoke steps for web, Android, Linux desktop, Windows/iOS host runners, and live Hermes Agent validation. Added Flutter-generated iOS and Windows platform folders with `--no-overwrite`; iOS `Info.plist` includes microphone and speech-recognition usage descriptions for continuous voice.
  validation (platform build gates): `test/platform/platform_scaffold_test.dart` (2 pass, verifies iOS voice permission copy and Windows scaffold files); `flutter build web --release -t lib/main_e2e.dart` (pass); `flutter build apk --debug` (pass, artifact at `build/app/outputs/flutter-apk/app-debug.apk`); `flutter build linux` (blocked by missing native package `libsecret-1>=0.18.4` required by `flutter_secure_storage_linux`, documented in the runbook); `flutter build windows` (blocked: Windows host required); `flutter build ios` (blocked: not available from this Linux Flutter toolchain, macOS/Xcode required).
  progress (2026-07-01, host-platform smoke runbook): documented the host gates that cannot all run in this Linux container: Ubuntu analyze/tests/web e2e build/Hermes browser smoke/Android debug APK/Linux release build with `libsecret-1-dev`; Windows desktop debug build on a Windows runner; iOS simulator debug build on a macOS runner. A GitHub Actions workflow draft was prepared but not shipped because the current push credential lacks GitHub `workflow` scope; host CI remains an owner follow-up.
  validation (host-platform smoke runbook): `flutter test --concurrency=1 test/platform/platform_scaffold_test.dart` (2 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (986 pass, up from 985); `git diff --check` (pass). Remote Windows/iOS/Linux host-runner receipts remain pending.
  progress (2026-07-01, Hermes-first startup/docs): changed fresh no-Gormes-server startup to land at `AppRoutes.hermes` instead of the legacy Gormes setup path, added `AppRoutes.isHermesLocation()` so Hermes query/deep links bypass the no-Gormes-server setup redirect safely, preserved seeded legacy Gormes sessions starting at Chats for transition tests/users, and kept legacy Gormes setup reachable explicitly at `/setup`. Refreshed `README.md`, `pubspec.yaml`, and `package.json` to describe Navivox as the Hermes Agent companion app with local continuous voice; Gormes surfaces are now documented as legacy/preserved transition UI.
  validation (Hermes-first startup/docs): `flutter test --concurrency=1 test/router/app_routes_test.dart test/router/app_router_test.dart test/e2e/connect_and_talk_web_e2e_test.dart` (12 pass); focused setup/readme/e2e regressions passed; `flutter analyze` (no issues); `flutter test --concurrency=1` (986 pass); `git diff --check` (pass).
  progress (2026-07-01, `flutter build linux` unblocked): the "blocked by missing `libsecret-1-dev`" note above (this container has no passwordless `sudo`, so the package can't be `apt-get install`ed) is now resolved without root: `apt-get download libsecret-1-dev libgcrypt20-dev libgpg-error-dev` (download-only, no root needed), `dpkg -x` each into a local prefix, rewrite the extracted `.pc` files' `prefix=/usr` to point at the local prefix, fix the dev package's `libsecret-1.so -> libsecret-1.so.0` symlink (dangling inside the extracted prefix, since the versioned `.so.0` only exists system-wide via the already-installed `libsecret-1-0` runtime package) to point at the real system file, then `rm -rf build/linux` (a stale `CMakeCache.txt` from the earlier failed attempt keeps a cached `find_library` `NOTFOUND` even after the symlink is fixed) and rebuild with `PKG_CONFIG_PATH`/`CPATH` pointing at the local prefix. Full recipe in `docs/runbooks/hermes-platform-smoke.md`. Confirmed via `ldd` that the resulting binary and plugin `.so` link the real system `libsecret-1.so.0` at runtime — the local prefix is build-time-only, not a runtime dependency of the shipped binary.
  validation (`flutter build linux` unblocked): `flutter build linux` — pass, artifact at `build/linux/x64/release/bundle/navivox`; `ldd` on the binary and `libflutter_secure_storage_linux_plugin.so` both show `libsecret-1.so.0 => /lib/x86_64-linux-gnu/libsecret-1.so.0`; `flutter analyze` (no issues); `flutter test --concurrency=1` (986 pass).
  progress (2026-07-01, read-only Hermes catalog): added minimal read-only Hermes catalog support from the native API server endpoints already advertised by `/v1/capabilities`: `HermesApiClient.listModels()` parses `/v1/models`, `listSkills()` parses `/v1/skills`, and `listEnabledToolsets()` parses `/v1/toolsets`. `HermesApiChannel.connect()` loads those only when the endpoint is advertised and treats failures as non-blocking so chat/session connection still works. `HermesChannelState` carries the catalog names, and the connected capability strip shows model names plus skills/toolsets counts as a first small Hermes Desktop-style operator surface. The web e2e Hermes fake now advertises/serves `/v1/models`, `/v1/skills`, and `/v1/toolsets`, the browser smoke asserts the catalog chips through Flutter semantics, and `README.md` now lists the read-only catalog surface.
  validation (read-only Hermes catalog): focused Hermes API/channel/screen tests (35 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (987 pass, up from 986); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter build web --release -t lib/main_e2e.dart`; `npx playwright test --config=playwright.config.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list` (2 pass); `git diff --check` (pass). Onklaud checkpoint: operational status; advisory loop ran against `/tmp/navivox-onklaud-checkpoint-20260701-hermes-catalog.md` and passed but surfaced only pass/fail summary; final gate `/tmp/navivox-onklaud-gate-20260701-hermes-catalog-v2.md` passed 10/10 with only a generic edge-case note, so Pi used source-backed local validation for implementation details.
  progress (2026-07-02, session rename): added minimal Hermes session rename support via capability-gated `PATCH /api/sessions/{session_id}`. `HermesApiClient.updateSessionTitle()` uses a new PATCH transport seam (io/web/stub), `HermesChannel.renameSession()` trims/rejects blank titles and updates the returned session row pessimistically after the server response, and `HermesChatScreen` shows a Rename action only when the active session exists and `/v1/capabilities` advertises `session_update`. Failed renames surface a SnackBar and do not overwrite local state. The web e2e Hermes fake supports CORS/PATCH and the browser smoke renames a session through the real web bundle.
  validation (session rename): Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-hermes-session-rename.md` failed on plan under-specification; useful issues (capability gate, input validation, error handling, non-optimistic update) were addressed in Pi implementation. Final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-hermes-session-rename-v2.md` passed 10/10 with only a generic edge-case note. Focused Hermes API/channel/screen tests (39 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (991 pass, up from 987); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter build web --release -t lib/main_e2e.dart`; Playwright Hermes smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, session delete): added capability-gated `DELETE /api/sessions/{session_id}` support. `HermesApiClient.deleteSession()` uses a new DELETE transport seam (io/web/stub), verifies the server's deletion envelope id and `deleted: true`, and rejects unconfirmed deletes. `HermesApiChannel.deleteSession()` is pessimistic (no local removal until the server confirms), guards duplicate in-flight deletes, removes the deleted row/messages, selects the next remaining session when the active session is deleted, and supports an empty connected session list without leaving a stale active session. `HermesChatScreen` shows Delete only when `session_delete` is advertised, confirms destructive deletion, disables composer/mic/send with no active session, and shows an empty-state prompt to create a new session. The web e2e fake supports CORS/DELETE and the browser smoke deletes a session through the real web bundle.
  validation (session delete): Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-hermes-session-delete.md` failed on plan under-specification; useful issues (delete envelope validation, pessimistic update, last-session/empty state, failure handling, e2e DELETE details) were addressed in Pi implementation. Final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-hermes-session-delete.md` passed 10/10 with only a generic edge-case note. Focused Hermes API/channel/screen tests (44 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (996 pass, up from 991); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter build web --release -t lib/main_e2e.dart`; Playwright Hermes smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, session fork): added capability-gated `POST /api/sessions/{session_id}/fork` support. `HermesApiConfig.sessionForkUri()` and `HermesApiClient.forkSession()` post a generated `navi-*` id/title and parse the returned `hermes.session`; `HermesApiChannel.forkSession()` adds/selects the returned child session and loads forked messages only after the server response, leaving local state unchanged on failure. `HermesChatScreen` shows a Fork action only when `session_fork` is advertised and surfaces failures with a SnackBar. The web e2e fake advertises/serves session fork by copying source messages, and the browser smoke forks a renamed session through the real web bundle.
  validation (session fork): Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-hermes-session-fork.md` timed out during arbitration and produced no usable fork-specific issue list; first gate failed only on generic edge coverage, so a fork-failure no-local-mutation test was added. Final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-hermes-session-fork-v2.md` passed 10/10 with only a generic edge-case note. Focused Hermes API/channel/screen tests (48 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (1000 pass, up from 996); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter build web --release -t lib/main_e2e.dart`; Playwright Hermes smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, sessions panel): moved Hermes session management out of separate chat app-bar actions into a dedicated Sessions bottom-sheet panel. The app bar now exposes Sessions, New session, and Disconnect; the panel lists sessions with active marker, title/id, message count, preview, row-tap selection, a New button, and capability-gated row actions for Rename/Fork/Delete. The old inline dropdown picker was removed. Panel actions close the sheet before invoking channel operations, keep existing confirmation/error SnackBar behavior, and use stable keys/semantic button labels for widget and browser tests. Playwright now drives rename/fork/delete through the Sessions panel.
  validation (sessions panel): Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-hermes-sessions-panel.md` failed on plan under-specification; useful issues (row actions must target row sessions, empty/single-session states, stable keys/semantics, stale sheet mitigation) were addressed in Pi implementation. Final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-hermes-sessions-panel-v2.md` passed 10/10 with only a generic edge-case note. Focused Hermes API/channel/screen tests (49 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (1001 pass, up from 1000); `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter build web --release -t lib/main_e2e.dart`; Playwright Hermes smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, local installed Hermes smoke): confirmed `hermes` is installed on this PC (`hermes --version` reports Hermes Agent v0.16.0 under `/home/xel/.hermes/hermes-agent`) and added `scripts/run_live_hermes_smoke.sh` plus `npm run hermes:live-smoke`. The script starts installed `hermes gateway` with an isolated temp `HERMES_HOME`, generated test API key, loopback API server, and local CORS; builds the Navivox e2e web bundle; serves it; and runs a new gated Playwright live-connect spec against the real Hermes API. `lib/main_e2e.dart` now lets tests pass an optional Hermes base URL/API key while keeping the fake default. The live smoke intentionally validates connect/session UI without provider/model credentials or LLM chat turns.
  validation (local installed Hermes smoke): Onklaud status operational; Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-live-hermes-smoke.md` timed out during arbitration, so Pi used source-backed local validation. Final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-live-hermes-smoke-v3.md` passed 10/10 with only a generic edge-case note. Manual temp-home Hermes API probe passed (`/health`, `/v1/capabilities`). `node -e` package JSON parse; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `bash -n scripts/run_live_hermes_smoke.sh`; `flutter analyze`; `flutter build web --release -t lib/main_e2e.dart`; fake Hermes Playwright smoke (2 pass); `scripts/run_live_hermes_smoke.sh` (Playwright live Hermes spec 1 pass); `flutter test --concurrency=1` (1001 pass); `git diff --check` (pass).
  progress (2026-07-02, catalog detail dialogs): made the read-only Hermes model/skill/toolset catalog chips actionable. The capability strip still stays compact, but Models, Skills, and Toolsets chips now open simple dialogs listing the loaded catalog names. This is a minimal Hermes Desktop-style operator inventory surface with no new API calls or dependencies.
  validation (catalog detail dialogs): final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-hermes-catalog-dialogs.md` passed 10/10 with only a generic edge-case note. `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (18 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (1001 pass); `flutter build web --release -t lib/main_e2e.dart`; fake Hermes Playwright smoke (2 pass).
  progress (2026-07-02, health detail strip): added optional `/health/detailed` support. `HermesApiClient.healthDetailed()` uses the existing config URI; `HermesApiChannel.connect()` loads it only when capabilities advertise `health_detailed` and treats failure as non-blocking. `HermesChannelState` now carries safe detailed health fields, and the Hermes capability strip shows version, gateway state, and active agent count without raw platform errors, logs, PIDs, or secrets. The fake web Hermes API advertises/serves `/health/detailed`, and the browser smoke asserts the new health chips.
  validation (health detail strip): Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-hermes-health-panel.md` timed out during arbitration, so Pi used source-backed local validation. Final Onklaud gate `/tmp/navivox-onklaud-gate-20260702-hermes-health-panel-v2.md` passed 10/10. Focused Hermes API/channel/screen tests (50 pass); `flutter analyze` (no issues); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter test --concurrency=1` (1002 pass); `flutter build web --release -t lib/main_e2e.dart`; fake Hermes Playwright smoke (2 pass); `scripts/run_live_hermes_smoke.sh` (live Hermes spec 1 pass); `git diff --check` (pass).
  progress (2026-07-02, setup presets): added Hermes Desktop-style setup presets to the Hermes connect form. `Local Hermes` fills the loopback default, `Android emulator` fills the host-emulator URL, and `Remote/LAN` clears the field so the user enters a trusted LAN/VPN/Tailscale/TLS endpoint. This keeps the existing URL/API-key form and secure storage behavior; no installer, background service, or secret handling was added.
  validation (setup presets): Onklaud planning loop against `/tmp/navivox-onklaud-checkpoint-20260702-hermes-setup-presets.md` timed out during arbitration. Onklaud gates `/tmp/navivox-onklaud-gate-20260702-hermes-setup-presets.md` and `-v2.md` failed only on generic `prefer HTTPS` advice despite this being documented loopback/emulator local development; Pi recorded the advice and used source-backed validation. `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (19 pass); `flutter analyze` (no issues); `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter test --concurrency=1` (1003 pass); fake Hermes Playwright smoke (2 pass); `scripts/run_live_hermes_smoke.sh` (live Hermes spec 1 pass); `git diff --check` (pass).
  progress (2026-07-02, bounded diagnostics): added a connected-state Hermes diagnostics dialog and copy action. The exported snapshot is intentionally bounded to already-visible connection state, health/capability metadata, catalog counts, transport availability, and unsupported config/memory/realtime-voice markers; it excludes API keys, authorization headers, raw logs, transcripts, tool payloads, and platform error dumps. This closes the first minimal diagnostics/export slice without claiming full log export.
  validation (bounded diagnostics): `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (21 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (1005 pass, up from 1003); `flutter build web --release -t lib/main_e2e.dart` (pass); fake Hermes Playwright smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, surface readiness): added a central `hermesSurfaceReadiness()` projection and a compact Surfaces chip/dialog in the Hermes capability strip. It explicitly classifies the current Hermes/mobile surfaces: available chat/session/local STT, read-only bounded diagnostics, deferred realtime/server audio, config admin, memory, jobs/schedules, messaging gateways, persona/SOUL, attachments/media, files/context folders, multi-endpoint/profile management, and blocked legacy Gormes durable reconnect. The same classification is included in the bounded diagnostics export so missing/deferred features are inspectable without exposing secrets or logs.
  validation (surface readiness): `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (21 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (1005 pass); `flutter build web --release -t lib/main_e2e.dart` (pass); fake Hermes Playwright smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, provider-backed smoke harness): added a gated provider-backed smoke path for the first remaining live-chat blocker. `playwright/tests/regression/hermes-provider-chat.spec.mjs` connects the real web bundle to `NAVIVOX_PROVIDER_HERMES_URL`, sends a typed prompt, then submits a simulated device transcript via `HermesApiChannel` and waits for provider/model replies. `scripts/run_provider_hermes_smoke.sh` verifies `/health` and `/v1/capabilities`, builds/serves `lib/main_e2e.dart`, and runs the spec; `npm run hermes:provider-smoke` exposes it. This does not claim the blocker is resolved because credentials are still external and Android physical microphone smoke is separate.
  validation (provider-backed smoke harness): `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs`; `bash -n scripts/run_provider_hermes_smoke.sh`; `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`; `npx playwright test --config=playwright.config.mjs playwright/tests/regression/hermes-provider-chat.spec.mjs --reporter=list` with no `NAVIVOX_PROVIDER_HERMES_URL` (1 skipped as intended); `git diff --check` (pass).
  progress (2026-07-02, Android voice smoke harness): added `integration_test/android_device_speech_smoke_test.dart` and `scripts/run_android_voice_smoke.sh`, exposed as `npm run android:voice-smoke`. The script selects an Android device/emulator (or uses `NAVIVOX_ANDROID_DEVICE_ID`), grants `RECORD_AUDIO` when possible, and runs an integration test against the native `com.trebuchetdynamics.navivox/device_speech` MethodChannel. The test requires an installed Android speech recognition service and microphone permission before the manual continuous-voice live smoke. This does not claim the physical microphone/continuous loop blocker is resolved because no responsive Android device/emulator is available here and real audio still needs a human/device runner.
  validation (Android voice smoke harness): `dart format integration_test/android_device_speech_smoke_test.dart`; `flutter analyze` (no issues); `bash -n scripts/run_android_voice_smoke.sh`; `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`. Attempting to run the integration test in this Linux container is blocked by Flutter requiring an explicit device and no Android target being connected.
  progress (2026-07-02, host-runner workflow): finalized `.github/workflows/hermes-platform-smoke.yml` as the host-runner path for the remaining Windows/iOS/Linux receipts. Ubuntu runs analyze/tests, web e2e build, fake Hermes browser smoke, Android debug APK build, and Linux release build with native dependencies. Windows builds the Flutter Windows app on `windows-latest`; macOS builds an iOS simulator app on `macos-latest`. Manual `workflow_dispatch` can also run the provider-backed Hermes web smoke against a supplied `provider_url` using the `NAVIVOX_PROVIDER_HERMES_API_KEY` repository secret. This still needs an actual GitHub Actions run receipt before marking host-platform smoke complete.
  validation (host-runner workflow): `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/hermes-platform-smoke.yml")'` (workflow YAML parses); `bash -n scripts/run_android_voice_smoke.sh scripts/run_provider_hermes_smoke.sh scripts/run_live_hermes_smoke.sh`; `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`; `git diff --check` (pass).
  progress (2026-07-02, read-only jobs inventory): added capability-gated read-only Hermes jobs support for the schedules/jobs deferred area. `HermesApiConfig.jobsUri()`/`HermesApiClient.listJobs()` parse `GET /api/jobs` when an endpoint advertises `jobs`; `HermesApiChannel.connect()` loads jobs non-blockingly into `HermesChannelState.jobs`; the Hermes capability strip shows a Jobs chip/dialog and bounded diagnostics include the job count. `hermesSurfaceReadiness()` now classifies jobs/schedules as read-only when the list endpoint is advertised but `jobs_admin` is false, while keeping admin deferred. The fake Hermes web server advertises/serves one job and browser smoke asserts the Jobs chip. No create/update/pause/resume/delete UI was added.
  validation (read-only jobs inventory): focused Hermes API/channel/screen tests (54 pass); `flutter analyze` (no issues); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `flutter build web --release -t lib/main_e2e.dart` (pass); fake Hermes Playwright smoke (2 pass); `git diff --check` (pass).
  progress (2026-07-02, future-surface honesty): tightened `hermesSurfaceReadiness()` so advertised-but-unwired Hermes capabilities do not appear as implemented mobile features. Realtime/server audio remains deferred even if `realtime_voice` is advertised, because Navivox still uses device STT; config editing/admin and memory UI remain deferred even if their write features are advertised; jobs remain read-only even if `jobs_admin` is advertised; attachments/media remain deferred even if multimodal/attachment capability appears. The capability strip now says `Server voice advertised; using device STT` instead of implying server voice support.
  validation (future-surface honesty): focused Hermes API/screen tests (34 pass); `flutter analyze` (no issues); `flutter test --concurrency=1` (1007 pass, up from 1006); `git diff --check` (pass).
  progress (2026-07-02, local configured provider smoke helper): added `scripts/run_local_configured_hermes_provider_smoke.sh`, exposed as `npm run hermes:provider-smoke:local`. It starts the installed `hermes gateway` from an already configured Hermes home (default `$HOME/.hermes` or `NAVIVOX_CONFIGURED_HERMES_HOME`) with a generated temporary API-server key and local CORS, then delegates to the provider-backed Playwright smoke. This reduces the remaining provider-backed smoke blocker to supplying a configured Hermes home and accepting the live provider/model call; it still does not mark the blocker resolved without an actual run receipt.
  validation (local configured provider smoke helper): `bash -n scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`; `git diff --check` (pass).
  progress (2026-07-02, provider-backed smoke receipt): ran the configured-provider smoke against the installed local Hermes server using `$HOME/.hermes` as the configured Hermes home. The smoke starts `hermes gateway`, builds/serves the Navivox web e2e bundle, connects through the real web `HermesApiChannel`, sends a typed provider-backed prompt, submits a device-transcript voice turn through the Hermes voice path, and waits for the configured model replies. This closes the provider-backed web chat + transcript-voice smoke item; Android physical microphone/continuous voice remains a separate device-gated blocker.
  validation (provider-backed smoke receipt): `NAVIVOX_CONFIGURED_HERMES_HOME=$HOME/.hermes npm run hermes:provider-smoke:local` returned success (Playwright provider smoke 1 pass).
  progress (2026-07-02, host build artifacts): extended `.github/workflows/hermes-platform-smoke.yml` to upload build artifacts from the host-runner gates: Android debug APK and Linux release bundle from Ubuntu, Windows debug bundle from `windows-latest`, and iOS simulator app from `macos-latest`. This makes future host receipts auditable from workflow artifacts instead of relying only on log text. Actual Windows/iOS completion still requires a successful Actions run on those hosts.
  validation (host build artifacts): workflow YAML parse via Ruby; `git diff --check` (pass).
  progress (2026-07-02, realtime voice copy regression): added a widget regression proving that even when a Hermes endpoint advertises `realtime_voice`, the Hermes screen says `Server voice advertised; using device STT`, does not show the old `Server realtime voice advertised` claim, and still classifies server realtime voice/audio as Deferred in the Surfaces dialog. This keeps the local-STT boundary explicit until a real server-audio transport is implemented.
  validation (realtime voice copy regression): `flutter test --concurrency=1 test/features/hermes_chat/screens/hermes_chat_screen_test.dart` (22 pass); `flutter analyze` (no issues); `git diff --check` (pass). `npm run android:voice-smoke` was also attempted and correctly blocked with no Android device/emulator connected.
  progress (2026-07-02, Android emulator voice-readiness receipt): launched the available `fractal_test` Android emulator directly in headless/software mode after `flutter emulators --launch fractal_test` crashed (`code -6`): `/usr/lib/android-sdk/emulator/emulator -avd fractal_test -no-window -no-audio -no-snapshot -gpu swiftshader_indirect -accel off`. Added a boot/package-service readiness wait to `scripts/run_android_voice_smoke.sh`, then ran the Android speech readiness gate successfully. This proves the emulator is responsive enough for app install and native speech diagnostics: `integration_test/android_device_speech_smoke_test.dart` found an Android speech recognition service and granted microphone permission. This still does not prove real spoken audio capture or the continuous voice loop; the emulator was launched with `-no-audio`, so a human/device audio closeout remains required.
  validation (Android emulator voice-readiness receipt): direct `flutter test -d emulator-5554 integration_test/android_device_speech_smoke_test.dart` passed (1 test); `npm run android:voice-smoke` passed after boot/package service readiness; `bash -n scripts/run_android_voice_smoke.sh`; `git diff --check` (pass).
  progress (2026-07-02, Android durable-key smoke harness): added `scripts/run_android_durable_key_smoke.sh`, exposed as `npm run android:durable-key-smoke`, to run the existing `integration_test/durable_key_store_android_smoke_test.dart` on a selected Android target after boot/package-service readiness. This creates/signs/deletes an ES256 Android keystore key and rejects unsafe aliases, covering the keypair readiness part of the legacy durable reconnect blocker. It does not claim Gormes durable credential issuance or reconnect works end to end.
  validation (Android durable-key smoke harness): `bash -n scripts/run_android_durable_key_smoke.sh`; `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`; `git diff --check` (pass). A later headless `fractal_test` emulator launch reached readiness in about 71 polling iterations, but `npm run android:durable-key-smoke` did not complete successfully: the first run reported one integration-test failure after ~2 minutes, and a direct rerun installed the APK but hung at `Android durable key store creates signs and deletes ES256 keys` until the 900s command timeout. The test app was force-stopped and the emulator killed; no durable-key receipt is claimed. `scripts/run_android_durable_key_smoke.sh` now wraps the Flutter test in a bounded timeout (`NAVIVOX_ANDROID_TEST_TIMEOUT_SECONDS`, default 900s) so future runs fail closed instead of hanging indefinitely.
  progress (2026-07-02, Android durable-key smoke test alias fix): fixed `integration_test/durable_key_store_android_smoke_test.dart` to use a valid `navivox_durable_...` alias suffix length. The old alias (`navivox_durable_integration_<timestamp>`) could be rejected by `DurableKeyAlias.MIN_RANDOM_SUFFIX_LENGTH` before it ever tested Android keystore behavior, turning the smoke into a false failure. The new alias appends deterministic padding after the timestamp.
  validation (durable-key alias fix): `dart format integration_test/durable_key_store_android_smoke_test.dart`; `flutter analyze` (no issues); `git diff --check` (pass). A Linux-target attempt of the integration test is not a valid proxy and failed at Linux desktop build setup because `libsecret-1-dev` is unavailable in this container path; no Android keypair receipt is claimed from this alias-only fix.
  progress (2026-07-02, Android durable-key receipt): relaunched the `fractal_test` emulator headlessly with software rendering, waited for boot/package/settings readiness, and reran the durable-key harness after the alias fix. `npm run android:durable-key-smoke` completed successfully under the bounded timeout, proving the native Android keystore MethodChannel can create an ES256 keypair, export public JWK coordinates only, sign, delete idempotently, and reject unsafe aliases on this emulator. This closes keypair-readiness evidence only; Gormes durable credential issuance/authentication and silent reconnect remain blocked/future work.
  validation (Android durable-key receipt): `NAVIVOX_ANDROID_TEST_TIMEOUT_SECONDS=900 npm run android:durable-key-smoke` returned success on `emulator-5554`.
  progress (2026-07-02, full local verification sweep): after the provider, Android voice-readiness, jobs inventory, future-surface honesty, workflow, and durable-key harness slices, reran the local non-host validation gate to make sure the accumulated changes still compose.
  validation (full local verification sweep): `flutter analyze` (no issues); `flutter test --concurrency=1` (1008 pass); `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `bash -n scripts/run_android_voice_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_provider_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_live_hermes_smoke.sh`; `git diff --check` (pass).
  progress (2026-07-02, Linux release build helper/receipt): added `scripts/run_linux_release_build.sh`, exposed as `npm run linux:release-build`. It runs a normal Linux release build when `libsecret-1` dev metadata is installed, or in rootless containers downloads/extracts the needed Debian dev packages into a temporary prefix, rewrites `.pc` files, repoints the `libsecret-1.so` dev symlink at the installed runtime library, clears stale `build/linux`, and builds with `PKG_CONFIG_PATH`/`CPATH`. This gives a repeatable local Linux release receipt without passwordless `sudo`.
  validation (Linux release build helper/receipt): `bash -n scripts/run_linux_release_build.sh`; `./scripts/run_linux_release_build.sh` passed and produced `build/linux/x64/release/bundle/navivox`.
  progress (2026-07-02, Android Hermes voice-loop smoke harness): added `integration_test/hermes_continuous_voice_android_smoke_test.dart` and `scripts/run_android_hermes_voice_loop_smoke.sh`, exposed as `npm run android:hermes-voice-loop-smoke`. The integration test runs the real `HermesChatScreen` on Android with a deterministic transcript capture service, fake Hermes channel, and fake TTS; it verifies continuous voice performs capture → submit transcript → assistant reply/TTS → re-arm for two turns. This fills the Android Flutter voice-loop gap between widget tests and physical microphone validation, but still does not prove real spoken audio capture.
  validation (Android Hermes voice-loop smoke harness): `dart format integration_test/hermes_continuous_voice_android_smoke_test.dart`; `flutter analyze` (no issues); `bash -n scripts/run_android_hermes_voice_loop_smoke.sh`; `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`; `git diff --check` (pass). No Android run receipt claimed yet for this new harness.
  progress (2026-07-02, Android readiness hardening): attempted to run `npm run android:hermes-voice-loop-smoke` on headless `fractal_test` after a readiness loop reported boot/package/settings ready, but install failed with `adb: ... cmd: Can't find service: package`, proving the emulator can still regress immediately after a single readiness hit. Hardened all Android smoke scripts (`run_android_voice_smoke.sh`, `run_android_durable_key_smoke.sh`, `run_android_hermes_voice_loop_smoke.sh`) to require repeated consecutive readiness checks for `sys.boot_completed=1`, `service check package`, and `service check settings` before installing/running tests. The failed emulator was killed; no voice-loop Android receipt is claimed.
  validation (Android readiness hardening): `bash -n scripts/run_android_voice_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh`; `git diff --check` (pass).
  progress (2026-07-02, Android deterministic Hermes voice-loop receipt): relaunched `fractal_test` headlessly in software mode and waited for boot to stabilize. The first `npm run android:hermes-voice-loop-smoke` attempt timed out before completion because boot/install on this software-emulated runner is extremely slow, but a direct run of the underlying integration test completed successfully. This proves the Android Flutter Hermes continuous-voice UI loop (with deterministic transcript capture, fake Hermes channel, and fake TTS) performs capture → submit transcript → assistant reply/TTS → re-arm for two turns on an Android emulator. It still does not prove physical microphone audio input because the emulator is launched with `-no-audio` and the capture service is deterministic.
  validation (Android deterministic Hermes voice-loop receipt): `flutter test -d emulator-5554 integration_test/hermes_continuous_voice_android_smoke_test.dart` passed (1 test; install ~269s, total ~3m46s after test start).
  progress (2026-07-02, Android emulator CI smoke): extended `.github/workflows/hermes-platform-smoke.yml` with a manual `run_android_emulator_smoke` workflow-dispatch job using `reactivecircus/android-emulator-runner`. The job runs speech-readiness, deterministic Hermes continuous-voice loop, and durable-key integration smokes on a GitHub-hosted Android emulator. This gives an auditable CI path for Android integration receipts, but still requires a workflow run before counting as a host/device receipt.
  validation (Android emulator CI smoke): workflow YAML parse via Ruby; `git diff --check` (pass).
  progress (2026-07-02, live Hermes connect regression rerun): reran the installed-Hermes live connect smoke after the jobs inventory, surface-readiness, Android smoke, Linux build, workflow, and durable-key changes to verify the real web bundle still connects to an installed Hermes API server without provider credentials.
  validation (live Hermes connect regression rerun): `npm run hermes:live-smoke` returned success (Playwright live Hermes spec 1 pass).
  progress (2026-07-02, Android debug APK regression rerun): rebuilt the Android debug APK after adding Android smoke harnesses and integration tests to make sure the app still packages for Android.
  validation (Android debug APK regression rerun): `flutter build apk --debug` returned success (artifact `build/app/outputs/flutter-apk/app-debug.apk`; only Kotlin Gradle Plugin migration warnings from plugins/project).
  progress (2026-07-02, durable reconnect evidence refresh): reran the durable reconnect core/unit coverage to separate what is proven from what remains blocked. Issuance/key metadata/readiness/unit credential coverage passes; fake-gateway reconnect fallback and provider startup reconnect pass. This proves the current Dart reconnection path with in-memory durable credentials and fake gateway auth, plus Android keystore key create/sign/delete from the prior Android smoke, but it is still not an end-to-end physical Android saved credential reconnect against real Gormes after app/server restart.
  validation (durable reconnect evidence refresh): `flutter test test/core/session/reconnect test/core/session/credentials test/core/gateway/capabilities/durable_reconnect_readiness_contract_test.dart test/core/session/readiness/reconnect_readiness_test.dart` passed (23 tests); `flutter test test/core/channel/gateway/runtime/channel_test.dart --plain-name 'falls back to device-bearer reconnect when stream reconnect exhausts'` passed; `flutter test test/core/channel/gateway/runtime/channel_test.dart --plain-name 'provider reconnects automatically from saved durable gateway'` passed.
  progress (2026-07-02, full unit/widget regression rerun): reran the whole Flutter unit/widget suite after the Hermes diagnostics/jobs/surface-readiness, Android smoke, CI workflow, Linux build helper, and durable reconnect evidence changes.
  validation (full unit/widget regression rerun): `flutter test --concurrency=1` passed (1008 tests).
  progress (2026-07-03, Playwright regression freshness): tried the broad `npm run web:e2e` entrypoint and it timed out at 600s in this environment, then ran the focused regression specs directly. Two regression assertions were stale relative to the current UI contract: memory can load a read-only/empty state instead of only an unavailable state, and mobile tabs are now Hermes/Chats/Profiles/Settings/More with Memory behind More. Updated `playwright/tests/regression/navivox-e2e.spec.mjs` to assert the current memory empty/scope state and Settings tab navigation.
  validation (Playwright regression freshness): targeted rerun `npx playwright test --config=playwright.config.mjs playwright/tests/regression/navivox-e2e.spec.mjs --grep '5c|11c' --reporter=json` passed (2 tests); focused regression rerun `npx playwright test --config=playwright.config.mjs playwright/tests/regression/navivox-e2e.spec.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list` passed (`test-results/.last-run.json` status `passed`, `failedTests: []`).
  progress (2026-07-03, configured provider transcript-voice refresh): reran the local configured Hermes provider smoke against `$HOME/.hermes` after the browser regression refresh. This covers provider-backed Hermes text and the Navivox device-transcript voice path (`navivoxE2EHermesSubmitVoice`) producing model replies; it does not cover physical microphone capture or Hermes realtime/server audio.
  validation (configured provider transcript-voice refresh): `NAVIVOX_CONFIGURED_HERMES_HOME=$HOME/.hermes npm run hermes:provider-smoke:local` returned success (Playwright provider-backed Hermes spec 1 pass).
  progress (2026-07-03, Android native unit contract refresh): attempted broad Android Gradle `testDebugUnitTest`; it fails in the upstream `image_picker_android` plugin unit test (`ImagePickerDelegateTest > classMethod`, `UnsupportedOperationException at DefaultSdkProvider.java:170`), outside Navivox app code. Then ran the app-scoped native unit tests for Navivox Android code, covering device speech diagnostics, durable key alias/signature encoding, and pairing handoff parsing.
  validation (Android native unit contract refresh): `cd android && ./gradlew :app:testDebugUnitTest` passed. Broad `cd android && ./gradlew testDebugUnitTest` remains unsuitable in this environment because it executes failing third-party plugin tests from `image_picker_android`.
  progress (2026-07-03, runbook receipt refresh): updated `docs/runbooks/hermes-platform-smoke.md` so the platform smoke runbook reflects current evidence instead of stale 2026-07-01 counts: full Flutter suite is now 1008 tests, app-scoped Android native units are documented, provider transcript-voice is refreshed through 2026-07-03, focused Playwright regression status is documented, and durable reconnect evidence is split between Android key readiness/Dart fake-gateway reconnect and the still-missing real Android Gormes restart reconnect.
  validation (runbook receipt refresh): `git diff --check` passed.
  progress (2026-07-03, README evidence refresh): updated `README.md` status and smoke-command sections so they match the current evidence: provider-backed transcript voice when a configured Hermes home is available, Android speech/key readiness, deterministic Android continuous-voice loop, Linux release build, and the still-missing physical Android microphone, Windows/iOS native host, and real Android Gormes durable reconnect receipts.
  validation (README evidence refresh): `git diff --check` passed.
  progress (2026-07-03, readiness audit artifact): added `docs/runbooks/hermes-readiness-audit.md`, a prompt-to-artifact checklist mapping every current Hermes companion readiness requirement to concrete evidence, status, and non-completion caveats. It explicitly separates provider transcript voice from physical mic/server audio, Android readiness from spoken audio, workflow definitions from host receipts, and durable key/unit/fake-gateway coverage from real Android Gormes restart reconnect. Linked it from README documentation.
  validation (readiness audit artifact): `git diff --check` passed.
  progress (2026-07-03, surface-readiness guard expansion): expanded the Hermes surface-readiness unit assertion so all explicit deferred/limited readiness surfaces from the current goal are covered: server realtime voice/audio, config editing/admin, memory UI, jobs/schedules read-only, messaging gateways, persona/SOUL, attachments/media, files/context folders, bounded diagnostics read-only, multi-endpoint/profile management, and legacy durable reconnect blocked. This prevents future capability advertisements from accidentally being shown as implemented mobile features.
  validation (surface-readiness guard expansion): `dart format test/core/hermes/hermes_api_test.dart`; `flutter test test/core/hermes/hermes_api_test.dart --plain-name 'surface readiness does not claim unwired Hermes APIs are implemented'` passed; `git diff --check` passed.
  progress (2026-07-03, analyzer refresh): reran static analysis after the readiness audit, README/runbook updates, and expanded Hermes surface-readiness guard.
  validation (analyzer refresh): `flutter analyze` passed with no issues.
  progress (2026-07-03, web e2e bundle refresh): rebuilt the Flutter web e2e bundle after the Playwright regression assertion updates and Hermes readiness/docs changes, confirming `lib/main_e2e.dart` still compiles for browser smoke runs.
  validation (web e2e bundle refresh): `flutter build web --release -t lib/main_e2e.dart` passed and produced `build/web` (Flutter still emits WebAssembly dry-run warnings from `flutter_secure_storage_web` using `dart:html`/`dart:js_util`, but the JS web build succeeds).
  progress (2026-07-03, GOAL_STATE refresh): updated `GOAL_STATE.md` to remove stale 2026-07-01 gate language, point to the readiness audit, record the latest 1008-test/analyze/web/provider/Android/Linux/native-unit evidence, and restate remaining blockers without implying live provider transcript voice, Android deterministic voice-loop, or workflow YAML are enough for completion.
  validation (GOAL_STATE refresh): `git diff --check` passed.
  progress (2026-07-03, Linux release receipt refresh): reran the repeatable Linux release build helper after the readiness audit/docs/test updates to keep the desktop companion receipt current.
  validation (Linux release receipt refresh): `npm run linux:release-build` returned success; `test -x build/linux/x64/release/bundle/navivox` passed and `ls -lh` showed the release binary at `build/linux/x64/release/bundle/navivox`; `git diff --check` passed.
  progress (2026-07-03, native-host blocker probe): directly probed Windows/iOS Flutter build commands from this Linux checkout to keep the blocker evidence current. `flutter build windows --debug` exits 1 with `"build windows" only supported on Windows hosts.` The Linux Flutter toolchain also rejects iOS build options here (`flutter build ios --simulator --debug` exits 64 with unknown `--simulator`; `flutter build ios --debug` exits 64 with unknown `--debug`), confirming there is no local iOS build receipt path from this host.
  validation (native-host blocker probe): command output captured locally; no Windows/iOS artifact produced, so these remain native-host/CI receipt blockers.
  progress (2026-07-03, workflow robustness): quoted the GitHub Actions `on` key in `.github/workflows/hermes-platform-smoke.yml` so generic YAML parsers do not read it as boolean `true`, and added bounded `timeout-minutes` to the Linux/web/Android, optional Android emulator, provider smoke, Windows, and iOS jobs. This improves the host-runner receipt path without pretending a workflow run has happened.
  validation (workflow robustness): Ruby YAML parse confirms an `on` key and no boolean-`true` key; `git diff --check` passed.
  progress (2026-07-03, diagnostics boundary guard): strengthened the bounded Hermes diagnostics export test to include active transcript text and raw tool payload fields in channel state, then assert the export reports only counts/statuses and excludes transcript/tool payload content. This backs up the raw-log/payload export boundary in the readiness audit.
  validation (diagnostics boundary guard): `dart format test/features/hermes_chat/screens/hermes_chat_screen_test.dart`; `flutter test test/features/hermes_chat/screens/hermes_chat_screen_test.dart --plain-name 'Hermes diagnostics export is bounded and excludes secrets'` passed; `git diff --check` passed.
  progress (2026-07-03, full regression after boundary/workflow/docs changes): reran the full Flutter unit/widget suite after the diagnostics boundary guard, workflow hardening, native-host blocker docs, GOAL_STATE, README, and readiness-audit changes.
  validation (full regression after boundary/workflow/docs changes): `flutter test --concurrency=1` passed (1008 tests).
  progress (2026-07-03, installed-Hermes live connect refresh): reran the installed local Hermes API live-connect smoke after the workflow/docs/diagnostics guard changes to keep the live API receipt current.
  validation (installed-Hermes live connect refresh): `npm run hermes:live-smoke` returned success (Playwright live Hermes spec 1 pass).
  progress (2026-07-03, Android smoke device-discovery hardening): hardened `scripts/run_android_voice_smoke.sh`, `scripts/run_android_hermes_voice_loop_smoke.sh`, and `scripts/run_android_durable_key_smoke.sh` so they wait up to `NAVIVOX_ANDROID_DEVICE_WAIT_SECONDS` for Flutter to list an Android target before failing. This addresses the observed emulator startup window where `adb` showed `emulator-5554 offline` and the smoke exited before the target became usable. Existing framework-service stabilization checks remain in place.
  validation (Android smoke device-discovery hardening): `bash -n scripts/run_android_voice_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_durable_key_smoke.sh`; `git diff --check` passed. No new Android run receipt claimed.
  progress (2026-07-03, readiness audit refresh): updated `docs/runbooks/hermes-readiness-audit.md` with the latest evidence boundaries: diagnostics tests now exclude transcript/tool payloads, full Dart/widget regression and analyzer receipts are current but not substitutes for device/host gates, workflow robustness is a CI path only, and direct Linux Windows/iOS build probes are recorded as blocker evidence.
  validation (readiness audit refresh): `git diff --check` passed.
  progress (2026-07-03, Android install-flake retry): after relaunching the headless/software `fractal_test` emulator, `npm run android:hermes-voice-loop-smoke` still failed during APK install because Android's package service disappeared (`Broken pipe`/`Can't find service: package`) even after readiness checks. Hardened the Android smoke scripts to capture Flutter test output and retry once after framework readiness when Flutter/ADB reports package-service/offline/startup flakes. This improves repeatability but does not produce a new Android voice-loop receipt; the emulator remained unstable and was killed.
  validation (Android install-flake retry): `bash -n scripts/run_android_voice_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_durable_key_smoke.sh`; attempted `NAVIVOX_ANDROID_DEVICE_ID=emulator-5554 NAVIVOX_ANDROID_BOOT_WAIT_SECONDS=900 NAVIVOX_ANDROID_READY_CONSECUTIVE_CHECKS=10 NAVIVOX_ANDROID_TEST_TIMEOUT_SECONDS=1200 npm run android:hermes-voice-loop-smoke` failed twice during install/start, so no pass receipt claimed; `git diff --check` passed.
  progress (2026-07-03, Android package-manager readiness): tightened Android smoke readiness checks again after the package service flake: all three Android smoke scripts now require `adb shell cmd package list packages` to succeed in addition to `sys.boot_completed=1`, `service check package`, and `service check settings`. The install-flake detector also explicitly matches `Broken pipe`. This targets the observed state where `service check package` briefly returned `found` but install still failed.
  validation (Android package-manager readiness): `bash -n scripts/run_android_voice_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_durable_key_smoke.sh`; `git diff --check` passed. No Android run receipt claimed.
  progress (2026-07-03, Android CI smoke wrapper alignment): changed the optional `android-emulator-smoke` workflow job to invoke the hardened local Android smoke wrappers instead of raw `flutter test` commands. This carries the device-discovery, framework-service, package-command, timeout, permission, and one-retry install-flake handling into the GitHub-hosted Android emulator receipt path.
  validation (Android CI smoke wrapper alignment): Ruby YAML parse confirms `.github/workflows/hermes-platform-smoke.yml` still has a string `on` key; `git diff --check` passed. No workflow run receipt claimed.
  progress (2026-07-03, Android durable-key adapter smoke coverage): extended `integration_test/durable_key_store_android_smoke_test.dart` beyond raw MethodChannel calls to also exercise the Dart `MethodChannelDurableCredentialKeyStore` adapter. The adapter path now creates a P-256 ES256 public JWK, verifies no private `d` material is exposed, signs a payload, and deletes the native alias. This reduces the Android secure credential/keypair gap but still does not prove full Gormes credential issuance/auth/silent reconnect.
  validation (Android durable-key adapter smoke coverage): `dart format integration_test/durable_key_store_android_smoke_test.dart`; `flutter analyze` passed with no issues; `git diff --check` passed. No Android rerun receipt claimed because the local emulator remains unstable.
  progress (2026-07-03, full regression after Android smoke/CI hardening): reran the full Flutter unit/widget suite after Android smoke wrapper hardening, CI wrapper alignment, readiness-audit updates, and durable-key adapter smoke coverage.
  validation (full regression after Android smoke/CI hardening): `flutter test --concurrency=1` passed (1008 tests).
  progress (2026-07-03, Android live microphone prep helper): added `scripts/prepare_android_live_mic_smoke.sh`, exposed as `npm run android:live-mic-prep`, to reduce friction for the remaining real spoken microphone blocker. It waits for an Android target, verifies stable package manager readiness, builds/installs the debug APK, grants `RECORD_AUDIO`, launches Navivox, and prints the required manual provider-backed spoken mic/continuous-loop checklist. The script explicitly says it is not a pass receipt.
  validation (Android live microphone prep helper): `bash -n scripts/prepare_android_live_mic_smoke.sh`; package JSON parse; `git diff --check` passed. No Android microphone receipt claimed.
  progress (2026-07-03, readiness audit live-mic prep caveat): updated `docs/runbooks/hermes-readiness-audit.md` to include `android:live-mic-prep` as readiness/prep evidence only and explicitly list it under signals that must not be counted as spoken-audio completion.
  validation (readiness audit live-mic prep caveat): `git diff --check` passed.
  progress (2026-07-03, platform workflow dispatch helper): added `scripts/run_hermes_platform_workflow.sh`, exposed as `npm run platform:workflow-smoke`, to dispatch/watch the GitHub-hosted Hermes platform workflow once `.github/workflows/hermes-platform-smoke.yml` is published to the remote branch. It supports optional Android emulator and provider smoke inputs via env vars and fails explicitly if the workflow is not visible to `gh`, avoiding accidental claims from an unpublished workflow.
  validation (platform workflow dispatch helper): `bash -n scripts/run_hermes_platform_workflow.sh`; package JSON parse; `git diff --check` passed. No workflow run receipt claimed.
  progress (2026-07-03, workflow dispatch blocker confirmation): ran the helper with `NAVIVOX_WATCH_WORKFLOW=false npm run platform:workflow-smoke`; it exited 2 because `gh workflow list` still does not show `Hermes platform smoke`. This confirms the native-host workflow receipt remains blocked until the workflow file is published/visible remotely.
  validation (workflow dispatch blocker confirmation): helper emitted the expected explicit blocker message; no workflow run receipt claimed.
  progress (2026-07-03, shell helper syntax sweep): reran a syntax sweep across all repository shell helpers after adding the Android live-mic prep, platform workflow dispatch helper, Android wrapper hardening, provider/local smoke helpers, and Linux release build helper.
  validation (shell helper syntax sweep): `bash -n scripts/*.sh`; `git diff --check` passed.
  progress (2026-07-03, browser smoke JavaScript syntax sweep): reran Node syntax checks for the updated web fake server and all Hermes/browser Playwright specs touched by the Hermes provider/live/fake smoke and regression freshness work.
  validation (browser smoke JavaScript syntax sweep): `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs`; `node --check playwright/tests/regression/navivox-e2e.spec.mjs`; `git diff --check` passed.
  progress (2026-07-03, durable reconnect closeout runbook): expanded `docs/runbooks/android/durable-keystore-smoke.md` from a terse key-storage checklist into a split runbook: automated key readiness via `npm run android:durable-key-smoke` and a separate full real Android + real Gormes durable reconnect closeout checklist. The runbook now documents native MethodChannel plus Dart adapter key coverage, public-JWK/no-private-material expectations, and explicitly says key readiness is not durable credential issuance/auth/silent reconnect.
  validation (durable reconnect closeout runbook): initial targeted test path was wrong and failed to load; corrected to `flutter test test/tooling/runbooks/android/pairing_and_keystore_contract_test.dart test/tooling/runbooks/topology/runbooks_topology_contract_test.dart`, fixed required contract phrases, and reran successfully (4 tests passed); `git diff --check` passed.
  progress (2026-07-03, docs index refresh): added the Hermes platform smoke checklist and Hermes companion readiness audit to `docs/README.md` so the current evidence/blocker audit is discoverable from the main docs index instead of only from README/TODO.
  validation (docs index refresh): `flutter test test/tooling/runbooks/topology/runbooks_topology_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, Hermes interface plan refresh): updated `docs/product/hermes-agent-interface-plan.md` to match implemented/current policy: schedules/jobs are read-only inventory with admin deferred, diagnostics are bounded status/capability/readiness export with raw logs/transcripts/tool payloads excluded, and the architecture section now reflects ADR 0007's native `HermesChannel` instead of the earlier rejected `HermesNavivoxChannel implements NavivoxChannel` adapter seam.
  validation (Hermes interface plan refresh): `git diff --check` passed.
  progress (2026-07-03, Hermes interface plan adapter cleanup): removed the remaining stale adapter wording from the Hermes interface plan delivery slices and first implementation recommendation, and rewrote the amendment so the plan no longer contains `HermesNavivoxChannel`/`implements NavivoxChannel` text. The plan now consistently points new work to native `HermesChannel` and `/hermes`.
  validation (Hermes interface plan adapter cleanup): `rg -n "HermesNavivoxChannel|implements NavivoxChannel" docs/product/hermes-agent-interface-plan.md` returns no matches; `git diff --check` passed.
  progress (2026-07-03, ADR 0006 amendment refresh): updated `docs/adr/0006-hermes-agent-first-runtime.md` so its status explicitly notes ADR 0007's native-Hermes-channel amendment and its decision list no longer instructs implementation through a Gormes-shaped `NavivoxChannel` transition adapter. This keeps the accepted ADR aligned with the implemented `/hermes` native channel path and avoids future agents following stale architecture guidance.
  validation (ADR 0006 amendment refresh): `rg -n "HermesNavivoxChannel|transition adapter behind the existing NavivoxChannel" docs/adr/0006-hermes-agent-first-runtime.md docs/product/hermes-agent-interface-plan.md` now only reports the ADR 0007 title in ADR 0006 status; `git diff --check` passed.
  progress (2026-07-03, analyzer after docs/script/code hardening): reran static analysis after the durable-key adapter integration test, Android smoke wrapper hardening, workflow dispatch helper, live-mic prep helper, and product/ADR/runbook refreshes.
  validation (analyzer after docs/script/code hardening): `flutter analyze` passed with no issues.
  progress (2026-07-03, testing plan status refresh): updated `docs/product/testing-plan.md` status to mark it as a historical Gormes-first planning draft and point current Hermes-first validation to the Hermes platform smoke checklist and readiness audit. This prevents stale `/v1/navivox/*` test-plan language from being mistaken for the active Hermes evidence gates.
  validation (testing plan status refresh): `git diff --check` passed.
  progress (2026-07-03, Hermes Desktop reference status refresh): updated `docs/product/hermes-desktop-reference.md` so its Gormes-first decision/boundary sections are clearly historical and superseded by ADR 0006/0007. It now says current mainline targets Hermes Agent through native `HermesChannel`/`/hermes`, while the old Gormes vocabulary applies only to legacy branch/path maintenance.
  validation (Hermes Desktop reference status refresh): `git diff --check` passed.
  progress (2026-07-03, Linux helper relative-prefix fix): while trying to compile the Android durable-key integration smoke on Linux as a non-Android compile check, discovered `scripts/run_linux_release_build.sh` failed when `NAVIVOX_LINUX_BUILD_DEPS_DIR` was relative because rewritten `.pc` files kept relative prefixes that CMake evaluated from nested build dirs. Fixed the helper to require `python3` and resolve `NAVIVOX_LINUX_BUILD_DEPS_DIR` to an absolute path before creating prefix/pkgconfig/deb dirs.
  validation (Linux helper relative-prefix fix): `bash -n scripts/run_linux_release_build.sh`; `rm -rf .dart_tool/navivox-linux-deps build/linux && NAVIVOX_LINUX_BUILD_DEPS_DIR=.dart_tool/navivox-linux-deps npm run linux:release-build` returned success; `git diff --check` passed. Follow-up `PKG_CONFIG_PATH=$PWD/.dart_tool/navivox-linux-deps/pkgconfig CPATH=$PWD/.dart_tool/navivox-linux-deps/prefix/usr/include flutter test -d linux integration_test/durable_key_store_android_smoke_test.dart` built the Linux bundle but failed to start a Linux integration-test debug connection, so no integration-test pass receipt is claimed.
  progress (2026-07-03, helper/workflow manifest syntax sweep): reran syntax/manifest checks after the latest shell helper, package script, workflow, and runbook edits.
  validation (helper/workflow manifest syntax sweep): `bash -n scripts/*.sh`; package JSON parse; Ruby YAML parse confirms `.github/workflows/hermes-platform-smoke.yml` has a string `on` key and no boolean-`true` key; `git diff --check` passed.
  progress (2026-07-03, Android APK build after integration/helper changes): rebuilt the Android debug APK after the durable-key adapter integration smoke edit, live-mic prep helper, and Android smoke wrapper hardening to ensure normal Android packaging still works.
  validation (Android APK build after integration/helper changes): `flutter build apk --debug` returned success (artifact `build/app/outputs/flutter-apk/app-debug.apk`; only existing Kotlin Gradle Plugin migration warnings from project/plugins).
  progress (2026-07-03, durable reconnect surface honesty): updated Hermes surface readiness copy for the legacy durable reconnect blocker. It no longer implies all Android secure keypair work is missing; it now says Android keypair readiness exists while full Gormes issuance/auth/silent reconnect remains unproven. Expanded the surface-readiness test to assert that nuance.
  validation (durable reconnect surface honesty): `dart format lib/core/hermes/policy/hermes_surface_readiness.dart test/core/hermes/hermes_api_test.dart`; `flutter test test/core/hermes/hermes_api_test.dart --plain-name 'surface readiness does not claim unwired Hermes APIs are implemented'` passed; `git diff --check` passed.
  progress (2026-07-03, full regression after docs/readiness/code cleanup): reran the full Flutter unit/widget suite after the ADR/product docs cleanup, durable reconnect runbook expansion, Linux helper fix, Android APK rebuild, and durable reconnect surface honesty change.
  validation (full regression after docs/readiness/code cleanup): `flutter test --concurrency=1` passed (1008 tests).
  progress (2026-07-03, configured provider smoke after cleanup): reran the configured local Hermes provider smoke after the docs/readiness/code cleanup to keep the provider-backed text plus transcript-voice receipt current.
  validation (configured provider smoke after cleanup): `npm run hermes:provider-smoke:local` returned success (Playwright provider-backed Hermes spec 1 pass). This remains transcript voice, not physical Android microphone or Hermes realtime/server audio.
  progress (2026-07-03, remote workflow visibility recheck): rechecked GitHub workflow visibility after adding the dispatch helper and workflow hardening. `gh workflow list` still only shows `pages-build-deployment`, and `gh run list --limit 5` shows only Pages runs, so the Hermes platform workflow is still not published/visible remotely and cannot produce Windows/iOS/hosted Android receipts yet.
  validation (remote workflow visibility recheck): `gh workflow list && gh run list --limit 5` captured the blocker state; no host-runner receipt claimed.
  progress (2026-07-03, read-only readiness audit helper): added `scripts/audit_hermes_readiness.sh`, exposed as `npm run hermes:readiness-audit`, to print a quick local prompt-to-artifact/blocker snapshot without claiming completion. It checks key docs/helpers/workflow files, local build artifacts, gh workflow visibility, Android device availability, configured Hermes home presence, and explicit external/deferred blockers. It exits successfully by default but supports `NAVIVOX_FAIL_ON_BLOCKERS=1` for strict use.
  validation (read-only readiness audit helper): `bash -n scripts/audit_hermes_readiness.sh`; package JSON parse; `npm run hermes:readiness-audit` printed 5 blockers (workflow not visible, no online Android device, realtime/server audio unimplemented, admin/edit deferred/read-only, real Android Gormes durable reconnect unproven); `git diff --check` passed.
  progress (2026-07-03, strict readiness audit receipt): ran the readiness helper in strict mode to prove the completion gate stays red while external blockers remain.
  validation (strict readiness audit receipt): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 and reported the same 5 blockers, so the active goal must not be marked complete.
  progress (2026-07-03, readiness audit explicit blocker expansion): expanded `scripts/audit_hermes_readiness.sh` so the read-only helper enumerates every explicit remaining deferred/product blocker instead of collapsing them into one admin/edit line: config editing/admin, memory UI, jobs/schedules admin, messaging gateways, persona/SOUL, attachments/media, files/context folders, raw diagnostics/log export, and multi-endpoint/profile management.
  validation (readiness audit explicit blocker expansion): `bash -n scripts/audit_hermes_readiness.sh`; `npm run hermes:readiness-audit` printed 13 blockers including each explicit deferred surface; `git diff --check` passed.
  progress (2026-07-03, readiness audit blocker taxonomy): added a `Current blocker detail` section to `docs/runbooks/hermes-readiness-audit.md` grouping the helper's blockers into native/host receipts, voice/audio, deferred Hermes surfaces, and legacy durable reconnect. This gives future agents a compact map from the expanded helper output back to the original active-goal categories.
  validation (readiness audit blocker taxonomy): `git diff --check` passed.
  progress (2026-07-03, tooling docs regression): reran tooling/documentation contract tests after README, docs index, runbook, product plan, ADR, readiness-audit, and screenshot-related status refreshes.
  validation (tooling docs regression): `flutter test test/tooling` passed (19 tests).
  progress (2026-07-03, readiness audit action hints): updated `scripts/audit_hermes_readiness.sh` blocker messages with direct next commands/runbooks for the remaining external receipt paths: publish workflow then `npm run platform:workflow-smoke`, start an audio-capable Android target then `npm run android:live-mic-prep`, install `gh` if missing, and follow `docs/runbooks/android/durable-keystore-smoke.md` for real Gormes durable reconnect closeout.
  validation (readiness audit action hints): `bash -n scripts/audit_hermes_readiness.sh`; `npm run hermes:readiness-audit` printed the expanded blockers with action hints; `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after action hints): reran the readiness helper in strict mode after expanding blocker action hints to verify it still fails closed while the external/deferred blockers remain.
  validation (strict readiness audit after action hints): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 and reported 13 blockers; no completion claimed.
  progress (2026-07-03, README helper contract): expanded the README tooling contract so future edits must keep the current readiness/workflow/Android closeout helper commands discoverable: `npm run hermes:readiness-audit`, `npm run platform:workflow-smoke`, `npm run android:live-mic-prep`, and `npm run android:durable-key-smoke`.
  validation (README helper contract): `dart format test/tooling/readme_assets_contract_test.dart`; `flutter test test/tooling/readme_assets_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, package helper script contract): added `test/tooling/package_scripts_contract_test.dart` to lock the active Hermes/provider/Android/platform/Linux helper scripts into `package.json` and assert their target shell files exist and are executable. This protects the closeout/runbook commands from drifting out of package scripts.
  validation (package helper script contract): `dart format test/tooling/package_scripts_contract_test.dart`; `flutter test test/tooling/package_scripts_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after package contract): reran the full tooling/documentation contract suite after adding the package helper script contract.
  validation (full tooling regression after package contract): `flutter test test/tooling` passed (20 tests).
  progress (2026-07-03, Hermes platform workflow contract): added `test/tooling/hermes_platform_workflow_contract_test.dart` to lock the unpublished host-runner workflow shape in tests. It asserts the workflow keeps the dispatch trigger, timeouts, Linux analyze/test/web/Android/Linux build gates, provider smoke, hardened Android emulator wrapper scripts, Windows build/artifact, and iOS simulator build/artifact.
  validation (Hermes platform workflow contract): `dart format test/tooling/hermes_platform_workflow_contract_test.dart`; `flutter test test/tooling/hermes_platform_workflow_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after workflow contract): reran the full tooling/documentation contract suite after adding the Hermes platform workflow contract.
  validation (full tooling regression after workflow contract): `flutter test test/tooling` passed (21 tests).
  progress (2026-07-03, readiness audit helper contract): added `test/tooling/hermes_readiness_audit_contract_test.dart` to lock the read-only readiness helper's blocker coverage and non-completion caveats. It asserts the helper names every explicit external/deferred blocker plus action hints, and the audit doc still warns not to count prep/proxy signals as completion.
  validation (readiness audit helper contract): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, Android target availability recheck): rechecked attached Android targets before attempting real mic/durable reconnect closeout. No Android device/emulator is currently online, so the real spoken mic loop and Android reconnect receipts remain blocked.
  validation (Android target availability recheck): `adb devices` listed no devices; `flutter devices` listed only Linux desktop and Chrome web.
  progress (2026-07-03, durable reconnect runbook contract tightening): strengthened `test/tooling/runbooks/android/pairing_and_keystore_contract_test.dart` so the durable keystore runbook must keep the key-readiness-vs-full-Gormes-reconnect distinction, the `npm run android:durable-key-smoke` command, the integration test path, the explicit non-proof warning, the full reconnect closeout section, silent reconnect evidence, and the open-blocker warning.
  validation (durable reconnect runbook contract tightening): `dart format test/tooling/runbooks/android/pairing_and_keystore_contract_test.dart`; `flutter test test/tooling/runbooks/android/pairing_and_keystore_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, Android live mic runbook): added `docs/runbooks/android/live-mic-smoke.md` and linked it from `docs/README.md` to capture the manual physical-audio closeout for the remaining Android microphone + continuous voice blocker. The runbook separates prep from pass evidence, requires real provider/model credentials, spoken phrase capture, provider reply, continuous voice re-arm, and secret/diagnostics checks, and lists proxy signals that do not count.
  validation (Android live mic runbook): added `test/tooling/runbooks/android/live_mic_smoke_contract_test.dart`; `dart format test/tooling/runbooks/android/live_mic_smoke_contract_test.dart`; `flutter test test/tooling/runbooks/android/live_mic_smoke_contract_test.dart` passed.
  progress (2026-07-03, runbook topology contract): expanded `test/tooling/runbooks/topology/runbooks_topology_contract_test.dart` so the docs index must keep the Hermes platform smoke, Hermes readiness audit, and Android live microphone smoke runbooks discoverable alongside the existing Android/Termux/web runbooks.
  validation (runbook topology contract): `dart format test/tooling/runbooks/topology/runbooks_topology_contract_test.dart`; `flutter test test/tooling/runbooks/topology/runbooks_topology_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, platform-to-live-mic runbook link): linked the Android live smoke section of `docs/runbooks/hermes-platform-smoke.md` to the canonical `docs/runbooks/android/live-mic-smoke.md` physical-audio receipt checklist, and tightened the live mic contract test so the cross-link cannot drift.
  validation (platform-to-live-mic runbook link): `dart format test/tooling/runbooks/android/live_mic_smoke_contract_test.dart`; `flutter test test/tooling/runbooks/android/live_mic_smoke_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after live-mic docs): reran the full tooling/documentation contract suite after adding the Android live mic runbook, topology coverage, and platform cross-link.
  validation (full tooling regression after live-mic docs): `flutter test test/tooling` passed (23 tests).
  progress (2026-07-03, raw diagnostics/log export deferred surface): split raw diagnostics/log export into its own Hermes surface readiness row with `deferred` status while keeping bounded diagnostics `readOnly`. This makes the active-goal diagnostics/log-export blocker visible in-app instead of relying only on runbook/audit text.
  validation (raw diagnostics/log export deferred surface): `dart format lib/core/hermes/policy/hermes_surface_readiness.dart test/core/hermes/hermes_api_test.dart`; `flutter test test/core/hermes/hermes_api_test.dart test/features/hermes_chat/screens/hermes_chat_screen_test.dart` passed (35 tests); `git diff --check` passed.
  progress (2026-07-03, surface dialog raw diagnostics coverage): tightened `test/features/hermes_chat/screens/hermes_chat_screen_test.dart` so the Hermes surface readiness dialog must expose the new `Raw diagnostics/log export` deferred row, scrolling the dialog to prove the in-app blocker is reachable.
  validation (surface dialog raw diagnostics coverage): `dart format test/features/hermes_chat/screens/hermes_chat_screen_test.dart`; `flutter test test/features/hermes_chat/screens/hermes_chat_screen_test.dart --plain-name 'shows connected Hermes capability status and local voice boundary'` passed; `git diff --check` passed.
  progress (2026-07-03, jobs admin deferred surface split): split the Hermes `Jobs/schedules` readiness row into `Jobs/schedules inventory` (`readOnly` when `/api/jobs` is advertised) and `Jobs/schedules admin` (`deferred`) so the active-goal jobs/schedules admin blocker is explicit in-app while preserving the read-only inventory receipt.
  validation (jobs admin deferred surface split): `dart format lib/core/hermes/policy/hermes_surface_readiness.dart test/core/hermes/hermes_api_test.dart`; `flutter test test/core/hermes/hermes_api_test.dart test/features/hermes_chat/screens/hermes_chat_screen_test.dart` passed (35 tests); `git diff --check` passed.
  progress (2026-07-03, readiness audit live-mic runbook hint): updated `scripts/audit_hermes_readiness.sh` so the no-Android-target blocker points at `docs/runbooks/android/live-mic-smoke.md` before `npm run android:live-mic-prep`, and tightened the readiness audit contract test to keep that canonical runbook discoverable.
  validation (readiness audit live-mic runbook hint): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `npm run hermes:readiness-audit` still reported 13 blockers; `git diff --check` passed.
  progress (2026-07-03, static analysis after readiness surface/docs changes): reran analyzer after the latest Hermes surface readiness splits, live-mic runbook linkage, and audit helper changes.
  validation (static analysis after readiness surface/docs changes): `flutter analyze` passed with no issues.
  progress (2026-07-03, README live-mic runbook link): updated the README Android live microphone closeout copy and docs list to point directly at `docs/runbooks/android/live-mic-smoke.md`, then tightened the README tooling contract so that canonical runbook link remains discoverable with `npm run android:live-mic-prep`.
  validation (README live-mic runbook link): `dart format test/tooling/readme_assets_contract_test.dart`; `flutter test test/tooling/readme_assets_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, surface dialog jobs admin coverage): tightened the Hermes surface dialog widget test so it scrolls to and verifies the explicit `Jobs/schedules admin` deferred row as well as the raw diagnostics/log export row, ensuring the in-app deferred admin blocker remains visible after the inventory/admin split.
  validation (surface dialog jobs admin coverage): `dart format test/features/hermes_chat/screens/hermes_chat_screen_test.dart`; `flutter test test/features/hermes_chat/screens/hermes_chat_screen_test.dart --plain-name 'shows connected Hermes capability status and local voice boundary'` passed; `git diff --check` passed.
  progress (2026-07-03, Hermes focused regression after readiness splits): reran the focused Hermes core/channel/UI regression set after splitting jobs admin and raw diagnostics/log export readiness surfaces.
  validation (Hermes focused regression after readiness splits): `flutter test test/core/hermes test/features/hermes_chat` passed (71 tests).
  progress (2026-07-03, external blocker recheck): rechecked the two external receipt gates before attempting completion: remote workflow visibility and Android target availability.
  validation (external blocker recheck): `gh workflow list` still listed only `pages-build-deployment`; `adb devices` listed no Android devices; `flutter devices` listed only Linux desktop and Chrome web. Windows/iOS hosted receipts, Android live mic, and Android durable reconnect remain blocked.
  progress (2026-07-03, provider-backed Hermes smoke refresh): refreshed the configured local Hermes provider-backed web smoke after the readiness surface/docs/test guard changes. This covers typed provider chat plus deterministic transcript voice against the local configured Hermes home; it remains not a physical microphone or server-audio receipt.
  validation (provider-backed Hermes smoke refresh): `npm run hermes:provider-smoke:local` passed (Playwright reported 1 pass).
  progress (2026-07-03, full Dart regression after readiness changes): reran the full Flutter test suite after the Hermes surface readiness splits, live mic runbook/docs updates, audit helper hints, and tooling contract additions.
  validation (full Dart regression after readiness changes): `flutter test --concurrency=1` passed (1012 tests).
  progress (2026-07-03, GOAL_STATE readiness refresh): updated `GOAL_STATE.md` with the latest 1012-test receipt, provider smoke refresh, canonical Android live mic runbook pointer, and a delivered-slice summary for readiness guardrails/closeout runbooks. It still records workflow visibility and Android target blockers.
  validation (GOAL_STATE readiness refresh): `git diff --check` passed.
  progress (2026-07-03, web e2e release build refresh): rebuilt the Flutter web e2e bundle after the Hermes surface readiness splits, live-mic docs/runbook links, audit helper updates, and GOAL_STATE refresh.
  validation (web e2e release build refresh): `flutter build web --release -t lib/main_e2e.dart` passed and produced `build/web` (with existing wasm dry-run warnings from `flutter_secure_storage_web`); `git diff --check` passed.
  progress (2026-07-03, installed Hermes live connect smoke refresh): refreshed the installed-Hermes live API connect smoke after the latest readiness/docs/build updates. This validates `/hermes` against a real installed Hermes API server with isolated temp home and generated test API key, but still does not cover provider/model credentials or physical voice.
  validation (installed Hermes live connect smoke refresh): `npm run hermes:live-smoke` passed (Playwright reported 1 pass).
  progress (2026-07-03, Android debug APK build refresh): rebuilt the Android debug APK after the latest readiness/docs/test updates to keep the install artifact fresh for the live mic and durable-key Android runbooks.
  validation (Android debug APK build refresh): `flutter build apk --debug` passed and produced the debug APK; Gradle printed only existing Kotlin plugin compatibility warnings.
  progress (2026-07-03, Linux release build refresh): reran the rootless Linux release build helper after the latest readiness/docs/test updates to keep the desktop artifact current.
  validation (Linux release build refresh): `npm run linux:release-build` passed; `build/linux/x64/release/bundle/navivox` exists and is executable; `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after artifact refreshes): reran the readiness helper in strict mode after refreshing web, Android APK, Linux, live Hermes, provider smoke, full tests, and docs state. The completion gate still fails closed because external/deferred blockers remain.
  validation (strict readiness audit after artifact refreshes): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 and reported 13 blockers, including workflow not visible to `gh`, no Android target, realtime/server audio, deferred product surfaces, and real Android + Gormes durable reconnect.
  progress (2026-07-03, platform workflow dispatch blocker recheck): reran the platform workflow helper without watching to verify the native-host receipt path is still blocked at workflow visibility rather than by a later job failure.
  validation (platform workflow dispatch blocker recheck): `NAVIVOX_WATCH_WORKFLOW=false npm run platform:workflow-smoke` exited 2 with `Workflow 'Hermes platform smoke' is not visible to gh`, so Windows/iOS/hosted Android receipts cannot be claimed from CI yet.
  progress (2026-07-03, platform runbook receipt refresh): updated `docs/runbooks/hermes-platform-smoke.md` with the latest local receipt count (`flutter test --concurrency=1` now 1012 tests), refreshed installed-Hermes live smoke wording, and refreshed provider smoke wording (`npm run hermes:provider-smoke:local`, 1 Playwright pass). The runbook still states physical mic/server audio and native-host receipts remain separate.
  validation (platform runbook receipt refresh): `git diff --check` passed.
  progress (2026-07-03, readiness audit runbook presence checks): updated `scripts/audit_hermes_readiness.sh` so the read-only audit verifies the canonical Android live microphone and durable reconnect runbooks exist alongside the platform/readiness docs.
  validation (readiness audit runbook presence checks): `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `npm run hermes:readiness-audit` still reported 13 blockers; `git diff --check` passed.
  progress (2026-07-03, fake Hermes browser smoke refresh): refreshed the fake Hermes browser e2e smoke against the rebuilt `lib/main_e2e.dart` web bundle. A first direct Playwright invocation failed with `ERR_CONNECTION_REFUSED` because no server was running; after starting `node serve_web.mjs`, the focused Hermes smoke passed.
  validation (fake Hermes browser smoke refresh): `node serve_web.mjs` plus `npx playwright test --config=playwright.config.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list` passed (2 Chromium tests).
  progress (2026-07-03, helper syntax sweep): reran shell and JS syntax checks for the Hermes/provider/Android/platform/Linux helper scripts and Playwright smoke specs after the readiness-audit/live-mic/workflow updates.
  validation (helper syntax sweep): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs` all passed.
  progress (2026-07-03, Android app unit refresh): reran app-scoped Android native unit tests after Android APK, durable-key, live-mic runbook, and readiness-helper changes.
  validation (Android app unit refresh): `cd android && ./gradlew :app:testDebugUnitTest` passed (`BUILD SUCCESSFUL`).
  progress (2026-07-03, readiness audit receipt refresh): updated `docs/runbooks/hermes-readiness-audit.md` to reflect the latest provider smoke, APK/native Android unit, Linux release, fake browser smoke, live Hermes smoke, 1012-test, and platform workflow-dispatch blocker receipts.
  validation (readiness audit receipt refresh): `git diff --check` passed.
  progress (2026-07-03, tooling regression after readiness audit receipt refresh): reran the full tooling/documentation contract suite after refreshing the readiness audit evidence table.
  validation (tooling regression after readiness audit receipt refresh): `flutter test test/tooling` passed (23 tests).
  progress (2026-07-03, focused browser regression refresh): reran the focused browser regression set against `node serve_web.mjs` after the latest readiness docs/build/test updates.
  validation (focused browser regression refresh): `node serve_web.mjs` plus `npx playwright test --config=playwright.config.mjs playwright/tests/regression/navivox-e2e.spec.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list` completed successfully.
  progress (2026-07-03, testing-plan Hermes smoke matrix): added a current Hermes smoke matrix near the top of `docs/product/testing-plan.md` so the historical Gormes-first testing draft points to the active Hermes commands, completion boundaries, physical-audio runbook, native-host workflow gate, and non-completion readiness audit caveat.
  validation (testing-plan Hermes smoke matrix): `flutter test test/tooling` passed (23 tests); `git diff --check` passed.
  progress (2026-07-03, testing-plan contract): added `test/tooling/testing_plan_contract_test.dart` so the historical testing plan must keep pointing readers at the active Hermes smoke matrix, Android live-mic runbook, platform workflow gate, and readiness-audit non-completion caveat.
  validation (testing-plan contract): `dart format test/tooling/testing_plan_contract_test.dart`; `flutter test test/tooling/testing_plan_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after testing-plan contract): reran the full tooling/documentation contract suite after adding the testing-plan contract.
  validation (full tooling regression after testing-plan contract): `flutter test test/tooling` passed (24 tests).
  progress (2026-07-03, surface readiness exact-title contract): tightened `test/core/hermes/hermes_api_test.dart` so the Hermes surface readiness test asserts the exact set of surfaced rows, covering all active-goal availability/read-only/deferred/blocked surfaces including jobs inventory/admin split and bounded-vs-raw diagnostics split.
  validation (surface readiness exact-title contract): `dart format test/core/hermes/hermes_api_test.dart`; `flutter test test/core/hermes/hermes_api_test.dart --plain-name 'surface readiness does not claim unwired Hermes APIs are implemented'` passed; `git diff --check` passed.
  progress (2026-07-03, readiness audit live-mic checklist link): updated `docs/runbooks/hermes-readiness-audit.md` so the Android speech/mic readiness row points to the canonical `docs/runbooks/android/live-mic-smoke.md` physical-audio checklist, and tightened the readiness audit tooling contract to keep that link present.
  validation (readiness audit live-mic checklist link): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after readiness live-mic link): reran the full tooling/documentation contract suite after linking the readiness audit evidence table to the canonical Android live mic checklist.
  validation (full tooling regression after readiness live-mic link): `flutter test test/tooling` passed (24 tests).
  progress (2026-07-03, analyzer after testing-plan/readiness contracts): reran static analysis after adding the testing-plan contract, tightening surface readiness title coverage, and refreshing readiness-audit links.
  validation (analyzer after testing-plan/readiness contracts): `flutter analyze` passed with no issues.
  progress (2026-07-03, readiness audit native-host blocker split): expanded `scripts/audit_hermes_readiness.sh` so Windows and iOS native-host build receipts are explicit blockers in addition to the unpublished workflow blocker, matching the active-goal native runner requirement.
  validation (readiness audit native-host blocker split): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `npm run hermes:readiness-audit` now reports 15 blockers; `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after native-host split): reran the readiness helper in strict mode after splitting Windows/iOS host blockers to verify the completion gate still fails closed with the expanded blocker taxonomy.
  validation (strict readiness audit after native-host split): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 and reported 15 blockers, including workflow visibility, Windows host, iOS host, Android target, realtime/server audio, deferred surfaces, and real Android + Gormes durable reconnect.
  progress (2026-07-03, readiness audit doc native-host contract): tightened `test/tooling/hermes_readiness_audit_contract_test.dart` so the readiness audit doc must keep separate Windows and iOS/macOS host receipt blockers in its blocker taxonomy, matching the helper's split native-host output.
  validation (readiness audit doc native-host contract): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, readiness audit emulator inventory hint): updated `scripts/audit_hermes_readiness.sh` so when no Android device is online it prints the Flutter emulator inventory as informational context while explicitly stating emulator availability is not an online/audio receipt.
  validation (readiness audit emulator inventory hint): `bash -n scripts/audit_hermes_readiness.sh`; `npm run hermes:readiness-audit` still reported 15 blockers; `git diff --check` passed.
  progress (2026-07-03, readiness audit emulator hint contract): tightened `test/tooling/hermes_readiness_audit_contract_test.dart` so the audit helper must keep the emulator inventory hint and the `not an online/audio receipt` caveat.
  validation (readiness audit emulator hint contract): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, readiness audit provider proxy caveat): changed `scripts/audit_hermes_readiness.sh` so a configured local Hermes home is printed as informational context rather than an OK provider-smoke receipt, and added contract coverage requiring the `not a provider-smoke receipt` caveat plus `npm run hermes:provider-smoke:local` proof command.
  validation (readiness audit provider proxy caveat): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `npm run hermes:readiness-audit` still reported 15 blockers; `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after provider proxy caveat): reran the readiness helper in strict mode after changing configured Hermes home from an OK signal to informational context.
  validation (strict readiness audit after provider proxy caveat): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 and still reported 15 blockers, so configured-home presence is no longer treated as completion evidence.
  progress (2026-07-03, full tooling regression after provider proxy caveat): reran the full tooling/documentation contract suite after the readiness audit provider proxy caveat and emulator hint contract changes.
  validation (full tooling regression after provider proxy caveat): `flutter test test/tooling` passed (24 tests).
  progress (2026-07-03, GOAL_STATE guardrail refresh): refreshed delivered slice 28 in `GOAL_STATE.md` with the testing-plan contract, 24-test tooling suite, strict readiness audit's 15-blocker split, and the configured-Hermes-home informational-only caveat.
  validation (GOAL_STATE guardrail refresh): `git diff --check` passed.
  progress (2026-07-03, GOAL_STATE guardrail contract): added `test/tooling/goal_state_contract_test.dart` so GOAL_STATE must preserve the active Hermes continuous-voice goal, latest local test count, live-mic runbook, strict readiness blocker count, configured-Hermes-home caveat, remaining Android/native-host/durable reconnect blockers, and spoken-audio/server-audio non-proof caveats.
  validation (GOAL_STATE guardrail contract): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after GOAL_STATE contract): reran the full tooling/documentation contract suite after adding the GOAL_STATE guardrail contract.
  validation (full tooling regression after GOAL_STATE contract): `flutter test test/tooling` passed (25 tests).
  progress (2026-07-03, Android emulator speech-smoke retry): attempted to reduce the Android target blocker by launching `fractal_test` directly with `/usr/lib/android-sdk/emulator/emulator -avd fractal_test -no-window -no-audio -no-snapshot -gpu swiftshader_indirect -accel off` and running `npm run android:voice-smoke` with extended waits. The emulator reached enough discovery for Flutter to attempt install, but package service instability returned.
  validation (Android emulator speech-smoke retry): `NAVIVOX_ANDROID_DEVICE_WAIT_SECONDS=240 NAVIVOX_ANDROID_BOOT_WAIT_SECONDS=420 NAVIVOX_ANDROID_TEST_TIMEOUT_SECONDS=900 npm run android:voice-smoke` failed during install/start with `cmd: Failure calling service package: Broken pipe (32)` and `Unable to start the app on the device`; no Android voice receipt claimed.
  progress (2026-07-03, Android emulator blocker evidence refresh): updated `docs/runbooks/hermes-readiness-audit.md` and `GOAL_STATE.md` with the latest `fractal_test` retry failure so future audits distinguish prior Android receipts from the current package-service instability.
  validation (Android emulator blocker evidence refresh): `flutter test test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, live-mic runbook package-service caveat): updated `docs/runbooks/android/live-mic-smoke.md` failure notes with the observed Android package-service instability (`Broken pipe (32)`, `Unable to start the app`) and clarified those install/start failures are not microphone evidence. Tightened the live-mic runbook contract to preserve that caveat.
  validation (live-mic runbook package-service caveat): `dart format test/tooling/runbooks/android/live_mic_smoke_contract_test.dart`; `flutter test test/tooling/runbooks/android/live_mic_smoke_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after live-mic failure caveat): reran the full tooling/documentation contract suite after adding the Android package-service failure caveat to the live mic runbook.
  validation (full tooling regression after live-mic failure caveat): `flutter test test/tooling` passed (25 tests).
  progress (2026-07-03, configured-home non-completion caveat): updated `docs/runbooks/hermes-readiness-audit.md` so the `Do not count as completion` section explicitly says configured Hermes home presence is not provider-smoke proof; only an actual `npm run hermes:provider-smoke:local` receipt counts for provider-backed chat/transcript voice. Tightened the readiness audit contract accordingly.
  validation (configured-home non-completion caveat): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after configured-home caveat): reran the full tooling/documentation contract suite after adding the configured-Hermes-home non-completion caveat to the readiness audit.
  validation (full tooling regression after configured-home caveat): `flutter test test/tooling` passed (25 tests).
  progress (2026-07-03, focused Hermes regression after caveat/docs updates): reran the focused Hermes core/channel/UI regression set after the latest readiness-audit, live-mic runbook, GOAL_STATE, and surface-readiness contract changes.
  validation (focused Hermes regression after caveat/docs updates): `flutter test test/core/hermes test/features/hermes_chat` passed (71 tests).
  progress (2026-07-03, full Dart regression after guardrail contracts): reran the full Flutter suite after adding the GOAL_STATE/testing-plan/readiness/live-mic contracts and latest blocker docs.
  validation (full Dart regression after guardrail contracts): `flutter test --concurrency=1` passed (1014 tests).
  progress (2026-07-03, GOAL_STATE contract count refresh): updated the GOAL_STATE tooling contract for the refreshed 1014-test suite count.
  validation (GOAL_STATE contract count refresh): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, platform runbook test-count refresh): updated `docs/runbooks/hermes-platform-smoke.md` so its local Flutter test receipt matches the latest 1014-test full-suite pass.
  validation (platform runbook test-count refresh): `git diff --check` passed.
  progress (2026-07-03, platform smoke runbook contract): added `test/tooling/hermes_platform_smoke_runbook_contract_test.dart` so the platform smoke runbook must preserve build commands, native-host boundaries, workflow dispatch, live/provider smoke commands, transcript-vs-physical-voice caveats, Android live-mic prep non-receipt caveat, and durable-key non-reconnect caveat.
  validation (platform smoke runbook contract): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after platform runbook contract): reran the full tooling/documentation contract suite after adding the platform smoke runbook contract.
  validation (full tooling regression after platform runbook contract): `flutter test test/tooling` passed (26 tests).
  progress (2026-07-03, full Dart regression after platform runbook contract): reran the full Flutter suite after adding the platform smoke runbook contract and refreshing the tooling/docs contracts.
  validation (full Dart regression after platform runbook contract): `flutter test --concurrency=1` passed (1015 tests). Updated `GOAL_STATE.md`, `docs/runbooks/hermes-readiness-audit.md`, `docs/runbooks/hermes-platform-smoke.md`, and tooling contracts to the 1015-test count.
  progress (2026-07-03, full tooling regression after 1015-count refresh): reran the full tooling/documentation contract suite after refreshing the platform/readiness/GOAL_STATE test-count receipts.
  validation (full tooling regression after 1015-count refresh): `flutter test test/tooling` passed (26 tests).
  progress (2026-07-03, analyzer after 1015-count refresh): reran static analysis after the platform/readiness/GOAL_STATE count refresh and platform smoke runbook contract.
  validation (analyzer after 1015-count refresh): `flutter analyze` passed with no issues.
  progress (2026-07-03, platform workflow helper diagnostics): improved `scripts/run_hermes_platform_workflow.sh` so when the Hermes platform workflow is not visible it prints the visible `gh workflow list` output, making the native-host CI blocker self-contained in logs.
  validation (platform workflow helper diagnostics): `bash -n scripts/run_hermes_platform_workflow.sh`; `NAVIVOX_WATCH_WORKFLOW=false npm run platform:workflow-smoke` exited 2 and printed visible workflows (`pages-build-deployment` only).
  progress (2026-07-03, package helper strict-mode contract): tightened `test/tooling/package_scripts_contract_test.dart` so every package-exposed Hermes/provider/Android/platform/Linux helper must start with `#!/usr/bin/env bash` and `set -euo pipefail`, preserving fail-closed shell behavior for closeout scripts.
  validation (package helper strict-mode contract): `dart format test/tooling/package_scripts_contract_test.dart`; `flutter test test/tooling/package_scripts_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after helper strict-mode contract): reran the full tooling/documentation contract suite after tightening the package helper strict-mode contract.
  validation (full tooling regression after helper strict-mode contract): `flutter test test/tooling` passed (26 tests).
  progress (2026-07-03, helper syntax sweep after workflow diagnostics): reran shell and JS syntax checks after the platform workflow helper diagnostics and readiness-helper caveat changes.
  validation (helper syntax sweep after workflow diagnostics): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs` all passed.
  progress (2026-07-03, workflow helper diagnostics contract): tightened `test/tooling/hermes_platform_workflow_contract_test.dart` so the dispatch helper must preserve invisible-workflow diagnostics, visible `gh workflow list` evidence, the publish-the-workflow guidance, and fail with exit 2 instead of silently treating workflow YAML as a host-runner receipt.
  validation (workflow helper diagnostics contract): `dart format test/tooling/hermes_platform_workflow_contract_test.dart`; `flutter test test/tooling/hermes_platform_workflow_contract_test.dart` passed (2 tests); `git diff --check` passed.
  progress (2026-07-03, full tooling regression after workflow diagnostics contract): reran the full tooling/documentation contract suite after locking the platform workflow helper diagnostics behavior.
  validation (full tooling regression after workflow diagnostics contract): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, readiness audit workflow visibility evidence): updated `scripts/audit_hermes_readiness.sh` so the workflow-visibility blocker prints the visible `gh workflow list` output and labels it as not native-host receipt evidence, matching the platform workflow dispatch helper diagnostics.
  validation (readiness audit workflow visibility evidence): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 with 15 blockers as expected.
  progress (2026-07-03, readiness audit doc workflow evidence): updated `docs/runbooks/hermes-readiness-audit.md` so the CI receipt robustness row says visible workflow output is blocker evidence only, not native-host receipt evidence, and locked that wording in the readiness audit contract.
  validation (readiness audit doc workflow evidence): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, Android device inventory refresh): ran fresh device inventory for the Android mic/reconnect closeouts. `adb devices` listed no online Android targets; `flutter devices` listed only Linux desktop and Chrome web. Updated `GOAL_STATE.md` to record that the Android mic and reconnect closeouts remain blocked, and refreshed the tooling contract count to 27.
  validation (Android device inventory refresh): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, readiness audit Android device evidence): updated `scripts/audit_hermes_readiness.sh` so the no-Android-target blocker prints `flutter devices` before emulator inventory and labels both as context only, not Android/audio receipt evidence.
  validation (readiness audit Android device evidence): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 with 15 blockers as expected.
  progress (2026-07-03, full tooling regression after Android evidence audit): reran the full tooling/documentation contract suite after adding the `flutter devices` Android blocker evidence to the readiness audit and docs.
  validation (full tooling regression after Android evidence audit): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, provider-backed Hermes smoke refresh): reran the configured local Hermes provider smoke to keep the provider-backed text + deterministic transcript-voice receipt fresh while Android physical mic/server audio remain blocked.
  validation (provider-backed Hermes smoke refresh): `npm run hermes:provider-smoke:local` passed (1 Playwright pass marker).
  progress (2026-07-03, installed-Hermes live connect smoke refresh): reran the installed-Hermes live API connect/session smoke after the readiness-audit and workflow-helper evidence updates.
  validation (installed-Hermes live connect smoke refresh): `npm run hermes:live-smoke` passed (1 Playwright pass marker).
  progress (2026-07-03, platform runbook workflow visibility caveat): updated `docs/runbooks/hermes-platform-smoke.md` so the host-runner section records that local workflow YAML is not a receipt and the current dispatch helper still exits 2 with only `pages-build-deployment` visible.
  validation (platform runbook workflow visibility caveat): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after platform workflow caveat): reran the full tooling/documentation contract suite after adding the explicit workflow-visibility caveat to the platform smoke runbook.
  validation (full tooling regression after platform workflow caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, readiness audit online-Android caveat contract): tightened `test/tooling/hermes_readiness_audit_contract_test.dart` so the audit must keep the online-device caveat: an Android target alone is not enough without manual spoken-audio/provider evidence.
  validation (readiness audit online-Android caveat contract): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, provider smoke non-physical caveat): updated `scripts/run_provider_hermes_smoke.sh` so a passing provider smoke prints that it covered typed text plus deterministic transcript voice only, not physical microphone evidence or Hermes realtime/server audio; locked the caveat in the package scripts contract.
  validation (provider smoke non-physical caveat): `dart format test/tooling/package_scripts_contract_test.dart`; `bash -n scripts/run_provider_hermes_smoke.sh`; `flutter test test/tooling/package_scripts_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, provider smoke after non-physical caveat): reran the configured local Hermes provider smoke after adding the pass-time non-physical-mic/server-audio caveat.
  validation (provider smoke after non-physical caveat): `npm run hermes:provider-smoke:local` passed (Playwright pass marker plus pass-time caveat output).
  progress (2026-07-03, full tooling regression after provider caveat): reran the full tooling/documentation contract suite after adding the provider-smoke pass-time non-physical-mic/server-audio caveat.
  validation (full tooling regression after provider caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, analyzer after provider caveat): reran static analysis after the provider-smoke pass-time caveat and package contract update.
  validation (analyzer after provider caveat): `flutter analyze` passed with no issues.
  progress (2026-07-03, strict readiness audit after provider caveat): reran the fail-closed readiness audit after the provider-smoke pass-time caveat and latest analyzer pass.
  validation (strict readiness audit after provider caveat): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, helper syntax sweep after provider caveat): reran shell and JS syntax checks after the provider-smoke pass-time caveat and latest readiness-audit script updates.
  validation (helper syntax sweep after provider caveat): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs` all passed.
  progress (2026-07-03, live smoke non-provider caveat): updated `scripts/run_live_hermes_smoke.sh` so a passing installed-Hermes smoke prints that it covers API connect/session rendering only and is not provider/model, chat/voice provider, or physical microphone evidence; locked the caveat in the package scripts contract.
  validation (live smoke non-provider caveat): `dart format test/tooling/package_scripts_contract_test.dart`; `bash -n scripts/run_live_hermes_smoke.sh`; `flutter test test/tooling/package_scripts_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, live smoke after non-provider caveat): reran the installed-Hermes live API connect/session smoke after adding the pass-time non-provider/non-mic caveat.
  validation (live smoke after non-provider caveat): `npm run hermes:live-smoke` passed (Playwright pass marker plus pass-time caveat output).
  progress (2026-07-03, full tooling regression after live smoke caveat): reran the full tooling/documentation contract suite after adding the installed-Hermes live-smoke pass-time non-provider/non-mic caveat.
  validation (full tooling regression after live smoke caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, workflow no-watch non-receipt caveat): updated `scripts/run_hermes_platform_workflow.sh` so a successful dispatch with `NAVIVOX_WATCH_WORKFLOW=false` warns that it did not wait for job results and that successful Windows/iOS/Android/Linux job receipts are still required before claiming platform readiness; locked this in the workflow contract.
  validation (workflow no-watch non-receipt caveat): `dart format test/tooling/hermes_platform_workflow_contract_test.dart`; `bash -n scripts/run_hermes_platform_workflow.sh`; `flutter test test/tooling/hermes_platform_workflow_contract_test.dart` passed (2 tests); `git diff --check` passed.
  progress (2026-07-03, platform runbook no-watch non-receipt caveat): updated `docs/runbooks/hermes-platform-smoke.md` so the host-runner section says `NAVIVOX_WATCH_WORKFLOW=false` only proves dispatch and still requires successful Windows/iOS/Android/Linux job receipts from `gh run view`/artifact logs before claiming platform readiness.
  validation (platform runbook no-watch non-receipt caveat): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after no-watch caveat): reran the full tooling/documentation contract suite after adding the no-watch non-receipt caveat to the platform workflow helper and runbook.
  validation (full tooling regression after no-watch caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, workflow missing-run-id fail-closed): updated `scripts/run_hermes_platform_workflow.sh` so a dispatched workflow with no visible run id exits 4 and prints that this is not a platform receipt; successful job evidence from `gh run list`/`gh run view` is still required before claiming readiness.
  validation (workflow missing-run-id fail-closed): `dart format test/tooling/hermes_platform_workflow_contract_test.dart`; `bash -n scripts/run_hermes_platform_workflow.sh`; `flutter test test/tooling/hermes_platform_workflow_contract_test.dart` passed (2 tests); `git diff --check` passed.
  progress (2026-07-03, platform runbook missing-run-id caveat): updated `docs/runbooks/hermes-platform-smoke.md` so the host-runner section documents the dispatch-without-visible-run-id case: helper exits 4, it is not a platform receipt, and `gh run view`/artifact evidence is still required.
  validation (platform runbook missing-run-id caveat): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, platform workflow visibility recheck after fail-closed helper): reran the platform workflow dispatch helper after adding the no-watch and missing-run-id fail-closed caveats.
  validation (platform workflow visibility recheck after fail-closed helper): `NAVIVOX_WATCH_WORKFLOW=false npm run platform:workflow-smoke` still exited 2 as expected because `Hermes platform smoke` is not visible to `gh`; visible workflows remain `pages-build-deployment` only.
  progress (2026-07-03, readiness audit workflow-dispatch non-receipt caveat): updated `docs/runbooks/hermes-readiness-audit.md` so the non-completion list explicitly says workflow dispatch by itself, `NAVIVOX_WATCH_WORKFLOW=false`, missing visible run ids, and unwatched runs are not platform receipts; successful `gh run view` job/artifact evidence is required before claiming Windows/iOS/Android/Linux readiness.
  validation (readiness audit workflow-dispatch non-receipt caveat): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, readiness audit dispatch caveat output): updated `scripts/audit_hermes_readiness.sh` so the read-only audit itself prints that workflow dispatch without successful `gh run view` job/artifact evidence is not a platform receipt and `NAVIVOX_WATCH_WORKFLOW=false` only proves dispatch was requested.
  validation (readiness audit dispatch caveat output): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 with 15 blockers as expected.
  progress (2026-07-03, full tooling regression after readiness dispatch caveat): reran the full tooling/documentation contract suite after adding the workflow-dispatch non-receipt caveat to the readiness audit output.
  validation (full tooling regression after readiness dispatch caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, helper syntax sweep after workflow fail-closed changes): reran shell and JS syntax checks after the platform workflow helper exit-4/missing-run-id change plus the live/provider smoke pass-time caveats.
  validation (helper syntax sweep after workflow fail-closed changes): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs` all passed.
  progress (2026-07-03, Linux release build refresh after helper changes): reran the Linux release build helper after the platform/readiness helper caveat and fail-closed changes.
  validation (Linux release build refresh after helper changes): `npm run linux:release-build` passed and refreshed the local Linux release build receipt.
  progress (2026-07-03, web e2e build refresh after helper changes): reran the web e2e release build after the platform/readiness helper caveat and fail-closed changes.
  validation (web e2e build refresh after helper changes): `flutter build web --release -t lib/main_e2e.dart` passed and rebuilt `build/web`.
  progress (2026-07-03, Android debug APK build refresh after helper changes): reran the Android debug APK build after the platform/readiness helper caveat and fail-closed changes.
  validation (Android debug APK build refresh after helper changes): `flutter build apk --debug` passed and refreshed `build/app/outputs/flutter-apk/app-debug.apk`.
  progress (2026-07-03, full Dart regression after helper/readiness caveats): reran the full Flutter suite after the platform/readiness helper caveat and fail-closed changes.
  validation (full Dart regression after helper/readiness caveats): `flutter test --concurrency=1` passed (1016 tests). Updated `GOAL_STATE.md`, `docs/runbooks/hermes-readiness-audit.md`, `docs/runbooks/hermes-platform-smoke.md`, and tooling contracts to the 1016-test count.
  progress (2026-07-03, full tooling regression after 1016-count refresh): reran the full tooling/documentation contract suite after updating the full-suite count to 1016.
  validation (full tooling regression after 1016-count refresh): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, platform runbook live-smoke non-provider caveat): updated `docs/runbooks/hermes-platform-smoke.md` so the installed-Hermes live-smoke receipt explicitly says it proves API connect/session rendering only, not provider/model behavior, chat/voice provider smoke, or physical microphone evidence.
  validation (platform runbook live-smoke non-provider caveat): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after platform live-smoke caveat): reran the full tooling/documentation contract suite after adding the installed-Hermes live-smoke non-provider/non-mic caveat to the platform smoke runbook.
  validation (full tooling regression after platform live-smoke caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, readiness audit live-smoke non-provider caveat): updated `docs/runbooks/hermes-readiness-audit.md` so the non-completion list says installed-Hermes live connect smoke by itself proves only API connect/session rendering, not provider/model behavior, chat/voice provider smoke, or physical microphone evidence.
  validation (readiness audit live-smoke non-provider caveat): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after live-smoke caveat): reran the fail-closed readiness audit after adding the installed-Hermes live-smoke non-provider/non-mic caveat.
  validation (strict readiness audit after live-smoke caveat): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, full tooling regression after readiness live-smoke caveat): reran the full tooling/documentation contract suite after adding the installed-Hermes live-smoke non-provider/non-mic caveat to the readiness audit docs.
  validation (full tooling regression after readiness live-smoke caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, Android app native unit refresh after APK build): reran the app-scoped Android native unit suite after refreshing the debug APK and helper/readiness caveats.
  validation (Android app native unit refresh after APK build): `cd android && ./gradlew :app:testDebugUnitTest` passed. This is app-scoped native/unit evidence only, not physical mic or full durable reconnect evidence.
  progress (2026-07-03, readiness audit Android build/native-unit caveat): updated `docs/runbooks/hermes-readiness-audit.md` so the Android debug APK/native-unit row explicitly says it is build/native-unit evidence only and not a live Android target, physical mic, or full durable reconnect receipt.
  validation (readiness audit Android build/native-unit caveat): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after Android build/native-unit caveat): reran the full tooling/documentation contract suite after adding the Android build/native-unit non-receipt caveat to the readiness audit docs.
  validation (full tooling regression after Android build/native-unit caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, analyzer after readiness caveat updates): reran static analysis after the latest readiness/platform runbook caveats, 1016-test count refresh, and helper fail-closed changes.
  validation (analyzer after readiness caveat updates): `flutter analyze` passed with no issues.
  progress (2026-07-03, Android release handoff Hermes caveat/hash refresh): updated `docs/runbooks/android/release-handoff.md` with the refreshed debug APK SHA-256 (`453e746d9773b466a7393ec73713943a49276f4bee4465d18a3d083e5cb5ab0a`) and added the Hermes live-mic follow-up boundary: install handoff must still use `docs/runbooks/android/live-mic-smoke.md`, configured Hermes provider/model credentials, provider-backed reply evidence, and must not count APK install as physical-audio evidence.
  validation (Android release handoff Hermes caveat/hash refresh): `sha256sum build/app/outputs/flutter-apk/app-debug.apk`; `dart format test/tooling/runbooks/android/release_handoff_contract_test.dart`; `flutter test test/tooling/runbooks/android/release_handoff_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after Android release handoff caveat): reran the full tooling/documentation contract suite after refreshing the Android release handoff SHA and Hermes live-mic boundary.
  validation (full tooling regression after Android release handoff caveat): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after Android handoff refresh): reran the fail-closed readiness audit after refreshing the Android release handoff hash and Hermes live-mic boundary.
  validation (strict readiness audit after Android handoff refresh): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, readiness audit release-handoff coverage): updated `scripts/audit_hermes_readiness.sh` so the read-only audit checks that `docs/runbooks/android/release-handoff.md` exists alongside the live-mic and durable-keystore runbooks.
  validation (readiness audit release-handoff coverage): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 with 15 blockers as expected.
  progress (2026-07-03, full tooling regression after release-handoff audit coverage): reran the full tooling/documentation contract suite after adding Android release handoff coverage to the readiness audit.
  validation (full tooling regression after release-handoff audit coverage): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, readiness audit APK hash identity): updated `scripts/audit_hermes_readiness.sh` so when the Android debug APK exists it prints the APK SHA-256 as artifact identity only and explicitly labels it as not live Android, mic, or reconnect evidence.
  validation (readiness audit APK hash identity): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 with 15 blockers as expected.
  progress (2026-07-03, full tooling regression after APK hash audit): reran the full tooling/documentation contract suite after adding APK SHA-256 identity output to the readiness audit.
  validation (full tooling regression after APK hash audit): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, platform runbook APK hash identity): updated `docs/runbooks/hermes-platform-smoke.md` so the Android debug APK build receipt records SHA-256 `453e746d9773b466a7393ec73713943a49276f4bee4465d18a3d083e5cb5ab0a` and explicitly labels it as artifact identity only, not live Android/mic/reconnect evidence.
  validation (platform runbook APK hash identity): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, readiness audit APK hash identity doc): updated `docs/runbooks/hermes-readiness-audit.md` so the Android debug APK evidence row records the refreshed SHA-256 and labels it as build/native-unit plus artifact identity only, not live Android/mic/reconnect evidence.
  validation (readiness audit APK hash identity doc): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after readiness APK hash doc): reran the full tooling/documentation contract suite after adding APK SHA-256 artifact identity to the readiness audit docs.
  validation (full tooling regression after readiness APK hash doc): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after APK hash docs): reran the fail-closed readiness audit after adding APK SHA-256 artifact identity to the readiness/platform docs and audit output.
  validation (strict readiness audit after APK hash docs): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, GOAL_STATE APK hash identity): updated `GOAL_STATE.md` so the verified gate records the refreshed Android debug APK SHA-256 and explicitly says the hash is artifact identity only, not physical microphone evidence.
  validation (GOAL_STATE APK hash identity): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after GOAL_STATE APK hash): reran the full tooling/documentation contract suite after adding APK SHA-256 artifact identity to GOAL_STATE.
  validation (full tooling regression after GOAL_STATE APK hash): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, KVM Android voice-readiness refresh): `emulator -accel-check` reported KVM usable. Launched `fractal_test` with KVM/no-window/no-snapshot/gpu swiftshader, waited for boot, then ran `NAVIVOX_ANDROID_DEVICE_ID=<emulator> NAVIVOX_ANDROID_DEVICE_WAIT_SECONDS=1 npm run android:voice-smoke`, which passed. The emulator was stopped after the run; `adb devices` then listed no attached devices. Updated `GOAL_STATE.md` and `docs/runbooks/hermes-readiness-audit.md` to replace the stale no-fresh-Android-target note with a refreshed recognizer/permission readiness receipt while preserving that this is not spoken-audio/provider/TTS/re-arm or real durable reconnect evidence.
  validation (KVM Android voice-readiness refresh): `dart format test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart` passed (2 tests); `git diff --check` passed.
  progress (2026-07-03, KVM Android deterministic voice-loop refresh): relaunched `fractal_test` with KVM/no-window/no-snapshot/gpu swiftshader, waited for boot, then ran `NAVIVOX_ANDROID_DEVICE_ID=<emulator> NAVIVOX_ANDROID_DEVICE_WAIT_SECONDS=1 npm run android:hermes-voice-loop-smoke`, which passed. Updated `GOAL_STATE.md`, `docs/runbooks/hermes-readiness-audit.md`, and `docs/runbooks/hermes-platform-smoke.md` to record refreshed deterministic Android loop evidence while preserving that it is not physical microphone/provider/server-audio evidence.
  validation (KVM Android deterministic voice-loop refresh): `dart format test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed (3 tests); `git diff --check` passed.
  progress (2026-07-03, KVM Android durable-key refresh): relaunched `fractal_test` with KVM/no-window/no-snapshot/gpu swiftshader, waited for boot, then ran `NAVIVOX_ANDROID_DEVICE_ID=<emulator> NAVIVOX_ANDROID_DEVICE_WAIT_SECONDS=1 NAVIVOX_ANDROID_TEST_TIMEOUT_SECONDS=900 npm run android:durable-key-smoke`, which passed. Updated `GOAL_STATE.md`, `docs/runbooks/hermes-readiness-audit.md`, and `docs/runbooks/hermes-platform-smoke.md` to record refreshed Android keypair readiness while preserving that durable credential issuance and silent reconnect remain future work.
  validation (KVM Android durable-key refresh): `dart format test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed (3 tests); `git diff --check` passed.
  progress (2026-07-03, KVM Android live-mic prep refresh): relaunched `fractal_test` with KVM/no-window/no-snapshot/gpu swiftshader, waited for boot, then ran `NAVIVOX_ANDROID_DEVICE_ID=<emulator> NAVIVOX_ANDROID_DEVICE_WAIT_SECONDS=1 NAVIVOX_ANDROID_SKIP_BUILD=1 NAVIVOX_ANDROID_HERMES_URL=http://10.0.2.2:8642 npm run android:live-mic-prep`, which installed/launched/granted microphone permission. Updated `GOAL_STATE.md`, `docs/runbooks/hermes-readiness-audit.md`, and `docs/runbooks/hermes-platform-smoke.md` while preserving that prep is not physical-audio/provider/TTS/re-arm evidence.
  validation (KVM Android live-mic prep refresh): `dart format test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed (3 tests); `git diff --check` passed.
  progress (2026-07-03, full tooling regression after Android KVM receipts): reran the full tooling/documentation contract suite after refreshing Android voice readiness, deterministic voice-loop, and durable-key receipts on KVM-backed `fractal_test`.
  validation (full tooling regression after Android KVM receipts): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after Android KVM receipts): reran the fail-closed readiness audit after refreshing Android recognizer/permission, deterministic voice-loop, and durable-key receipts on KVM-backed `fractal_test`.
  validation (strict readiness audit after Android KVM receipts): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, analyzer after Android KVM receipts): reran static analysis after refreshing Android recognizer/permission, deterministic voice-loop, and durable-key evidence plus related docs/contracts.
  validation (analyzer after Android KVM receipts): `flutter analyze` passed with no issues.
  progress (2026-07-03, live-mic runbook prep receipt): updated `docs/runbooks/android/live-mic-smoke.md` itself with the KVM `fractal_test` live-mic prep receipt and preserved that it is prep evidence only, not spoken-audio/provider/TTS/re-arm evidence.
  validation (live-mic runbook prep receipt): `dart format test/tooling/runbooks/android/live_mic_smoke_contract_test.dart`; `flutter test test/tooling/runbooks/android/live_mic_smoke_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after live-mic prep receipt): reran the full tooling/documentation contract suite after adding the KVM `fractal_test` live-mic prep receipt to the canonical live-mic runbook.
  validation (full tooling regression after live-mic prep receipt): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, readiness audit emulator acceleration context): updated `scripts/audit_hermes_readiness.sh` so when no Android target is online it also prints `emulator -accel-check` output as context only, explicitly not audio/live-mic evidence.
  validation (readiness audit emulator acceleration context): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 with 15 blockers as expected.
  progress (2026-07-03, readiness audit doc emulator-accel context): updated `docs/runbooks/hermes-readiness-audit.md` so the blocker-detail section says `flutter devices`, `flutter emulators`, and `emulator -accel-check` are context only, not Android/audio/live-mic receipt evidence.
  validation (readiness audit doc emulator-accel context): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after emulator-accel context): reran the full tooling/documentation contract suite after adding emulator acceleration context to the readiness audit output and docs.
  validation (full tooling regression after emulator-accel context): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after emulator-accel context): reran the fail-closed readiness audit after adding emulator acceleration context to the readiness audit output/docs.
  validation (strict readiness audit after emulator-accel context): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, helper syntax sweep after Android KVM receipts): reran shell and JS syntax checks after refreshing Android KVM voice-readiness, deterministic voice-loop, durable-key, live-mic prep, and readiness-audit context updates.
  validation (helper syntax sweep after Android KVM receipts): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs` all passed.
  progress (2026-07-03, full Dart regression after Android KVM receipts): reran the full Flutter suite after refreshing Android KVM voice-readiness, deterministic voice-loop, durable-key, live-mic prep, and related docs/contracts.
  validation (full Dart regression after Android KVM receipts): `flutter test --concurrency=1` passed (1016 tests).
  progress (2026-07-03, provider smoke refresh after Android KVM receipts): reran configured local Hermes provider smoke after the Android KVM receipt/doc refreshes to keep the provider-backed text + deterministic transcript-voice receipt fresh.
  validation (provider smoke refresh after Android KVM receipts): `npm run hermes:provider-smoke:local` passed (Playwright pass marker plus non-physical-mic/server-audio caveat output).
  progress (2026-07-03, focused Hermes regression after Android KVM receipts): reran focused Hermes core/channel/UI regression after refreshing Android KVM voice-readiness, deterministic voice-loop, durable-key, live-mic prep, and related docs/contracts.
  validation (focused Hermes regression after Android KVM receipts): `flutter test test/core/hermes test/features/hermes_chat` passed (71 tests).
  progress (2026-07-03, installed-Hermes live smoke after Android KVM receipts): reran the installed-Hermes live API connect/session smoke after the Android KVM receipt/doc refreshes.
  validation (installed-Hermes live smoke after Android KVM receipts): `npm run hermes:live-smoke` passed (Playwright pass marker plus non-provider/non-mic caveat output).
  progress (2026-07-03, focused browser regression after Android KVM receipts): reran the focused browser regression (`navivox-e2e.spec.mjs` plus `hermes-smoke.spec.mjs`) against `node serve_web.mjs` after Android KVM receipt/doc refreshes. A first 300-second attempt timed out mid-suite; reran with a longer 900-second timeout.
  validation (focused browser regression after Android KVM receipts): `npx playwright test --config=playwright.config.mjs playwright/tests/regression/navivox-e2e.spec.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list` passed (68 Chromium tests, 8.6m).
  progress (2026-07-03, hosted workflow visibility recheck after browser regression): directly rechecked GitHub Actions workflow visibility before treating Windows/iOS host receipts as available.
  validation (hosted workflow visibility recheck after browser regression): `gh workflow list` still shows only `pages-build-deployment`; `.github/workflows/hermes-platform-smoke.yml` remains unpublished/not visible remotely, so no Windows/iOS/native-host build receipt is available from Actions.
  progress (2026-07-03, analyzer recheck after closeout status refresh): reran static analysis after the Android KVM receipt, browser smoke, workflow visibility, and status-documentation updates.
  validation (analyzer recheck after closeout status refresh): `flutter analyze` passed with no issues.
  progress (2026-07-03, web release build recheck after closeout status refresh): rebuilt the E2E web bundle after Android KVM receipt, browser smoke, workflow visibility, and status-documentation updates.
  validation (web release build recheck after closeout status refresh): `flutter build web --release -t lib/main_e2e.dart` passed and produced `build/web`; Flutter still reports wasm dry-run incompatibilities from `flutter_secure_storage_web` (`dart:html`/`dart:js_util`), which do not fail the JS web build.
  progress (2026-07-03, Android target availability recheck after closeout status refresh): directly rechecked attached Android targets before treating live mic/reconnect as available.
  validation (Android target availability recheck after closeout status refresh): `adb devices` listed no attached Android devices; `flutter devices` listed only Linux desktop and Chrome web, so there is still no responsive Android target for live microphone or real Android durable reconnect closeout.
  progress (2026-07-03, readiness audit receipt refresh after closeout rechecks): updated `docs/runbooks/hermes-readiness-audit.md` to record the latest direct no-Android-target recheck, focused 68-test browser regression, E2E web release build receipt, repeated analyzer pass, and `gh workflow list` visibility blocker.
  validation (readiness audit receipt refresh after closeout rechecks): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after readiness audit receipt refresh): reran the full tooling/documentation contract suite after adding the latest Android-target, web build, browser regression, analyzer, and workflow visibility receipts to the readiness audit.
  validation (full tooling regression after readiness audit receipt refresh): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after receipt refresh): reran the fail-closed readiness audit after refreshing the readiness audit receipts and full tooling contracts.
  validation (strict readiness audit after receipt refresh): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, Android debug APK rebuild after receipt refresh): rebuilt the Android debug APK after the readiness audit receipt refresh and closeout rechecks.
  validation (Android debug APK rebuild after receipt refresh): `flutter build apk --debug` passed; `sha256sum build/app/outputs/flutter-apk/app-debug.apk` remained `453e746d9773b466a7393ec73713943a49276f4bee4465d18a3d083e5cb5ab0a` for a 208.6 MB artifact. This remains artifact identity/build evidence only, not live Android, mic, provider-loop, or reconnect proof.
  progress (2026-07-03, app-scoped Android native unit recheck after receipt refresh): reran app-scoped Android native unit tests after the readiness audit receipt refresh and Android APK rebuild.
  validation (app-scoped Android native unit recheck after receipt refresh): `cd android && ./gradlew :app:testDebugUnitTest` passed (`BUILD SUCCESSFUL`). This remains app-scoped native unit coverage only; the broader third-party `image_picker_android` Gradle caveat and real-device reconnect blocker remain.
  progress (2026-07-03, Linux release build recheck after receipt refresh): reran the Linux release build helper after readiness audit receipt refreshes and Android/web build rechecks.
  validation (Linux release build recheck after receipt refresh): `npm run linux:release-build` passed; `build/linux/x64/release/bundle/navivox` exists and is executable. This is a local Linux receipt only, not Windows/iOS/native-host or Android mic/reconnect evidence.
  progress (2026-07-03, platform workflow helper recheck after receipt refresh): reran the workflow dispatch helper to verify it still fails closed while the Hermes platform workflow is not visible remotely.
  validation (platform workflow helper recheck after receipt refresh): `npm run platform:workflow-smoke` exited 2 as expected; output says `Workflow 'Hermes platform smoke' is not visible to gh` and lists only `pages-build-deployment`. No Windows/iOS/native-host receipt was produced.
  progress (2026-07-03, read-only readiness audit detail snapshot): ran the readiness audit without strict mode and captured the blocker-detail section for the current environment.
  validation (read-only readiness audit detail snapshot): `./scripts/audit_hermes_readiness.sh` reported 15 blockers and 0 warning state; visible workflows only `pages-build-deployment`; `flutter devices` only Linux desktop and Chrome web; `flutter emulators` shows available `fractal_test`; `emulator -accel-check` says KVM usable; configured Hermes home presence is INFO only, not provider-smoke proof.
  progress (2026-07-03, durable reconnect unit-path recheck after receipt refresh): reran the durable reconnect issuance/key/readiness unit contracts after Android key readiness and closeout receipt refreshes.
  validation (durable reconnect unit-path recheck after receipt refresh): `flutter test test/core/session/reconnect test/core/session/credentials test/core/gateway/capabilities/durable_reconnect_readiness_contract_test.dart test/core/session/readiness/reconnect_readiness_test.dart` passed (23 tests). This remains Dart/unit contract coverage only; real Android + real Gormes restart reconnect is still unproven.
  progress (2026-07-03, durable reconnect fake-gateway recheck after receipt refresh): reran the two targeted fake-gateway durable reconnect fallback/provider startup tests called out by the readiness audit.
  validation (durable reconnect fake-gateway recheck after receipt refresh): `flutter test test/core/channel/gateway/runtime/channel_test.dart --plain-name 'falls back to device-bearer reconnect when stream reconnect exhausts'` passed; `flutter test test/core/channel/gateway/runtime/channel_test.dart --plain-name 'provider reconnects automatically from saved durable gateway'` passed. This remains fake-gateway coverage only; real Android + real Gormes restart reconnect is still unproven.
  progress (2026-07-03, helper syntax sweep after receipt-refresh rechecks): reran shell and JS syntax checks after the latest readiness-audit, Android/web/Linux build, workflow, and reconnect receipt refreshes.
  validation (helper syntax sweep after receipt-refresh rechecks): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs`; `git diff --check` all passed.
  progress (2026-07-03, readiness audit durable reconnect receipt detail): updated `docs/runbooks/hermes-readiness-audit.md` to record the 23-test durable reconnect unit/readiness recheck and exact fake-gateway reconnect `--plain-name` receipts while preserving that real Android + real Gormes restart reconnect remains unproven.
  validation (readiness audit durable reconnect receipt detail): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after durable reconnect receipt detail): reran the full tooling/documentation contract suite after adding exact durable reconnect receipt detail to the readiness audit.
  validation (full tooling regression after durable reconnect receipt detail): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after durable reconnect receipt detail): reran the fail-closed readiness audit after adding exact durable reconnect receipt detail and rerunning full tooling contracts.
  validation (strict readiness audit after durable reconnect receipt detail): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, readiness audit completion checklist): added a current completion-audit verdict to `docs/runbooks/hermes-readiness-audit.md` mapping each explicit objective item to concrete artifacts/evidence and a partial/blocked/deferred verdict, with a reminder not to promote proxy evidence (tests, APK hashes, configured Hermes home, workflow YAML, dispatch-only output) to completion.
  validation (readiness audit completion checklist): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after completion checklist): reran the full tooling/documentation contract suite after adding the explicit completion-audit verdict to the readiness audit.
  validation (full tooling regression after completion checklist): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after completion checklist): reran the fail-closed readiness audit after adding the explicit completion-audit verdict and rerunning full tooling contracts.
  validation (strict readiness audit after completion checklist): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, helper completion verdict guard): updated `scripts/audit_hermes_readiness.sh` to print `Completion verdict: NOT COMPLETE` when blockers remain and to explicitly warn not to promote proxy evidence (tests, APK hashes, configured Hermes home, workflow YAML, or dispatch-only output) to completion.
  validation (helper completion verdict guard): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected with 15 blockers and 0 warning state; read-only audit tail printed the `NOT COMPLETE` verdict; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after helper completion verdict guard): reran the full tooling/documentation contract suite after adding the helper-level `NOT COMPLETE` verdict and proxy-evidence warning.
  validation (full tooling regression after helper completion verdict guard): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, testing plan completion-audit guard): updated `docs/product/testing-plan.md` so the current Hermes smoke matrix points strict readiness audit users at the helper-level `Completion verdict: NOT COMPLETE` output and proxy-evidence warning while blockers remain.
  validation (testing plan completion-audit guard): `dart format test/tooling/testing_plan_contract_test.dart`; `flutter test test/tooling/testing_plan_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, platform smoke completion-audit guard): updated `docs/runbooks/hermes-platform-smoke.md` to require strict readiness audit before any platform completion claim and to preserve the `Completion verdict: NOT COMPLETE`/proxy-evidence warning while blockers remain.
  validation (platform smoke completion-audit guard): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, README completion-audit guard): updated `README.md` to point completion checks at strict readiness audit and to preserve the `Completion verdict: NOT COMPLETE`/proxy-evidence warning while blockers remain.
  validation (README completion-audit guard): `dart format test/tooling/readme_assets_contract_test.dart`; `flutter test test/tooling/readme_assets_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, docs index completion-audit guard): updated `docs/README.md` so the runbooks index also points completion checks at strict readiness audit and preserves the `Completion verdict: NOT COMPLETE`/proxy-evidence warning while blockers remain.
  validation (docs index completion-audit guard): `dart format test/tooling/runbooks/topology/runbooks_topology_contract_test.dart`; `flutter test test/tooling/runbooks/topology/runbooks_topology_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after completion-audit doc guard expansion): reran the full tooling/documentation contract suite after adding the completion-audit guard to README, docs index, testing plan, platform smoke runbook, readiness audit doc, and readiness audit helper.
  validation (full tooling regression after completion-audit doc guard expansion): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, GOAL_STATE completion-audit guard): updated `GOAL_STATE.md` to record that strict readiness audit now prints `Completion verdict: NOT COMPLETE` and warns not to promote proxy evidence (tests, APK hashes, configured Hermes home, workflow YAML, dispatch-only output) while blockers remain.
  validation (GOAL_STATE completion-audit guard): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after GOAL_STATE completion-audit guard): reran the full tooling/documentation contract suite after adding the completion-audit guard to GOAL_STATE.
  validation (full tooling regression after GOAL_STATE completion-audit guard): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, native-host blocker reprobe after completion-guard expansion): directly reprobed native host build commands on this Linux host before treating Windows/iOS receipts as available.
  validation (native-host blocker reprobe after completion-guard expansion): `flutter build windows --debug` exited 1 with `"build windows" only supported on Windows hosts.`; `flutter build ios --simulator --debug` exited 64 with `Could not find an option named "--simulator".` No Windows/iOS/native-host receipt was produced.
  progress (2026-07-03, strict readiness audit after completion-guard expansion): reran the fail-closed readiness audit after adding the completion-audit guard to README, docs index, testing plan, platform smoke runbook, readiness audit doc/helper, and GOAL_STATE.
  validation (strict readiness audit after completion-guard expansion): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers and 0 warning state.
  progress (2026-07-03, readiness audit native-host reprobe detail): updated `docs/runbooks/hermes-readiness-audit.md` to record exact Windows/iOS local reprobe failures (`flutter build windows --debug` exit 1; `flutter build ios --simulator --debug` exit 64) while preserving that native-host receipts still require Windows/macOS runners or visible workflow receipts.
  validation (readiness audit native-host reprobe detail): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, platform smoke native-host reprobe detail): updated `docs/runbooks/hermes-platform-smoke.md` to record the same exact Windows/iOS local reprobe failures and preserve the native-runner boundary.
  validation (platform smoke native-host reprobe detail): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, GOAL_STATE native-host reprobe detail): updated `GOAL_STATE.md` to include the exact Windows/iOS Linux-host reprobe failures alongside the strict audit and workflow visibility blockers.
  validation (GOAL_STATE native-host reprobe detail): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after native-host reprobe detail): reran the full tooling/documentation contract suite after adding exact native-host reprobe failures to readiness audit, platform smoke runbook, and GOAL_STATE.
  validation (full tooling regression after native-host reprobe detail): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, full Dart regression after completion/native-host guard refresh): reran the full Flutter suite after the completion-audit guard expansion and exact native-host reprobe details were added across docs/state/contracts.
  validation (full Dart regression after completion/native-host guard refresh): `flutter test --concurrency=1` passed (1016 tests).
  progress (2026-07-03, analyzer recheck after completion/native-host guard refresh): reran static analysis after the completion-audit guard expansion and exact native-host reprobe details were added across docs/state/contracts.
  validation (analyzer recheck after completion/native-host guard refresh): `flutter analyze` passed with no issues.
  progress (2026-07-03, strict readiness audit after analyzer/native-host detail refresh): reran the fail-closed readiness audit after analyzer and native-host reprobe detail refreshes.
  validation (strict readiness audit after analyzer/native-host detail refresh): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning.
  progress (2026-07-03, Android live-mic completion-audit guard): updated `docs/runbooks/android/live-mic-smoke.md` so even after recording a real spoken Android receipt, operators must run strict readiness audit and must not promote that Android receipt, tests, APK hashes, configured Hermes home, workflow YAML, or dispatch-only output to whole-goal completion while unrelated blockers remain.
  validation (Android live-mic completion-audit guard): `dart format test/tooling/runbooks/android/live_mic_smoke_contract_test.dart`; `flutter test test/tooling/runbooks/android/live_mic_smoke_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, durable reconnect completion-audit guard): updated `docs/runbooks/android/durable-keystore-smoke.md` so even after recording a real Android + Gormes reconnect receipt, operators must run strict readiness audit and must not promote that reconnect receipt, key smoke, tests, APK hashes, configured Hermes home, workflow YAML, or dispatch-only output to whole-goal completion while unrelated blockers remain.
  validation (durable reconnect completion-audit guard): `dart format test/tooling/runbooks/android/pairing_and_keystore_contract_test.dart`; `flutter test test/tooling/runbooks/android/pairing_and_keystore_contract_test.dart` passed (3 tests); `git diff --check` passed.
  progress (2026-07-03, full tooling regression after Android closeout completion guards): reran the full tooling/documentation contract suite after adding strict-audit completion guards to the Android live-mic and durable reconnect runbooks.
  validation (full tooling regression after Android closeout completion guards): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, GOAL_STATE Android closeout completion guards): updated `GOAL_STATE.md` to record that Android live-mic and durable reconnect runbooks now require strict readiness audit after future real Android receipts and warn not to promote a single Android/reconnect receipt or proxy evidence to whole-goal completion while unrelated blockers remain.
  validation (GOAL_STATE Android closeout completion guards): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, Hermes smoke helper completion guards): updated `scripts/run_live_hermes_smoke.sh` and `scripts/run_provider_hermes_smoke.sh` to print that their receipts are not whole-goal completion evidence by themselves and to point operators at strict readiness audit before completion claims.
  validation (Hermes smoke helper completion guards): `dart format test/tooling/package_scripts_contract_test.dart`; `bash -n scripts/run_provider_hermes_smoke.sh scripts/run_live_hermes_smoke.sh`; `flutter test test/tooling/package_scripts_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, installed-Hermes live smoke after helper completion guard): reran installed-Hermes live API connect/session smoke after adding the helper-level whole-goal completion caveat.
  validation (installed-Hermes live smoke after helper completion guard): `npm run hermes:live-smoke` passed (Playwright pass marker plus non-provider/non-mic and not-whole-goal-completion caveat output).
  progress (2026-07-03, provider smoke after helper completion guard): reran configured local Hermes provider smoke after adding the helper-level whole-goal completion caveat.
  validation (provider smoke after helper completion guard): `npm run hermes:provider-smoke:local` passed (Playwright pass marker plus non-physical-mic/server-audio and not-whole-goal-completion caveat output).
  progress (2026-07-03, platform smoke helper-caveat alignment): updated `docs/runbooks/hermes-platform-smoke.md` so installed-Hermes live smoke and provider transcript-voice smoke sections also state these receipts are not whole-goal completion evidence by themselves and point back to strict readiness audit.
  validation (platform smoke helper-caveat alignment): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after smoke-helper completion guards): reran the full tooling/documentation contract suite after adding helper-level and runbook-level completion caveats for live/provider smoke receipts.
  validation (full tooling regression after smoke-helper completion guards): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after smoke-helper completion guards): reran the fail-closed readiness audit after adding helper-level/runbook-level completion caveats and rerunning live/provider smokes.
  validation (strict readiness audit after smoke-helper completion guards): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning.
  progress (2026-07-03, GOAL_STATE smoke-helper completion caveats): updated `GOAL_STATE.md` to record that installed-Hermes live smoke and configured provider smoke still pass with both non-mic/server-audio caveats and not-whole-goal-completion caveats.
  validation (GOAL_STATE smoke-helper completion caveats): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after GOAL_STATE smoke-helper caveats): reran the full tooling/documentation contract suite after recording the live/provider not-whole-goal-completion caveats in GOAL_STATE.
  validation (full tooling regression after GOAL_STATE smoke-helper caveats): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, helper syntax sweep after completion-caveat guards): reran shell and JS syntax checks after adding completion-caveat guards to readiness, Android closeout, live/provider smoke, platform-smoke, and GOAL_STATE paths.
  validation (helper syntax sweep after completion-caveat guards): `bash -n scripts/audit_hermes_readiness.sh scripts/prepare_android_live_mic_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_voice_smoke.sh scripts/run_hermes_platform_workflow.sh scripts/run_linux_release_build.sh scripts/run_live_hermes_smoke.sh scripts/run_local_configured_hermes_provider_smoke.sh scripts/run_provider_hermes_smoke.sh`; `node --check serve_web.mjs`; `node --check playwright/tests/regression/hermes-smoke.spec.mjs`; `node --check playwright/tests/regression/hermes-live-api.spec.mjs`; `node --check playwright/tests/regression/hermes-provider-chat.spec.mjs`; `git diff --check` all passed.
  progress (2026-07-03, readiness audit smoke-helper caveat alignment): updated `docs/runbooks/hermes-readiness-audit.md` so the installed-Hermes live smoke and provider-backed smoke evidence rows also record their helper-level not-whole-goal-completion caveats and strict-readiness-audit pointers.
  validation (readiness audit smoke-helper caveat alignment): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after readiness smoke-helper caveat alignment): reran the full tooling/documentation contract suite after aligning readiness audit live/provider smoke evidence rows with helper-level completion caveats.
  validation (full tooling regression after readiness smoke-helper caveat alignment): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, Android helper completion guards): updated Android voice-readiness, deterministic voice-loop, durable-key, and live-mic-prep helpers to print that their receipts/prep are not whole-goal completion evidence by themselves and to point operators at strict readiness audit before completion claims.
  validation (Android helper completion guards): `dart format test/tooling/package_scripts_contract_test.dart`; `bash -n scripts/run_android_voice_smoke.sh scripts/run_android_hermes_voice_loop_smoke.sh scripts/run_android_durable_key_smoke.sh scripts/prepare_android_live_mic_smoke.sh`; `flutter test test/tooling/package_scripts_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after Android helper completion guards): reran the full tooling/documentation contract suite after adding not-whole-goal-completion caveats to Android voice-readiness, deterministic voice-loop, durable-key, and live-mic-prep helpers.
  validation (full tooling regression after Android helper completion guards): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after Android helper completion guards): reran the fail-closed readiness audit after adding Android helper completion caveats and rerunning tooling contracts.
  validation (strict readiness audit after Android helper completion guards): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning; `git diff --check` passed.
  progress (2026-07-03, readiness audit Android-helper caveat alignment): updated `docs/runbooks/hermes-readiness-audit.md` so Android speech/prep, deterministic voice-loop, and durable-key evidence rows also record their helper-level not-whole-goal-completion caveats and strict-readiness-audit pointers.
  validation (readiness audit Android-helper caveat alignment): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after readiness Android-helper caveat alignment): reran the full tooling/documentation contract suite after aligning readiness audit Android evidence rows with helper-level completion caveats.
  validation (full tooling regression after readiness Android-helper caveat alignment): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, platform smoke Android-helper caveat alignment): updated `docs/runbooks/hermes-platform-smoke.md` so Android voice-readiness, deterministic voice-loop, live-mic-prep, and durable-key sections also state their receipts/prep are not whole-goal completion evidence by themselves and point back to strict readiness audit.
  validation (platform smoke Android-helper caveat alignment): `dart format test/tooling/hermes_platform_smoke_runbook_contract_test.dart`; `flutter test test/tooling/hermes_platform_smoke_runbook_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after platform Android-helper caveat alignment): reran the full tooling/documentation contract suite after aligning platform smoke Android sections with helper-level completion caveats.
  validation (full tooling regression after platform Android-helper caveat alignment): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after platform Android-helper caveat alignment): reran the fail-closed readiness audit after aligning platform smoke Android sections with helper-level completion caveats.
  validation (strict readiness audit after platform Android-helper caveat alignment): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning.
  progress (2026-07-03, README Android-helper caveat alignment): updated `README.md` so Android helper commands also state their receipts are readiness/deterministic/key-storage evidence only, not whole-goal completion evidence by themselves, and point to strict readiness audit before completion claims.
  validation (README Android-helper caveat alignment): `dart format test/tooling/readme_assets_contract_test.dart`; `flutter test test/tooling/readme_assets_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after README Android-helper caveat alignment): reran the full tooling/documentation contract suite after aligning README Android helper caveats with helper-level completion caveats.
  validation (full tooling regression after README Android-helper caveat alignment): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, testing plan Android-helper caveat alignment): updated `docs/product/testing-plan.md` so Android readiness/prep and durable-key rows also state these receipts are not whole-goal completion evidence by themselves and point to strict readiness audit before completion claims.
  validation (testing plan Android-helper caveat alignment): `dart format test/tooling/testing_plan_contract_test.dart`; `flutter test test/tooling/testing_plan_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after testing plan Android-helper caveat alignment): reran the full tooling/documentation contract suite after aligning the testing plan Android helper rows with helper-level completion caveats.
  validation (full tooling regression after testing plan Android-helper caveat alignment): `flutter test test/tooling` passed (27 tests).
  progress (2026-07-03, strict readiness audit after testing plan Android-helper caveat alignment): reran the fail-closed readiness audit after aligning the testing plan Android helper rows with helper-level completion caveats.
  validation (strict readiness audit after testing plan Android-helper caveat alignment): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning.
  progress (2026-07-03, GOAL_STATE Android-helper caveat alignment): updated `GOAL_STATE.md` so Android live-mic/durable runbooks and Android helper scripts are all recorded as pointing to strict readiness audit and not as whole-goal completion evidence by themselves.
  validation (GOAL_STATE Android-helper caveat alignment): `dart format test/tooling/goal_state_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after GOAL_STATE Android-helper caveat alignment): reran the full tooling/documentation contract suite after recording Android helper completion caveats in GOAL_STATE.
  validation (full tooling regression after GOAL_STATE Android-helper caveat alignment): `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, README live-mic-prep caveat tightening): updated `README.md` so the `npm run android:live-mic-prep` closeout snippet explicitly says the helper only installs/launches/grants microphone permission, is not whole-goal completion evidence by itself, and still requires the spoken microphone checklist plus strict readiness audit before completion claims.
  validation (README live-mic-prep caveat tightening): `dart format test/tooling/readme_assets_contract_test.dart`; `flutter test test/tooling/readme_assets_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after README live-mic-prep caveat tightening): reran the full tooling/documentation contract suite after tightening README live-mic-prep caveats.
  validation (full tooling regression after README live-mic-prep caveat tightening): `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after README live-mic-prep caveat tightening): reran the fail-closed readiness audit after tightening README live-mic-prep caveats.
  validation (strict readiness audit after README live-mic-prep caveat tightening): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 15 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning.
  progress (2026-07-03, explicit provider smoke blocker): updated `scripts/audit_hermes_readiness.sh` so configured Hermes home presence no longer leaves provider readiness implicit; the audit now always blocks on the missing full live provider-backed Hermes chat/voice smoke receipt and names `npm run hermes:provider-smoke:local` plus configured model/provider credentials as the closeout path.
  validation (explicit provider smoke blocker): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 16 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning; `git diff --check` passed.
  progress (2026-07-03, explicit provider blocker docs/state alignment): updated `docs/runbooks/hermes-readiness-audit.md` current blocker detail and `GOAL_STATE.md` so the new 16-blocker fail-closed state includes the explicit full live provider-backed chat/voice smoke closeout blocker, configured model/provider credentials, and deterministic-transcript-not-mic/server-audio boundary.
  validation (explicit provider blocker docs/state alignment): `dart format test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart`; `flutter test test/tooling/goal_state_contract_test.dart test/tooling/hermes_readiness_audit_contract_test.dart` passed; `git diff --check` passed.
  progress (2026-07-03, full tooling regression after explicit provider blocker docs/state alignment): reran the full tooling/documentation contract suite after aligning docs/state with the explicit 16-blocker provider smoke closeout.
  validation (full tooling regression after explicit provider blocker docs/state alignment): `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, strict readiness audit after explicit provider blocker docs/state alignment): reran the fail-closed readiness audit after aligning docs/state with the explicit provider smoke closeout blocker.
  validation (strict readiness audit after explicit provider blocker docs/state alignment): `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 16 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the proxy-evidence warning.
  progress (2026-07-03, readiness audit objective checklist): updated `scripts/audit_hermes_readiness.sh` to print a read-only objective checklist that maps provider credentials/smoke, Android mic loop, native-host builds, realtime audio, deferred Hermes surfaces, multi-endpoint management, and legacy Gormes reconnect to the exact required closeout evidence without treating the checklist as completion evidence.
  validation (readiness audit objective checklist): `dart format test/tooling/hermes_readiness_audit_contract_test.dart`; `bash -n scripts/audit_hermes_readiness.sh`; `flutter test test/tooling/hermes_readiness_audit_contract_test.dart` passed; `NAVIVOX_FAIL_ON_BLOCKERS=1 npm run hermes:readiness-audit` exited 3 as expected, reporting 16 blockers, 0 warning state, `Completion verdict: NOT COMPLETE`, and the objective checklist; `git diff --check` passed.
  progress (2026-07-03, delivery docs after push blocker): committed the Hermes companion surface and readiness guardrails locally as `c6c7e61` and `d1ee413`, then documented that `git push` is blocked because GitHub rejected `.github/workflows/hermes-platform-smoke.yml` without OAuth `workflow` scope, leaving the workflow unpublished remotely and native-host receipts unavailable.
  validation (delivery docs after push blocker): `git status --short --branch` showed `main...origin/main [ahead 2]`; push output reported `refusing to allow an OAuth App to create or update workflow .github/workflows/hermes-platform-smoke.yml without workflow scope`; docs contracts updated to preserve this blocker.
  progress (2026-07-03, README/docs index push-scope blocker): updated root `README.md` and `docs/README.md` so operator entry points also state the workflow YAML exists locally but remains unpublished because the latest push was rejected without GitHub `workflow` scope; successful `gh run view` jobs/artifacts are still required before native-host readiness claims.
  validation (README/docs index push-scope blocker): `dart format test/tooling/readme_assets_contract_test.dart`; `flutter test test/tooling/readme_assets_contract_test.dart` passed; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, testing-plan push-scope blocker): updated `docs/product/testing-plan.md` native-host row so the current smoke matrix also says local workflow YAML is not a receipt while the latest push is rejected without GitHub `workflow` scope, and successful `gh run view` jobs/artifacts remain required.
  validation (testing-plan push-scope blocker): `dart format test/tooling/testing_plan_contract_test.dart`; `flutter test test/tooling/testing_plan_contract_test.dart` passed; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, product Hermes docs status refresh): updated `docs/product/hermes-agent-interface-plan.md` and `docs/product/hermes-desktop-reference.md` so product docs now describe the implemented native Hermes channel/UI source paths, preserve Gormes-first guidance as historical/legacy only, and repeat the local-commits-ahead plus workflow-scope push blocker for native-host readiness.
  validation (product Hermes docs status refresh): `rg -n "Hermes implementation lives|Delivery note:|Historical Gormes-First|workflow scope|Hermes Agent interface plan" docs/product/hermes-agent-interface-plan.md docs/product/hermes-desktop-reference.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, architecture Hermes addendum): updated `docs/architecture/architecture.md` status and high-level architecture to distinguish the active native Hermes `/hermes` path from the preserved legacy Gormes path, with source citations for `HermesApiChannel`, `HermesChatScreen`, surface readiness, and bounded diagnostics.
  validation (architecture Hermes addendum): `rg -n "active Hermes-first addendum|Hermes Agent API server|hermesSurfaceReadiness|Voice remains local device STT" docs/architecture/architecture.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, data-model Hermes addendum): updated `docs/architecture/data-model.md` status and model principles to distinguish active Hermes Agent authority from preserved legacy Gormes authority, added a Hermes endpoint/session model table for `HermesApiConfig`, `HermesSession`, `HermesChatTurn`, `HermesChannelState`, `HermesJob`, and `HermesSurfaceReadiness`, and clarified secure endpoint storage, bounded diagnostics, local-STT voice turns, and server-audio deferral.
  validation (data-model Hermes addendum): `rg -n "historical Gormes data model|Hermes Endpoint And Session Models|HermesSurfaceReadiness|server realtime|^## [0-9]+\." docs/architecture/data-model.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, routes Hermes addendum): updated `docs/product/routes.md` status, route constants, route table, router excerpt, guards, and navigation patterns so `/hermes` is documented as the fresh-install native Hermes entry point that bypasses legacy Gormes setup while `/setup` and `/chats` remain preserved Gormes paths.
  validation (routes Hermes addendum): `rg -n 'Hermes setup to chat|AppRoutes\.hermes|HermesChatScreen|native Hermes flow|hasGatewayProvider|historical Gormes route plan' docs/product/routes.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, PRD Hermes addendum): updated `docs/product/prd.md` status, summary, goals, and non-goals so it now distinguishes the active Hermes Agent companion product from the preserved legacy Gormes PRD, names Hermes API endpoints, documents local-STT-only voice, deferred Hermes surfaces, and the workflow-scope blocker for native-host readiness.
  validation (PRD Hermes addendum): `rg -n "historical Gormes PRD|Hermes Agent API server|Hermes realtime/server audio|workflow.*scope|hermesSurfaceReadiness|HermesApiChannel" docs/product/prd.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, UI design Hermes addendum): updated `docs/product/ui-design.md` status, principles, launch routing, and primary-screen guidance so it documents `/hermes` as the fresh-install native Hermes UI, names setup/session/voice/diagnostics surfaces, and preserves Gormes UI guidance as legacy.
  validation (UI design Hermes addendum): `rg -n "historical Gormes UI guide|Active Hermes Screen|HermesChatScreen|hermesSurfaceReadiness|Fresh install.*\/hermes|Hermes realtime/server audio" docs/product/ui-design.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, context glossary Hermes boundary refresh): updated `CONTEXT.md` so glossary language now treats pairing handoff as legacy Gormes-only, clarifies current Hermes voice runs as local STT text turns without server-audio evidence, keeps Hermes API keys out of diagnostics exports, marks deferred Hermes surfaces as not implemented, and resolves the `Hermes app` ambiguity toward Hermes endpoint/session language while preserving Gormes terms only for legacy paths.
  validation (context glossary Hermes boundary refresh): `rg -n "Hermes login|server audio receipt|multi-endpoint management can follow later|diagnostics exports|deferred/read-only|Hermes app" CONTEXT.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, decision record Hermes amendments): updated `docs/architecture/decision-record.md` so the decision surface now records ADR 0006/0007 as active Hermes-first amendments, cites the implemented Hermes channel/UI/readiness source paths, names `/hermes` as the fresh-install activation route, and preserves the original Gormes decisions for legacy paths only.
  validation (decision record Hermes amendments): `rg -n 'historical Gormes baseline|fresh-install first useful screen is `/hermes`|Hermes realtime/server audio is not implemented|multi-endpoint management|HermesApiChannel' docs/architecture/decision-record.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, ADR implementation status refresh): updated `docs/adr/0006-hermes-agent-first-runtime.md` and `docs/adr/0007-native-hermes-channel-not-navivox-channel-adapter.md` with 2026-07-03 implementation status: native Hermes channel/UI exists, deferred surfaces remain guarded by surface readiness, local workflow publishing is blocked by missing GitHub `workflow` scope, and `GatewayNavivoxChannel` remains preserved legacy rather than a Hermes adapter.
  validation (ADR implementation status refresh): `rg -n "Implementation status|workflow.*scope|HermesApiChannel|not a Hermes adapter|surface-readiness" docs/adr/0006-hermes-agent-first-runtime.md docs/adr/0007-native-hermes-channel-not-navivox-channel-adapter.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, chat UI research Hermes addendum): updated `docs/research/chat-ui-research.md` so the research now identifies `/hermes` and `HermesChatScreen` as the active mainline chat surface, keeps package adoption as rendering-only over `HermesChannel` state, and splits Telegram-pattern translation into active Hermes vs preserved Gormes guidance.
  validation (chat UI research Hermes addendum): `rg -n "historical Gormes chat UI research|Active Hermes-first update|HermesChatScreen|Active Hermes translation|server realtime audio deferred|multi-endpoint/profile management deferred" docs/research/chat-ui-research.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, library research Hermes addendum): updated `docs/research/library-research.md` so dependency research now matches the root `pubspec.yaml`, distinguishes active Hermes HTTP/SSE IO/web transports from preserved Gormes WebSocket transport, records local widgets over `HermesChannel` state as the current chat UI choice, and notes secure storage/shared-preferences roles for Hermes endpoint credentials.
  validation (library research Hermes addendum): `rg -n "historical Gormes library research|HermesApiClient|Browser/web support for Hermes|HermesChatScreen|Secure storage has landed|speech_to_text" docs/research/library-research.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, analyst summary Hermes status note): updated `docs/research/analyst-summary.md` to mark the 2026-05 Playwright report as historical Gormes evidence and point current browser/readiness status to the Hermes platform smoke and readiness audit runbooks, including the 68-test focused Playwright receipt and workflow-scope blocker.
  validation (analyst summary Hermes status note): `rg -n "historical 2026-05 Gormes|68 Chromium tests|workflow.*scope|Hermes platform smoke checklist" docs/research/analyst-summary.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, analyst summary historical blocker wording): updated `docs/research/analyst-summary.md` so the old 5-blocker Playwright section and "goal remains active" language are explicitly historical 2026-05 Gormes status and not current Hermes readiness status.
  validation (analyst summary historical blocker wording): `rg -n "Historical Blocker Specs|Historical Status & Next|not the current Hermes readiness status|runbooks linked at the top" docs/research/analyst-summary.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, superseded Gormes ADR refresh): updated `docs/adr/0005-gormes-first-navivox-with-hermes-desktop-as-reference.md` so the superseded ADR now points to both ADR 0006 and ADR 0007, records the old Gormes-first decision as historical, and clarifies that `/hermes` is the current mainline companion route while Gormes-first guidance remains legacy context only.
  validation (superseded Gormes ADR refresh): `rg -n "superseded by \\[ADR 0006|Historical decision|fresh-install Hermes Agent companion route|legacy Gormes paths" docs/adr/0005-gormes-first-navivox-with-hermes-desktop-as-reference.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, architecture package layout refresh): updated `docs/architecture/architecture.md` package layout and responsibilities so the active `core/hermes/` and `features/hermes_chat/` modules are represented alongside preserved legacy gateway modules, including Hermes API/client/channel responsibilities and endpoint credential boundaries.
  validation (architecture package layout refresh): `rg -n "core/hermes|features/hermes_chat|Hermes API client and channel|HermesApiConfig|HermesApiChannel|Legacy Gateway Client|Legacy Gateway Channel" docs/architecture/architecture.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, architecture credential storage refresh): updated `docs/architecture/architecture.md` to remove stale "secure storage once added" wording and document that issued durable Gormes device credentials use `SecureStorageDurableCredentialStore`/platform durable key stores while bootstrap tokens remain memory-only and secrets stay out of routes/diagnostics.
  validation (architecture credential storage refresh): `rg -n "Bootstrap tokens remain in memory|SecureStorageDurableCredentialStore|platform durable key stores|never embedded in route paths" docs/architecture/architecture.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, superpowers historical status banners): updated `docs/superpowers/plans/2026-05-20-transcript-surface-deepening.md`, `docs/superpowers/plans/2026-05-20-transcript-surface-task1-subagent-packet.md`, `docs/superpowers/plans/2026-05-21-voice-run-lifecycle.md`, `docs/superpowers/plans/2026-06-17-connect-screen-simplification.md`, `docs/superpowers/specs/2026-05-20-voice-run-lifecycle-design.md`, `docs/superpowers/specs/2026-06-16-voice-run-active-vs-latest-design.md`, and `docs/superpowers/specs/2026-06-17-connect-screen-simplification-design.md` so old Gormes plans/specs are explicitly historical and point current Hermes chat/voice/setup readiness to the active product/runbook docs.
  validation (superpowers historical status banners): `rg -n 'historical 2026-05 Gormes UI|historical 2026-05 Gormes voice-run|historical 2026-06 Gormes connect-screen|historical approved Gormes design|fresh-install companion path is `/hermes`|server/realtime audio remains deferred' docs/superpowers`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, superpowers historical path normalization): updated the historical transcript/voice superpowers plans to remove stale `/home/xel/git/sages-openclaw/workspace-mineru/navivox-app/app` paths and malformed parent-repo commands, replacing them with the current repo root `/home/xel/git/gormes/navivox-app`, project-relative paths, valid `git diff --check`, and `git add .` examples.
  validation (superpowers historical path normalization): `rg -n "sages-openclaw|workspace-mineru|/home/xel/git/sages|app/lib|app/test|git diff --check --($|/)|^git add$" docs/superpowers/plans/2026-05-20-transcript-surface-deepening.md docs/superpowers/plans/2026-05-20-transcript-surface-task1-subagent-packet.md docs/superpowers/plans/2026-05-21-voice-run-lifecycle.md || true` showed 0 matches; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, Playwright README Hermes coverage refresh): updated `playwright/README.md` from the old 83-test Gormes-only summary to the current 92 listed Playwright tests, documenting deterministic Hermes fake browser smoke, env-gated live/provider Hermes specs, preserved Gormes regression coverage, and the fact that browser receipts are not full platform readiness evidence.
  validation (Playwright README Hermes coverage refresh): `rg -n "92 listed Playwright tests|Hermes fake browser smoke|NAVIVOX_PROVIDER_HERMES_URL|not platform-readiness|E2EMockChannel|HermesApiChannel web transport" playwright/README.md`; `npx playwright test --config=playwright.config.mjs --list` reported `Total: 92 tests in 5 files`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, superseded ADR option wording): updated `docs/adr/0005-gormes-first-navivox-with-hermes-desktop-as-reference.md` considered-options bullets so the old rejection of Hermes-first is explicitly “at the time,” notes ADR 0006 later reversed the product direction, and marks the accepted Gormes-first choice as historical context only.
  validation (superseded ADR option wording): `rg -n "rejected at the time|ADR 0006 later reversed|historical context only" docs/adr/0005-gormes-first-navivox-with-hermes-desktop-as-reference.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, ADR 0001-0004 status refresh): updated early ADRs with current status boundaries: durable credentials are preserved legacy Gormes and still blocked on real Android + real Gormes durable reconnect, Run record evidence references govern legacy Gormes while Hermes run/tool progress uses `HermesApiChannel`, navigation intent remains active with current module paths, and Playwright fixture-gateway evidence is extended by Hermes fake/live/provider browser smokes but still not a readiness-completion substitute.
  validation (ADR 0001-0004 status refresh): `rg -n "Status: preserved legacy Gormes decision|full real Android \\+ real Gormes durable reconnect remains|HermesApiChannel|Status: active implementation decision|implementation lives under|Status: preserved legacy Gormes browser-evidence decision|fake Hermes" docs/adr/0001-durable-navivox-connection-credentials.md docs/adr/0002-run-record-inspection-evidence-references.md docs/adr/0003-navigation-intent-module.md docs/adr/0004-playwright-fixture-gateway-e2e.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, docs index coverage refresh): updated `docs/README.md` to state that `main` is Hermes Agent-first while Gormes/early ADR pages are legacy or historical unless explicitly active, and added links to the current Playwright E2E guide plus historical superpowers plans/specs.
  validation (docs index coverage refresh): `rg -n "Hermes Agent-first|legacy/historical context|Testing And Historical Implementation Notes|Playwright E2E guide|Superpowers plans/specs" docs/README.md`; local Markdown link audit across root/docs/playwright found `markdown local links ok`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, ADR 0007 implementation wording refresh): updated `docs/adr/0007-native-hermes-channel-not-navivox-channel-adapter.md` to mark ADR 0007 implemented, replace stale "name TBD" / "once run transport lands" language with concrete `HermesChannel` and capability-gated approvals/stop wording, and record `HermesVoiceRunController` as the Hermes voice controller instead of future work.
  validation (ADR 0007 implementation wording refresh): `rg -n 'implemented on 2026-07-03|capability-gated run transport|HermesVoiceRunController|plan doc has been updated' docs/adr/0007-native-hermes-channel-not-navivox-channel-adapter.md`; `rg -n 'name TBD|once run transport lands|will be updated|VoiceRunController.*NavivoxChannel-typed today' docs/adr/0007-native-hermes-channel-not-navivox-channel-adapter.md || true` showed 0 stale matches; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, platform smoke browser receipt inline): updated `docs/runbooks/hermes-platform-smoke.md` to inline the focused 68-test Playwright browser receipt command instead of pointing readers to `TODO.md`, and kept the broad `npm run web:e2e` wrapper timeout caveat.
  validation (platform smoke browser receipt inline): `rg -n "68 Chromium tests|npx playwright test --config=playwright.config.mjs playwright/tests/regression/navivox-e2e.spec.mjs playwright/tests/regression/hermes-smoke.spec.mjs --reporter=list|wrapper timeout is" docs/runbooks/hermes-platform-smoke.md`; `rg -n 'command recorded in `TODO.md`' docs/runbooks/hermes-platform-smoke.md || true` showed 0 matches; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, testing-plan path cleanup): updated `docs/product/testing-plan.md` to remove stale `flutter-navivox/app`, parent Gormes Go command, nonexistent `test/fixtures/...`, and obsolete gateway test path references; the historical Gormes plan now points current browser/build/docs commands at this repo and labels fixture names as historical guidance rather than current inventory.
  validation (testing-plan path cleanup): `rg -n "Current coverage.*gateway|Historical fixture guidance|fixture paths are not current repo inventory|flutter build web --release -t lib/main_e2e.dart|flutter test test/tooling" docs/product/testing-plan.md`; `rg -n "flutter-navivox/app|test/fixtures/navivox|go run ./cmd/progress|go test ./cmd/gormes|test/core/gateway/navivox_gateway_client_test.dart|test/core/channel/gateway_navivox_channel_test.dart" docs/product/testing-plan.md || true` showed 0 stale matches; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, analyst summary coverage wording): updated `docs/research/analyst-summary.md` so the old 55-check Playwright count and screenshot inventory are explicitly historical, and corrected the screenshot spec path to `playwright/tests/screenshots/e2e-screenshots.spec.mjs`.
  validation (analyst summary coverage wording): `rg -n "Historical E2E Test Counts|playwright/tests/screenshots/e2e-screenshots.spec.mjs|Historical valid coverage|Historical Screenshot Inventory" docs/research/analyst-summary.md`; `rg -n "Total valid coverage = 55|playwright/tests/e2e-screenshots.spec.mjs" docs/research/analyst-summary.md || true` showed 0 stale matches; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, voice/durable deferred wording cleanup): updated `docs/runbooks/hermes-platform-smoke.md`, `docs/architecture/architecture.md`, `docs/architecture/decision-record.md`, `docs/superpowers/plans/2026-05-21-voice-run-lifecycle.md`, and `docs/superpowers/specs/2026-05-20-voice-run-lifecycle-design.md` so durable reconnect and server/audio voice work are described as deferred/unproven historical scope rather than vague future work. Updated the platform smoke tooling contract accordingly.
  validation (voice/durable deferred wording cleanup): `rg -n "full real Gormes durable credential issuance plus silent reconnect remain|Historical Gormes server voice events remain deferred|Binary audio transport is deferred|Historical Gormes server voice events were deferred|voice control plane is deferred" docs/runbooks/hermes-platform-smoke.md docs/architecture/architecture.md docs/architecture/decision-record.md docs/superpowers/plans/2026-05-21-voice-run-lifecycle.md docs/superpowers/specs/2026-05-20-voice-run-lifecycle-design.md`; `flutter test test/tooling` passed (27 tests); `git diff --check` passed.
  progress (2026-07-03, Hermes Desktop parity roadmap): added `docs/product/hermes-desktop-parity-roadmap.md` and linked it from `docs/README.md` plus `docs/product/hermes-agent-interface-plan.md`. The roadmap stages Desktop-parity work through receipts/release blockers, chat hardening, mobile voice receipts, read-only surfaces, high-risk admin surfaces, multi-endpoint management, and a final parity review gate, with acceptance gates and rollback/hide behavior. Onklaud status was operational; `onklaud loop` returned FAIL (8/10) on the draft, and the usable critique was incorporated: add per-phase gates, move Android voice receipt earlier, separate admin surfaces by risk, include native-host/reconnect/raw-log/multi-endpoint gates, and add hide/rollback behavior.
  validation (Hermes Desktop parity roadmap): `rg -n "Hermes Desktop parity roadmap|Phase 0|Phase 1|Phase 2|Phase 3|Phase 4|Phase 5|Phase 6|rollback/hide|readiness-audit" docs/product/hermes-desktop-parity-roadmap.md docs/README.md docs/product/hermes-agent-interface-plan.md`; Markdown local links audit passed; `flutter test test/tooling` passed (27 tests); `git diff --check` passed. A later Onklaud final gate on the task-scoped docs failed 7/10 with generic `Contains TODO`/edge-case findings because the required progress ledger is named `TODO.md`; Pi treated that checkpoint as not source-actionable and kept local validation authoritative.
  progress (2026-07-03, SSE CR-only framing hardening): implemented the first Phase 1 chat-hardening slice from the parity roadmap by teaching `HermesSseEventDecoder` to recognize CR-only SSE frame separators and CR-only line endings while still refusing to flush incomplete final frames without a blank-line separator. Onklaud status was operational; `onklaud loop` failed on the original EOF-flush idea and flagged truncation risks, so Pi rejected that advice path and used the safer source-backed CR-only framing hardening instead.
  validation (SSE CR-only framing hardening): `flutter test test/core/hermes/hermes_api_test.dart` passed (16 tests); `flutter test test/tooling` passed (27 tests); `flutter test --concurrency=1` passed (1019 tests); `git diff --check` passed. Onklaud final gate first failed 8/10 with a generic edge-case warning, so an extra empty-chunk/no-data CR-only control-frame regression was added; rerun gate passed 10/10 with no issues.
  owner: Navivox app owner / Hermes Agent API owner
  next check: full live chat/voice smoke still requires a configured Hermes provider/model and Android target; otherwise continue deeper conversion/hiding of remaining Gormes-era internals.

[RESOLVED] Navivox-side Termux-to-Navivox pairing handoff without camera — 2026-05-24
  resolved: 2026-05-24
  result: Android now accepts `navivox://connect?...` VIEW intents and text/plain ACTION_SEND payloads, forwards initial and foreground intents into Flutter, fills the setup form through existing connect-info parsing, preserves explicit `websocket_url`, and keeps tokens out of notices/logging. Gormes still needs the Termux `am start ...` command-side launcher.

[RESOLVED] Stable Gormes gateway identity for Pairing handoff — 2026-05-25
  resolved: 2026-06-07
  result: Navivox now parses authenticated `gateway_id` from gateway status, persists non-secret saved-session metadata keyed by Gateway identity, and keeps credential key aliases based on Gateway identity instead of URLs/tokens/machine names.
  evidence: `NavivoxGatewayStatus.gatewayId` / `.hasGatewayIdentity` in `lib/core/gateway/capabilities/navivox_gateway_capabilities.dart`; session persistence stores `SessionPreferenceKeys.gatewayId`; gateway channel writes `status.gatewayId` on connect; E2E coverage in `test/e2e/gateway_identity_e2e_test.dart` proves authenticated status identity/label persistence.
  validation: `test/core/gateway/navivox_gateway_protocol_test.dart`, `test/core/session/session_persistence_service_test.dart`, and `test/e2e/gateway_identity_e2e_test.dart` cover the app-side identity contract.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Pairing handoff source trust classification — 2026-05-25
  resolved: 2026-06-07
  result: Android-originated handoffs now preserve source metadata into Flutter, distinguishing direct app-open links from shared text and QR/image imports for auto-connect policy.
  evidence: `PairingHandoffIntentParser` tags `ACTION_VIEW` and `ACTION_SEND`; `NavivoxPlatformConnectIntentPayload` normalizes payload/source; `SetupQrImageImport.source` feeds `PairingHandoffFlow.shouldAutoConnect()`.
  validation: `android/app/src/test/kotlin/com/trebuchetdynamics/navivox/pairing/PairingHandoffIntentParserTest.kt`, `test/features/servers/setup/sources/navivox_connect_intent_source_regression_test.dart`, and `test/features/servers/pairing/pairing_handoff_flow_test.dart` cover source classification and shared-text no-auto-connect behavior.
  owner: Navivox app owner / Android owner

[RESOLVED] Pairing handoff bridge-stopped recovery copy — 2026-05-25
  resolved: 2026-06-07
  result: Direct pairing-link auto-connect failures now show pairing-specific recovery copy, keep fields populated, stop retrying automatically, and do not render the token.
  evidence: setup applies `PairingHandoffSource.directAppOpen` through `PairingHandoffFlow`; failed direct app-open tests assert `Pairing needs attention` and `Could not connect from the pairing link.` without the secret token.
  validation: `test/features/servers/setup/flows/navivox_connect_and_talk_test.dart` covers direct app-open failure and token non-leakage.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Pairing handoff auto-connect and active-gateway confirmation — 2026-05-25
  resolved: 2026-06-07
  result: Direct app-open Pairing handoffs auto-connect only when no active gateway exists; shared text and QR/image imports fill fields and wait; active-gateway handoffs require confirmation before probing or switching.
  evidence: `PairingHandoffFlow.shouldAutoConnect()` gates on source plus active-gateway state, and `requiresActiveGatewayConfirmation()` covers every handoff source when a gateway is active.
  validation: `test/features/servers/pairing/pairing_handoff_flow_test.dart` and `test/features/servers/setup/flows/navivox_connect_and_talk_test.dart` cover no-active-gateway auto-connect, shared-text manual connect, and token-safe failure behavior.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Pairing handoff setup-intent routing — 2026-05-25
  resolved: 2026-06-07
  result: Pairing handoff imports now carry setup intent fields as suggestions and route to Config only after a successful connection with loaded config schema; Profile contact targets still take precedence, and unsupported config lands on the normal chat list.
  evidence: `SetupQrImageImport.setupIntent` carries `setup_entry_screen` / `setup_sections`; connection import parser preserves setup intent from core descriptors and JSON imports; `PairingHandoffLanding.navigationIntentAfterConnect()` maps config setup intent to `OpenConfig` only when `NavivoxChannelState.configSchema` is available.
  validation: `test/features/servers/shared/connection_import/connection_import_parser_test.dart` covers descriptor/JSON setup intent parsing; `test/features/servers/pairing/pairing_handoff_flow_test.dart` covers config capability gating and Profile contact precedence.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Optional Pairing handoff and reconnect Profile contact routing — 2026-05-25
  resolved: 2026-06-07
  result: Pairing handoff imports retain optional `server_id`/`profile_id` as post-connect route intent and open the Profile contact only when the connected gateway reports the exact contact.
  evidence: `SetupQrImageImport` carries `serverId`/`profileId`; `PairingHandoffFlow.afterConnect()` maps reported contacts to `OpenChatThread` and otherwise falls back to the chats list; manual connection edits reset the imported landing target.
  validation: `test/features/servers/pairing/pairing_handoff_flow_test.dart` covers successful target routing and manual-edit reset.
  owner: Navivox app owner / Gormes gateway owner

[BLOCKED] Durable Navivox connection credential storage — 2026-05-25 (interim slices landed 2026-06-17)
  problem: accepted Pairing handoff semantics should create/update durable Gormes gateway metadata after successful connection, but Pairing handoff tokens are bootstrap-only and Navivox currently has no secure device credential storage or gateway-issued durable credential protocol.
  evidence: setup/connect code keeps tokens in memory; `shared_preferences` is present for non-secret voice settings only; there is no secure storage dependency, token vault abstraction, device credential issuance endpoint, or reconnect proof flow yet.
  acceptance: keep Pairing handoff tokens short-lived/bootstrap-only; after successful authenticated setup on Android, durable credential issuance happens automatically immediately after authenticated connect plus capability/transport proof and before any chat turn, with clear non-modal post-connect notice/settings status such as `Reconnect saved for this gateway` and `Reconnect: saved on this Android install`, never showing token or credential IDs in normal UI; credential IDs may appear only in advanced diagnostics/export and should be redacted or truncated by default for support correlation without broad cross-context leakage; if issuance fails, Pairing handoff still completes because Navivox connected successfully, chat may continue session-only, and Navivox shows `Connected for this session; reconnect not saved` recovery copy without undoing the active connection; Navivox may perform one bounded automatic retry for transient issuance failure, then stops and leaves manual retry in gateway/settings, with no infinite retry loop; manual retry may use existing active session auth when still connected and scopes/capabilities allow issuance, otherwise it requires a new Pairing handoff; Gormes issues a revocable/rotatable Navivox device credential through a separate mutating endpoint, not `/v1/navivox/status`, bound to authenticated Gateway identity and a Navivox-generated random non-secret app install identity stored with non-secret metadata, not secure storage and not a hardware/user fingerprint; the endpoint accepts Pairing handoff token auth for initial issuance/re-pair and existing durable credential auth only for self-rotation of that install, not minting extra active credentials for other installs; self-rotation re-evaluates current gateway policy and caps new scopes to the previous credential scopes so rotation cannot expand privileges, while Pairing handoff re-pair can issue current default scopes, then invalidates the one-time Pairing handoff token when used or forces it to expire very soon; reinstalling Navivox creates a new install identity and therefore a new credential while old credentials remain server-revocable; static-token/admin auth remains separate legacy/non-Pairing semantics; gold-standard credential is one device public/private key challenge per Gormes gateway, with each private key kept in platform keystore/secure storage under an alias based on authenticated `gateway_id` when available, using a temporary local record ID only before Gateway identity proof, and each public key registered only with that gateway; challenge auth uses Gormes-issued short-TTL single-use nonces rather than signing arbitrary request bodies, with Navivox signing canonical JSON bytes, not ad-hoc concatenated strings, containing stable fields such as gateway ID, install ID, credential ID, nonce, issued/expiry times, intended action, and audience; Gormes/Navivox tests should include fixed signature vectors; Gormes consumes a nonce on successful auth, expires unused nonces quickly, rejects reuse, and may mark repeated reuse as suspicious; REST API calls and WebSocket auth should use short-lived non-persisted access/session tokens minted through the REST challenge flow and scoped to the gateway, credential, scopes, and stream connection where applicable; durable credential issuance and reconnect may use plain HTTP only for loopback/local bridge, while non-loopback exposure requires HTTPS or an authenticated private-network posture such as Tailscale/WireGuard as advertised by capability/status transport security metadata; Navivox refuses durable credential issuance/reconnect by default on insecure non-loopback HTTP instead of warning-only, though session-only Pairing handoff/manual connection may remain available for explicit recovery; Android/Termux same-device transport must use the explicit authenticated Pairing handoff `base_url`/`websocket_url` as connection authority and must not assume app loopback reaches Termux loopback or rewrite to loopback for persistence/security classification unless authenticated gateway capability/status confirms the exposure mode; descriptor URLs may be used only for preflight obvious-insecure checks, while final durable credential eligibility and transport-security classification must come from authenticated status/capabilities because descriptors can be stale or forged; Gormes status/capabilities should expose detailed transport-security fields plus a simple effective enum/gate, e.g. `effective_security` as `loopback`, `private_network`, `tls`, or `insecure`, `exposure_mode`, TLS flag, private-network provider such as Tailscale/WireGuard, and `durable_credentials_allowed`; REST access tokens default to about 5 minutes, while WebSocket stream tokens are single-connection-attempt tokens with no more than about 60 seconds to open the stream; active WebSocket lifetime is controlled by credential revocation/session close after handshake; Navivox repeats the challenge flow when tokens expire; first slice uses short TTL plus underlying credential revoke to invalidate minted tokens, not individual token revocation, avoiding per-request nonce signing and private-key signing inside WebSocket subprotocol/header constraints; key aliases never include base URLs, tokens, usernames, machine names, or paths; first durable persistence slice must include `/v1/navivox/capabilities` advertisement for durable credential support, including separate mutating credential issuance endpoint, supported auth methods such as `pairing_token` plus `device_key_challenge` or interim `device_bearer`, list/revoke endpoints, scope support, and explicit web durable reconnect unsupported/disabled status; credential scope storage in the Gormes credential record defaults the initial Android credential to the current Navivox capability set or a coarse `navivox` scope; include a minimal authenticated Gormes API to issue/list/revoke Navivox device credentials, capability-gated for Navivox clients and scoped so first-slice Navivox can view/revoke its own install while Gormes CLI/admin may list/revoke all installs; polished all-device management UI can follow later; revocation prevents all future auth immediately and should close active WebSocket streams authenticated by the revoked credential in the same slice when bounded, otherwise active stream closure is an explicit follow-up blocker before claiming full revocation semantics; Android durable reconnect is silent by default after app start, relying on platform keystore protection; config-admin actions do not require fresh Pairing handoff or per-action re-auth in the first slice and instead rely on credential scopes plus existing Gormes policy, with biometrics/device-unlock or re-auth for sensitive config/secret edits reserved for a future explicit high-security setting; first implementation should go straight to ECDSA P-256 with SHA-256 keypair challenge through a small Android-native keystore MethodChannel for non-exportable private-key generation/signing if Android secure key APIs are feasible from Flutter/native bridge, with RSA-PSS fallback only if Android compatibility testing forces it; Navivox sends public keys to Gormes as JWK protocol payloads with algorithm metadata such as `kty: EC`, `crv: P-256`, base64url `x`/`y`, `alg: ES256`, and optional Gormes-assigned `kid`; a simple bearer device token is allowed only as a bounded first slice if keypair integration is the schedule risk, explicitly marked interim in capabilities/docs, and stored only in platform secure storage; avoid broad secure-storage dependencies for the first Android keypair slice unless the native bridge proves impractical; future iOS support should use a parallel Keychain/Secure Enclave adapter; Navivox never stores device credentials in `shared_preferences`; persist non-secret Gateway identity/display/base URL/explicit WebSocket URL/last-connected/capability-summary metadata through a small repository abstraction backed initially by `shared_preferences`; durable credential metadata belongs only to gateway auth/admin state and local gateway metadata, never Gormes memory, Profile contact state, Transcript surface, sessions, run records, or agent-visible records; reconnect loads metadata plus secure credential, authenticates `/v1/navivox/status`, proves the same Gateway identity before updating metadata, and falls back to a new Pairing handoff if the credential is missing/expired/revoked even when Gateway identity matches; when the same `gateway_id` is restored without the public-key credential registry, Navivox keeps non-secret metadata and treats it as a known gateway that needs a new Pairing handoff; credential failure never auto-deletes gateway metadata, only explicit `forget gateway` does; if the Gateway identity changed after Gormes config reset, Navivox treats it as a different gateway; Gormes config reset invalidates durable credentials unless the operator explicitly preserves/restores both Gateway identity and credential registry during backup or migration; normal Gormes backup/export may include opaque-public Gateway identity by default for same-gateway restore/migration, while reset/regenerate identity must be explicit; Gormes backup/export must not include the credential registry by default, and should include it only in an explicit `preserve paired Navivox devices` mode; Flutter web gets no durable credential issuance or silent durable reconnect initially and may keep only session credentials plus non-secret metadata unless a future explicit opt-in/risk-copy design is accepted; Navivox `forget gateway` tries remote revocation for only this app install credential by default when reachable, always deletes local credential and non-secret metadata, and reports when remote revoke is pending/unconfirmed without retaining local secrets; normal UI does not offer a separate `forget local only` action because it invites leftover server grants, though emergency/debug wording can explain local deletion if remote revoke is unreachable; all-device revoke is reserved for Gormes CLI/admin or future management UI, and first durable credential slice should include Gormes CLI emergency commands to revoke this install, revoke by credential ID, and revoke all Navivox credentials for the gateway, with confirmation prompts for destructive actions and `--yes` support for scripted exact/broad revokes; Navivox UI `forget gateway` uses its own confirmation sheet; add tests proving secrets are not written to shared preferences, browser local storage, logs, routes, notices, screenshots, or transcript state.
  progress (2026-06-17): the non-device-gated, interim `device_bearer` slices are implemented and validated.
    - Gormes (gormes-agent, commit `5e6ef089f`, merged to `development` as `41009dcbe`): `/v1/navivox/device-credentials` issue/list and `/v1/navivox/device-credentials/revoke`, interim `device_bearer` credentials bound to gateway id + app-install id, secrets hashed (never stored raw or logged), idempotent revoke, transport-gated; `capability.DurableReconnect` advertises `supported` with issue/list/revoke endpoints + auth methods (`pairing_token`, `device_bearer`) + scopes + `interim` on safe transport (loopback/TLS/private-network), fail-closed otherwise; exported `DurableReconnectSecurityAllowed`. 76 navivox Go tests pass.
    - Navivox (navivox-app `main`): client parses `durable_reconnect` into `DurableReconnectReadinessContract`; `ReconnectReadiness` surfaced in the gateway-status Manage sheet (active gateway only); `NavivoxGatewayClient.issueDeviceCredential`; `DurableReconnectIssuanceCoordinator` runs issue → store → readiness with one bounded retry after authenticated connect, failures stay session-only ("Connected for this session; reconnect not saved") without undoing the connection; `DurableCredentialStore.saveCredential` write seam with a no-op `Empty` default so production never writes a secret insecurely.
  remaining/blocker: the real Android secure-storage `DurableCredentialStore` backing, the ECDSA P-256 keystore keypair (`device_key_challenge`), `device_bearer`-as-request-auth silent reconnect, and live validation are device-gated — they need a responsive Android target (same blocker as the Android live-smoke item). With the default no-op store, production readiness honestly stays session-only ("available, not saved"), never falsely "saved". Gormes Slice 1 is merged to `development` (`41009dcbe`).
  owner: Navivox app owner / Gormes gateway owner / security owner / local Android test environment
  next check: inject an Android secure-storage credential store + keypair challenge once a responsive Android target is available.

[RESOLVED] Android release signing config — 2026-05-24
  resolved: 2026-06-07
  result: Android release builds now select a keystore-backed signing config when ignored local properties or CI environment secrets provide all release signing values, while retaining debug signing only as an explicit local smoke-build fallback.
  evidence: `android/app/build.gradle.kts` reads `navivox.release.*` local properties or `NAVIVOX_RELEASE_*` environment variables; `docs/runbooks/android/release-handoff.md` documents local/CI signing setup and warns not to distribute fallback debug-signed release artifacts; `android/.gitignore` excludes `local.properties`, `key.properties`, `*.keystore`, and `*.jks`.
  validation: `./android/gradlew -p android :app:tasks` passed; `flutter build apk --release` passed and produced `build/app/outputs/flutter-apk/app-release.apk`.
  owner: Navivox app owner / release owner

[RESOLVED] Navivox config-admin channel wiring — 2026-05-24
  resolved: 2026-06-07
  result: Navivox now has typed config-admin client methods, channel refresh/apply wiring, capability gating, validation/diff/apply flow handling, and redacted config UI behavior.
  evidence: `NavivoxGatewayClient.configAdminSchema/configAdminValues/diffConfigAdmin/validateConfigAdmin/applyConfigAdmin`; `GatewayNavivoxChannel.refreshConfigAdmin()` and `applyConfigAdmin()`; `sendConfigSet()` / `sendConfigSecretSet()` route through config-admin when advertised.
  validation: `test/core/gateway/config_admin/navivox_gateway_config_admin_test.dart`, `test/core/gateway/navivox_gateway_protocol_test.dart`, `test/core/channel/gateway/runtime/channel_test.dart`, `test/features/config/apply/*`, and `test/features/config/screens/config_screen_test.dart` cover client, channel, validation, reload/pending state, and UI flows.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Navivox voice-profile settings wiring — 2026-05-24
  resolved: 2026-06-07
  result: Navivox now reads and validates Gormes profile-scoped voice profiles, capability-gates the config voice-profile card, surfaces provider/credential state without secrets, and preserves local continuous voice semantics.
  evidence: `NavivoxGatewayClient.voiceProfiles()` and `.validateVoiceProfile()`; `GatewayNavivoxChannel.voiceProfiles()` and `.validateVoiceProfile()`; `ProfileVoiceProfileCard`, coordinator, and presentation modules.
  validation: `test/core/gateway/voice/navivox_gateway_voice_test.dart`, `test/core/gateway/navivox_gateway_protocol_test.dart`, `test/features/profiles/*profile_voice_profile*`, and `test/features/config/screens/profile_voice_profile_test.dart` cover read/validate, invalid provider, missing credential/recovery copy, and UI apply behavior.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Connect screen simplification — 2026-06-17
  resolved: 2026-06-18
  result: Setup screen now leads with QR import as the primary full-width action; manual entry (Gateway URL + token + Connect) is behind a collapsed ExpansionTile that auto-expands on import. Separate address and port fields replaced by a single Gateway URL field. Token visibility toggle moved from a standalone TextButton to a suffixIcon IconButton inside the token field. "Copy fix instructions" removed from the main card and moved to the help card. 39/39 tests pass, 0 analyze issues.
  evidence: `lib/features/servers/screens/setup_screen.dart` (layout + state), `lib/features/servers/setup/presentation/screen/setup_screen_presentation.dart` (urlField labels, enterManuallyLabel), `lib/features/servers/setup/guide/setup_guide_presentation.dart` (visibleEntries includes navivoxPairHandoff); `test/features/servers/setup/` all updated.
  commits: `c917d98`–`e590b58` (8 commits, plan at `136acd8`).
  owner: Navivox app owner

[PLANNED] Navivox approval response protocol — 2026-05-24 (real scope traced 2026-06-17)
  problem: Navivox renders approval-required events, but `GatewayNavivoxChannel` cannot resolve approve/deny choices durably; current approval actions still report that tool approvals are unavailable.
  evidence: Gormes Navivox capabilities advertise `approval_required` events and `gormes-agent/internal/adapters/channels/navivox/channel.go` has `SendApprovalRequired`, but no Navivox approve/deny surface is advertised for returning the operator decision.
  scope finding (2026-06-17): this is NOT a localized Navivox adapter change. Audit of gormes-agent shows the interactive approval mechanism is a context-injected, BLOCKING `ApprovalCallback` (`internal/tools/approval/callback/callback.go`; tools call it and wait, absence = noninteractive fail-closed). NO production code injects `WithApprovalCallback` today — no channel (Telegram included) or gateway wires interactive approval; `SendApprovalRequired` is never called outside tests. Navivox turns are decoupled from tool execution: `handleTurn` -> `enqueueTurn` drops a `gateway.InboundEvent{EventSubmit}` on the gateway inbox, and the gateway/agent runtime runs the turn (and its tools) elsewhere with no path back to the originating channel. So delivering operator approve/deny requires first-of-its-kind plumbing of an interactive approval callback through the gateway turn-execution path back to the submitting channel — a core-runtime, concurrency- and safety-sensitive change spanning `internal/app/gateway` + the agent run context + per-channel callback routing, well beyond a navivox endpoint.
  decision (2026-06-17): resolve transport = dedicated HTTP endpoint (e.g. `POST /v1/navivox/approvals/{id}` approve/deny), matching the existing request/response surfaces (config-admin, device-credentials), over a WebSocket stream control message (the stream is server->client only today).
  acceptance: (Gormes Slice 1) inject an `ApprovalCallback` into the gateway run path for channel-originated turns that routes back to the submitting channel; in the Navivox channel keep a pending-approval registry keyed by `approval_id`, have the callback emit `approval_required` and block awaiting the decision with a bounded timeout and ctx-cancel -> fail-closed (deny); add the HTTP resolve endpoint that matches the decision by id, rejects stale/unknown ids safely, and never logs tool payloads/secrets; advertise the approve/deny endpoint in `/v1/navivox/capabilities`; (Navivox Slice 2) typed client + `respondToApproval` channel adapter only for the advertised surface, preserve risk copy and approval IDs, reject stale/missing IDs, tests for approve, deny, stale/missing id, timeout fail-closed, and no-secret logging.
  owner: Navivox app owner / Gormes gateway owner / agent-runtime owner
  next check: Gormes-side design/plan for gateway->channel interactive-approval routing (worth a brainstorm/plan pass given core-runtime + safety) before implementation.

[RESOLVED] Gateway session and run-record UI wiring — 2026-05-24
  resolved: 2026-06-07
  result: Navivox now capability-gates run-record inspection, fetches redacted run-record snapshots through the channel, and exposes transcript/voice diagnostics UI with unavailable fallback behavior.
  evidence: `NavivoxGatewayClient.sessions()` / `.session()` / `.runRecord()`; `GatewayNavivoxChannel.runRecord()`; `runRecordInspectionAvailable` in channel state; transcript message actions call `channel.runRecord()`; `ProfileVoiceProfileCard` can inspect profile voice run evidence.
  validation: `test/core/gateway/navivox_gateway_protocol_test.dart`, `test/core/channel/gateway/runtime/channel_test.dart`, `test/features/chat/actions/chat_run_record_inspection_test.dart`, and `test/features/config/screens/profile_voice_profile_test.dart` cover client reads, capability gating, UI inspection, and unavailable behavior.
  owner: Navivox app owner / Gormes gateway owner

[PLANNED] Composer attachment upload and media picker wiring — 2026-05-24
  problem: the Transcript surface exposes Telegram-style attachment rows, but local file upload, photo/video picking, workspace-file selection, and Gormes gateway handoff are not durable product behavior yet.
  evidence: `lib/features/chat/transcript_composer_presentation.dart` exposes upload/media rows as disabled-copy affordances; current composer tests verify the sheet and row presentation but not upload dispatch. Updated Gormes Navivox capabilities advertise attachments as unavailable until `/v1/navivox/uploads`, with raw local paths rejected.
  acceptance: wait for Gormes to advertise opaque upload IDs and a non-empty MIME allowlist, keep secrets/local file paths out of logs, add tests for operator intent emission, and wire upload/media rows only when the capability document enables `/v1/navivox/uploads`.
  owner: Navivox app owner / Gormes gateway owner
  next check: next attachment/upload slice.

[RESOLVED] Navivox Profile contact create-from-seed UI wiring — 2026-05-24
  resolved: 2026-06-07
  result: Agents/Profile contact creation now opens a capability-gated create-from-seed sheet, uses the channel/client profile-seed endpoint, requires explicit workspace confirmation, refreshes contacts after apply, and keeps local paths/tokens out of normal logs/UI echoes.
  evidence: `NavivoxChannel.profileSeed()`; `GatewayNavivoxChannel.profileSeed()`; `NavivoxGatewayClient.profileSeed()`; `ProfileSeedSheet`; Agents and Profile Contacts screens expose `Create from seed` actions.
  validation: `test/core/gateway/navivox_gateway_protocol_test.dart`, `test/features/profiles/profile_seed_flow_test.dart`, `test/features/profiles/actions/profile_seed_coordinator_test.dart`, `test/features/agents/screens/agents_screen_test.dart`, and `test/features/chat/profile_contacts/profile_contact_list_test.dart` cover client payloads, UI draft/apply, workspace confirmation, and contact selection after apply.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Gormes Navivox API capability contract — 2026-05-24
  resolved: 2026-05-24
  evidence: updated `../gormes-agent/internal/channels/navivox/capabilities.go` and `capabilities_test.go` add authenticated `/v1/navivox/capabilities`, link it from `/v1/navivox/status`, advertise `/healthz`, `/v1/navivox/profile-seed`, attachment unavailability, voice profile support, canonical `/v1/navivox/stream`, and dashboard-profile API deprecation.
  validation: Navivox app now parses `capabilities_url` plus the capability document shape in `test/core/gateway/navivox_gateway_protocol_test.dart`; full app validation remains green.
  owner: Navivox app owner / Gormes gateway owner

[RESOLVED] Web setup accessibility blocks keyboard/screen-reader connect flow — 2026-05-22 18:55 CST
  resolved: 2026-05-22 19:25 CST
  evidence: setup now exposes `Gateway base URL field`, `Pairing token field`, `Import pairing QR image`, `Show pairing token`, and `Connect and talk` in the Flutter web accessibility tree after semantics activation; `agent-browser find text "Connect and talk" click`, `find text "Import pairing QR image" click`, and `find text "Show pairing token" click` all exited 0; pressing Enter in the token field fired `/v1/navivox/status`.
  validation: `flutter analyze`, `flutter test`, `flutter build web`, and browser QA against `http://127.0.0.1:8765/#/setup` passed for this slice.
  owner: Navivox app owner / Mineru

[BLOCKED] Android Pairing handoff and continuous voice live smoke — 2026-05-27
  blocker: this host has no responsive Android target; previous emulator attempts timed out on `adb shell`, so it cannot prove Android OS intent delivery, microphone permission prompts, speech-recognition availability, or a real live phrase capture.
  evidence: Pairing handoff has Dart/JVM/provider-smoke coverage and optional/manual docs in `docs/runbooks/android/pairing-handoff-smoke.md` and `docs/runbooks/android/pairing-handoff-instrumentation.md`; continuous voice docs in `docs/runbooks/android/release-handoff.md` still say this host is not valid Android evidence until a responsive target is connected.
  acceptance: on a responsive physical USB-debuggable Android device or healthy emulator, run Pairing handoff before voice. Tier 1 import-only: `ACTION_SEND text/plain` with a sentinel Pairing handoff token fills setup fields, waits for operator action, and does not render the token; QR/image import follows the same no-probe/no-token-leak rule. Direct `ACTION_VIEW navivox://connect?...` is tested only with active-gateway confirmation or another no-probe guard unless a live gateway is intentionally available. Tier 2 live Gormes: direct cold-start and warm-start `ACTION_VIEW` connect successfully, active-gateway handoff asks confirmation before probing/switching, requested Profile contact landing happens only when reported by the gateway, and tokens/credentials do not appear in UI, screenshots, logs, transcripts, or failure copy. Continuous voice: after live Pairing handoff, `adb shell true` returns quickly, at least one Android `RecognitionService` exists, an online Profile contact opens, mic permission is granted if prompted, one short spoken phrase becomes a local transcript bubble and a sent Gormes turn, with no token/credential leakage.
  owner: local Android test environment / Juan.
  workaround/pivot: keep code-level `flutter analyze`, `flutter test --concurrency=1`, JVM Pairing handoff parser tests, and optional provider smoke green until a responsive Android target is available.
  next check: rerun with responsive Android target before claiming Android runtime validation.

[RESOLVED] Commit/push screenshot iteration 1 repeated agent-list message slice — 2026-05-22 16:23 CST
  resolved: 2026-05-22 16:45 CST
  evidence: `flutter analyze`, `flutter test`, and `git diff --check` all passed after fixing stale E2E/control finders, the profile-contact back-button expectation, and the README setup screenshot golden.
  owner: Navivox app owner / Mineru

[BLOCKED] Commit/push Transcript surface plan and context updates — 2026-05-20 20:56 CST
  blocker: `navivox-app` is untracked inside parent repo `/home/xel/git/sages-openclaw`, so committing only plan/context files would add a partial project tree.
  evidence: `git rev-parse --show-toplevel` => `/home/xel/git/sages-openclaw`; `git status --short -- /home/xel/git/sages-openclaw/workspace-mineru/navivox-app` => `?? workspace-mineru/navivox-app/`.
  unblocks when: Juan or owning agent decides whether `navivox-app` should be tracked whole, moved to its own repo, added as a submodule, or ignored.
  owner: Juan / repository owner
  workaround/pivot: saved the implementation plan and context wording without staging a partial commit; wait for ownership decision before commit/push.
  next check: 2026-05-21 10:00 CST

[BLOCKED] Run Subagent-Driven Development execution — 2026-05-20 21:02 CST
  blocker: current pi harness has no Agent/subagent dispatch tool, so fresh implementer/spec-review/code-review subagents cannot be launched.
  evidence: available tool surface in this session is file/command tools (`read`, `bash`, `edit`, `write`, `multi_tool_use.parallel`); no Agent/TodoWrite dispatch tool is exposed.
  unblocks when: this work runs in a subagent-capable harness, or Juan approves switching to inline execution.
  owner: harness / Juan
  workaround/pivot: prepared Task 1 subagent dispatch packet at `docs/superpowers/plans/2026-05-20-transcript-surface-task1-subagent-packet.md`.
  next check: 2026-05-21 10:00 CST

[BLOCKED] Commit/push Voice run lifecycle spec — 2026-05-21 08:34 CST
  blocker: `navivox-app` is untracked inside parent repo `/home/xel/git/sages-openclaw`, so committing the Voice run spec would require adding a partial project tree.
  evidence: `git status --short -- workspace-mineru/navivox-app` => `?? workspace-mineru/navivox-app/`.
  unblocks when: Juan or owning agent decides whether `navivox-app` should be tracked whole, moved to its own repo, added as a submodule, or ignored.
  owner: Juan / repository owner
  workaround/pivot: saved design spec at `docs/superpowers/specs/2026-05-20-voice-run-lifecycle-design.md` without staging a partial commit.
  next check: 2026-05-21 10:00 CST

[BLOCKED] Commit/push Voice run lifecycle implementation plan — 2026-05-21 08:39 CST
  blocker: `navivox-app` is untracked inside parent repo `/home/xel/git/sages-openclaw`, so committing the implementation plan would require adding a partial project tree.
  evidence: `git status --short -- workspace-mineru/navivox-app` => `?? workspace-mineru/navivox-app/`.
  unblocks when: Juan or owning agent decides whether `navivox-app` should be tracked whole, moved to its own repo, added as a submodule, or ignored.
  owner: Juan / repository owner
  workaround/pivot: saved implementation plan at `docs/superpowers/plans/2026-05-21-voice-run-lifecycle.md` without staging a partial commit.
  next check: 2026-05-21 10:00 CST

[BLOCKED] Commit/push Voice run lifecycle implementation — 2026-05-21 08:53 CST
  blocker: `navivox-app` is untracked inside parent repo `/home/xel/git/sages-openclaw`, so committing the validated implementation would require adding a partial project tree.
  evidence: `git status --short -- workspace-mineru/navivox-app` => `?? workspace-mineru/navivox-app/`; full `flutter test` passed locally after the implementation.
  unblocks when: Juan or owning agent decides whether `navivox-app` should be tracked whole, moved to its own repo, added as a submodule, or ignored.
  owner: Juan / repository owner
  workaround/pivot: completed and validated the client-local Voice run lifecycle implementation without staging a partial commit.
  next check: 2026-05-21 10:00 CST

[BLOCKED] Commit/push navivox-loop iteration 1 voice failure-reason slice — 2026-05-21 09:01 CST
  blocker: `navivox-app` is untracked inside parent repo `/home/xel/git/sages-openclaw`, so committing the validated iteration slice would require adding a partial project tree.
  evidence: `git status --short -- workspace-mineru/navivox-app` => `?? workspace-mineru/navivox-app/`; `flutter analyze`, `flutter test`, and `git diff --check -- workspace-mineru/navivox-app` all exited 0 in this iteration.
  unblocks when: Juan or owning agent decides whether `navivox-app` should be tracked whole, moved to its own repo, added as a submodule, or ignored.
  owner: Juan / repository owner
  workaround/pivot: completed the timeout failure-reason slice and left it unstaged.
  next check: 2026-05-21 10:00 CST

[BLOCKED] Navivox full gate for Pi delivery-loop extension — 2026-05-21 08:52 CST
  blocker: full repo gate is red from pre-existing Navivox app test/model drift and unrelated root whitespace outside the extension slice.
  evidence: `flutter analyze` reports undefined getter `voiceCapability` in `app/test/core/channel/gateway_navivox_channel_test.dart:149-152`; `flutter test` reports `loads profile contacts from snapshot and applies gateway updates` expected `available`, actual `unavailable`; unscoped `git diff --check` reports trailing whitespace in `.sisyphus/plans/gormes-port-master-plan.md:3` and `:5`.
  unblocks when: Navivox voice capability expectations are reconciled with `NavivoxProfileContact`, and unrelated root whitespace is fixed or excluded by an agreed gate scope.
  owner: Navivox app owner / root workspace owner
  workaround/pivot: completed the extension slice with focused contract test and scoped `git diff --check -- workspace-mineru/navivox-app`; did not modify unrelated app/root WIP.
  next check: 2026-05-21 10:00 CST
